# Design Brief — DataLink Automation Platform

**版本**：v1.0
**日期**：2026-06-06
**状态**：已确认

---

## 产品概要

- **产品名称**：DataLink Automation Platform
- **产品类型**：Desktop（Electron）
- **目标用户**：华为 Datalink 系统配置人员（运维/方案工程师），技术背景强，长时间使用
- **核心功能**：AI 提示词驱动浏览器自动操作、作业流录制与回放、内置 Datalink 浏览器、作业流管理

---

## 设计方向

### 情绪关键词

- **工程感** — 工具属性第一，UI 为任务服务，不为观赏存在，像指挥台而非展示台
- **精准** — 每个状态变化清晰可辨，执行中/成功/失败一眼分明，不用猜
- **克制** — 没有无意义的装饰，动效只用于传递信息，不用于取悦

### 参考产品

| 参考产品 | 喜欢的方面 |
|---------|-----------|
| Cursor IDE | 深黑底色、分屏布局、极简工具栏、信息密度高不杂乱 |
| Grafana 暗夜仪表盘 | 橙色/琥珀色状态指示，深色背景里高辨识度 |
| Linear | 行式列表、紧凑排版、操作直接无废话 |

### 反面参考

- **企业后台风格**：不要 Ant Design / Element UI 的蓝白管理后台感
- **消费品 AI 聊天风格**：不要 ChatGPT / Claude Web 那种大圆角气泡、大留白的亲切感
- **Grafana 仪表盘全貌**：只借其配色逻辑，不要多面板全铺开的复杂度

---

## 视觉规范

### 色彩方向

- **主题模式**：深色，仅支持深色（企业内部工具，不需要浅色切换）
- **色彩温度**：暖色强调 + 中性底色
- **底色方向**：近纯黑，`#0B0B0C` 到 `#141414` 区间，真黑而非深灰
- **强调色方向**：琥珀橙，用于操作按钮、执行中状态、交互焦点，`#F97316` 附近（Tailwind orange-500 色系）
- **状态色体系**：
  - 执行中：琥珀橙
  - 成功：低饱和绿（不要鲜绿，像终端 `#4ADE80` 降饱和）
  - 失败/错误：红（`#EF4444` 附近）
  - 等待：灰（`#52525B`）
- **品牌资产**：无，从零构建

### 信息密度

- **密度方向**：紧凑
- **参考基准**：Cursor IDE / VS Code 文件树的行密度
- **理由**：目标用户是工程师，单次会话有多条执行日志 + 作业流步骤，需要高密度信息展示

### 排版方向

- **字体气质**：几何无衬线（工程感），英文优先 — Inter / JetBrains Mono（代码/日志区域）
- **中文字体**：系统默认（`-apple-system` / `Microsoft YaHei`）
- **标题风格**：与正文差异适中，不用大号标题，用粗细 + 大小写区分层级

### 交互风格

- **动画程度**：极少——只有状态变化时的颜色过渡
- **过渡效果**：快速 opacity/color 过渡（`100ms~150ms`），无弹跳无缩放
- **整体节奏**：沉稳克制，像 CLI 工具而非消费品 App

---

## 核心页面视觉备注

### 主界面（左侧对话面板 + 右侧浏览器面板）

- **核心交互**：左右分屏，左侧 AI 执行日志 + 用户输入，右侧内置 Datalink 浏览器
- **视觉方向**：分屏布局参考 Cursor — 顶部窄工具栏 + 主内容区左右分割，分割线用 1px 深色边框
- **左侧对话区**：无气泡，纯文本块，AI 消息与用户消息靠左侧色条或缩进区分（像终端输出）
- **右侧浏览器区**：简洁浏览器 chrome（地址栏 + 前进后退），浏览器内容区域全高显示

### 执行步骤进度（对话消息内嵌）

- **核心交互**：AI 执行时，消息内展示逐步推进的步骤列表
- **视觉方向**：竖排步骤行，每行左侧一个状态点（⬜等待 → 🟠执行中 → ✅成功 → ❌失败），靠颜色变化传达状态，无进度条动画
- **特殊要求**：步骤描述用等宽或接近等宽字体，像 `log` 输出

### 作业流列表（抽屉/浮层）

- **核心交互**：从工具栏打开侧边抽屉，展示所有保存的作业流脚本
- **视觉方向**：行式列表，参考 Linear Issue 列表 — 每行：名称 + 步骤数 + 最后运行时间 + 操作图标
- **特殊要求**：不用卡片，不用阴影，靠 hover 背景色区分交互行

### 错误覆盖层（Error Overlay）

- **核心交互**：执行失败时弹出，展示失败步骤、错误信息、截图引用
- **视觉方向**：半透明遮罩 + 居中深色面板，红色边框或左侧红色竖条标记，内容紧凑，有"重试"和"关闭"两个操作
- **特殊要求**：不要大号图标，不要插画，直接展示错误代码/选择器信息

### 工具栏（顶部）

- **核心交互**：应用标题 + 录制控制 + 作业流库入口 + 速度控制 + 重置
- **视觉方向**：窄高（32-36px），深色背景，图标 + 短文字标签，橙色用于"录制中"高亮状态

---

## 状态设计

- **空状态**：纯文案引导，无插画。例："暂无作业流 — 点击录制按钮开始录制第一个"
- **加载状态**：AI 执行时用步骤状态点变化传达进度，无骨架屏，无旋转圈
- **错误状态**：Error Overlay 浮层（见上），内联错误用红色文字 + 左侧红色竖条，不用弹窗
