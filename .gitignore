# === cc-base 白名单：base 仓库只放框架 ===
# 默认忽略根目录一切；只放行下面列出的框架文件/目录。
# 实验/产品代码（frontend/src/electron/Product-Spec…）本地可留着做实验，
# 但默认不上库——冒出任何新文件都自动挡，无需逐个加黑名单。
/*
!/.gitignore
!/.claude/
!/ARCHITECTURE.md
!/README.md
!/progress.md
