# Product Spec — DataLink Automation Platform

**版本**：v1.0  
**日期**：2026-06-06  
**状态**：草稿

---

## 一、产品定位

### 一句话定位
一个本地 Electron 桌面应用，通过提示词驱动浏览器自动操作华为内部 Datalink 系统，让用户用自然语言完成复杂的页面节点编排和数据流配置，并将操作过程录制成可复用的作业流脚本。

### 核心痛点
- Datalink 2万+用户，但门槛极高——20-30种页面节点类型，每类配置表单复杂如IDE（多字段、多Tab、滚动、动态加载）
- 每个局点规则不同，官方模板形同虚设，仍需人工逐一配置
- 没有 CLI 接口，只能人肉浏览器操作，重复劳动多
- 新人上手成本高，配置过程难以传授和复现

### 核心价值
- 用提示词描述意图 → AI 驱动浏览器自动完成操作
- 操作过程自动录制为可回放的作业流脚本
- 积累每种节点类型的操作脚本库
- 下次配同类节点 → 提示词描述 + 参数微调 → 自动回放

---

## 二、目标用户

**主要用户**：Datalink 系统的配置人员（运维/方案工程师）  
**典型场景**：为某局点创建一套完整的 Datalink 工作流（包含多个页面节点）

**用户特征**：
- 熟悉业务但不擅长写脚本
- 需要频繁在 Datalink 里创建相似的应用和页面配置
- 有使用 Claude Code 或类似 AI 工具的基础

---

## 三、典型使用场景

**场景**：为某局点（如某城市 DU 项目）创建 Datalink 应用，配置以下工作流：
1. Upload LLD File（文件传入节点）
2. Box Information（表单数据节点）
3. New Duct（表单数据节点）
4. DU Details（表单数据节点）
5. Output（输出节点）

**目前手动需要**：登录 → 新建应用 → 依次拖入页面节点 → 每个节点配置10-20个字段 → 约 1-2小时/套

**自动化后**：描述需求 → 回放对应作业流 → 按提示微调参数 → 约 10-15分钟/套

---

## 四、技术架构（已确认）

| 维度 | 决策 |
|------|------|
| 产品类型 | Electron 桌面应用 |
| 浏览器控制 | chrome-devtools-mcp + CDP 协议 |
| LLM | Claude API（团队共享 Key，配置在应用设置里）|
| 录制方式 | 拦截 MCP tool call 序列（工具名 + 参数 + 语义快照）|
| 元素定位 | 语义定位优先（按钮文字、aria-label、role），失败时 AI 重新定位 |
| 交互方式 | V1：对话式（聊天框 + 内置浏览器）；V2：可视化节点画布 |
| 登录处理 | 首次手动（用户名密码 + 手机验证码），后续 Cookie 自动登录 |
| 错误处理 | 报错立即终止 + 截图 + 日志文件 |
| 本地存储 | 作业流 JSON 存本地磁盘（用户指定目录）|
| 分享机制 | V1：JSON 文件手动分发；V2：中心化平台上传/下载 |

---

## 五、功能范围（V1）

### 5.1 核心功能

#### F1 — 内置浏览器 + 对话面板
- Electron 窗口分为两个区域：左侧对话面板（聊天框）+ 右侧内置浏览器（目标：Datalink Web 应用）
- 内置浏览器启动时带 `--remote-debugging-port=9222`，供 chrome-devtools-mcp 连接
- 用户在对话面板用自然语言发出指令，AI 驱动浏览器执行

#### F2 — 提示词驱动操作
- 用户输入提示词（如"帮我在当前应用里新建一个 Box Information 表单节点"）
- 系统将提示词发送给 Claude API，Claude 通过 MCP tool_use 调用 chrome-devtools-mcp 的工具（click/fill/snapshot 等）
- 每次操作完成后，在对话面板展示执行结果（成功/失败 + 截图）

#### F3 — 录制与作业流生成
- 用户点击"开始录制"按钮 → 系统拦截所有 MCP tool call
- 每条录制记录包含：
  - 工具名（click/fill/select/scroll 等）
  - 元素参数（uid + 语义信息：文字、label、role、位置快照）
  - 输入值（如有）
  - 执行前的页面状态快照（截图 base64 的引用路径）
- 用户点击"停止录制并保存"→ 弹出命名窗口 → 保存为 `.json` 作业流文件

#### F4 — 作业流管理
- 作业流列表页：展示本地存储的所有 `.json` 作业流
- 支持：查看、重命名、删除、导入（选择本地 JSON 文件）、导出
- 作业流元数据：名称、描述、创建时间、最后运行时间、步骤数、目标节点类型标签

#### F5 — 作业流回放
- 用户从作业流列表选择一个 → 点击"回放"
- 系统按顺序重新执行录制的 MCP tool call 序列
- 元素定位策略：
  1. 用语义信息（文字、label、role）在当前页面 a11y tree 里重新定位元素
  2. 定位成功 → 执行操作，继续下一步
  3. 定位失败 → 触发 AI 重定位（Claude 看当前 snapshot 找对应元素）
  4. AI 也找不到 → 立即报错终止，截图保存，写错误日志
- 回放过程在对话面板实时展示进度（步骤 X/N：正在执行...）

#### F6 — 参数化回放（可选，V1 如时间允许）
- 作业流中的 fill 步骤的值可标记为"变量"
- 回放前弹出参数输入对话框，用户填入本次的具体值
- 适用场景：同类型局点的不同 City/CO/Sector 值替换

#### F7 — 登录管理
- 首次使用：提示用户手动在内置浏览器里完成 Datalink 登录（SSO + 短信验证码）
- 登录完成后，系统自动保存 Cookie（加密存储在本地）
- 后续启动：自动注入 Cookie，跳过登录步骤
- 支持登出和重新登录

#### F8 — 错误日志与截图
- 每次执行（录制/回放）都生成一个日志文件（`logs/YYYY-MM-DD_HH-mm-ss.log`）
- 日志包含：每步的执行状态、耗时、错误信息
- 错误时自动截图，保存到 `screenshots/error_YYYY-MM-DD_HH-mm-ss.png`
- 日志查看器：应用内可查看历史日志列表

#### F9 — 设置
- Claude API Key 配置（团队共用，输入后加密存储）
- Claude 模型选择（默认 claude-haiku-4-5 用于简单操作，claude-sonnet-4-6 用于复杂定位）
- 作业流存储目录配置
- Datalink 目标 URL 配置（适配不同环境的内部地址）

---

### 5.2 Datalink 节点类型支持范围（V1）

基于截图观察，V1 优先支持以下页面节点类型的操作录制和回放：

| 节点类型（Datalink 中文名）| 对应操作类型 | 典型字段特征 |
|---------------------------|-------------|-------------|
| 空白页面 | 新建空白表单 | 无预设字段 |
| 文件传入 | 上传 Excel 文件 | 文件选择 + 筛选条件（City/CO/Sector/Zone）|
| 表单数据 | 数据录入表单 | 多行文本字段（如 Box Info、New Duct）|
| 人工任务数据传入 | 手工数据导入 | 任务 ID + 数据来源 |

其他节点类型（Output、email Notice、数据流配置等）在 V1 录制中会被记录，但不提供专项的语义优化，AI fallback 兜底。

---

## 六、不做（V1 明确排除）

- 中心化分享平台（V2）
- 可视化节点画布（V2）
- 多人协作 / 权限管理
- Datalink 以外的其他 Web 系统支持
- 数据流算法自动生成（太复杂，先做操作层）
- 自动测试/验证作业流执行结果的正确性

---

## 七、文件结构

```
~/datalink-automation/        # 默认存储根目录（用户可改）
├── workflows/                # 作业流 JSON 文件
│   ├── upload-lld-file.json
│   ├── box-information.json
│   └── new-duct.json
├── logs/                     # 执行日志
│   └── 2026-06-06_10-30-00.log
└── screenshots/              # 错误截图
    └── error_2026-06-06_10-35-00.png
```

**作业流 JSON 格式**（示意）：
```json
{
  "id": "uuid",
  "name": "Box Information 节点配置",
  "description": "配置 Box Information 表单，包含 DU Boundary、City 等字段",
  "tags": ["表单数据", "Box Info"],
  "createdAt": "2026-06-06T10:30:00Z",
  "targetUrl": "https://datalink.internal/...",
  "steps": [
    {
      "seq": 1,
      "tool": "click",
      "semantic": { "text": "新建", "role": "button", "label": "新建页面" },
      "uid": "a3f2",
      "screenshotRef": "step_001.png"
    },
    {
      "seq": 2,
      "tool": "fill",
      "semantic": { "label": "Du boundary", "role": "textbox" },
      "uid": "b7c1",
      "value": "{{du_boundary}}",
      "isVariable": true
    }
  ]
}
```

---

## 八、验收标准（V1 上线门槛）

1. 用户能在内置浏览器里完成 Datalink 手动登录，系统自动保存 Cookie
2. 用户能通过提示词驱动 AI 完成至少一个"文件传入"类型节点的全程配置
3. 录制功能能捕获上述操作的完整 MCP tool call 序列，保存为 JSON 文件
4. 回放功能能在相同页面状态下准确重放上述操作（成功率 ≥ 80%）
5. 出错时能正确截图、写日志、展示错误信息，不崩溃
6. 作业流列表能展示、导入、导出 JSON 文件

---

## 九、技术依赖

| 依赖 | 用途 | 版本要求 |
|------|------|---------|
| Electron | 桌面应用框架 | ≥ 28 |
| React | 对话面板和 UI | ≥ 18 |
| @modelcontextprotocol/sdk | MCP Client | 最新 |
| chrome-devtools-mcp | 浏览器控制 MCP Server | 最新 |
| @anthropic-ai/sdk | Claude API 调用 | 最新 |
| electron-store | 本地加密配置存储 | ≥ 8 |

---

## 十、后续版本规划

**V2**：可视化节点画布编辑器 + 中心化作业流分享平台  
**V3**：数据流配置自动化（算法公式、外部脚本节点）  
**V4**：多 Web 系统支持（其他内部工具）
