# Project: cc-base（Claude Code 单机框架脚手架，Windows + Linux）

_Last updated: 2026-09-21_
> 从 ccb-base（多 Agent/CCB，仅 Linux）派生的**单机版**：用 Claude Code 原生 in-session subagent（implementer / code-reviewer / tester / deployer），不依赖 CCB daemon/tmux/派单。跨平台（Windows 经 Git Bash 跑 hooks）。

## Pinned（必守）
- **重构纲领「四个避免过度」（用户 2026-09-15 原话：避免过度设计、过度测试、过度检视、过度可信）**：新增任何闸 / 测试 / 审查层 / 记忆层之前先过这四条；一条规则要么点名机器闸、要么明标「靠自觉」，不许两头都不占。适用范围：本仓全部后续改动。
- **测试比例分母只算核心**（2026-09-15，取代 2026-09-10 那条的分母口径）：有效代码 = hooks / harness 核心（harness.mjs + lib + audit）/ scripts / githooks / 安装器；`.claude/harness/ext/` 与它的测试（cases/test-harness.sh、test-evidence-defects.sh、test-release-binding.sh）另成一对、各自算；核心测试占核心有效代码三分之一到二分之一，区间不变。依据：拆包后 ext 15.5K 行默认不装，再算进分母就是拿关闭的代码撑比例。 清点命令写死（口径不再重吵）：分子 `cat $(ls .claude/tests/*.sh .claude/tests/*.ps1 .claude/tests/cases/*.sh | grep -vE "test-harness.sh|test-evidence-defects.sh|test-release-binding.sh") | wc -l`；分母 `cat .claude/hooks/*.mjs .claude/hooks/lib/*.mjs .claude/harness/harness.mjs .claude/harness/lib/*.mjs .claude/harness/audit/*.mjs .claude/scripts/*.mjs .claude/scripts/*.sh .claude/scripts/*.ps1 .claude/githooks/* setup.sh setup.ps1 | wc -l`（2026-09-15 实测 5360 / 10726 = 0.50，压在上限）。
- 测试代码占有效代码的三分之一到二分之一之间（按行数；hooks / harness / scripts / githooks / 安装器算有效代码，tests/ 与 selftest lane 算测试），低了补、高了删，预算按风险分配而不是全量覆盖（依据：用户 2026-09-10 原话「测试代码要严格降低」「本末倒置」「基于风险的测试，不是全量测试」「不能低于 1/3，不能高于 1/2」；适用范围：本仓全部，今天新增的 predev-lint / ui-audit 测试与 spec.mjs lane 先砍，全仓按下方 Decisions 的预算表砍） → 被 2026-09-15 取代（分母口径变了，区间不变）
- 仓里剩余的 `.ps1`（`setup.ps1` / `fix-platform.ps1` / `fast-mode.ps1` / `install-githooks.ps1` / 两个 `.ps1` 测试）**纯 ASCII**：Windows PowerShell 5.1 按 GBK 读无 BOM UTF-8，含中文即解析崩；理由已改写为「兼容性面会变」（2026-09-05 用户拍板）。hook 自 2026-09-06 起全部是 `.mjs`，不再有 `.ps1` hook。
- hook 命令走 exec form：`"command": "node"` + `"args": ["${CLAUDE_PROJECT_DIR}/.claude/hooks/<name>.mjs"]`，不经 shell，占位符只认花括号，三个平台逐字相同（官方 hooks 文档 2026-09-05 核证）。只有 statusLine 还是 shell form，纯 PowerShell 环境由 `setup.ps1` 把它改写成 `$env:` 形式——旧的「`\$env:` 转义」只剩这一处。
- 权限「永不询问」：settings.json `permissions.defaultMode: bypassPermissions`（只跳工具权限提示，不影响 agent 决策提问 + 框架 guard hook）。
- **单模型审查承重墙**：每个判断必须锚定可执行外部证据（测试运行器/编译/grep/Spec 比对）；对抗式立场/多视角 lens 只是廉价补充，不是承重墙。依据：ICLR 2024「LLMs Cannot Self-Correct Reasoning Yet」（纯提示词自我修正会反噬）+ 「Stop Overvaluing Multi-Agent Debate」（同模型 debate 等算力打不过简单投票）。
- **压缩边界两端都有闸，回注不靠自觉**：`PostCompact`（压缩完成后触发，输入带 `compact_trigger` / `compaction_ratio` / `messages_before|after`）支持顶层 `additionalContext`，但**无 decision control**——拦不住也不该拦，压缩都发生完了。`postcompact-reinject.sh|.ps1` 挂在这个边界后自动跑 `harness invariants`，把「不可交易集 + 活跃状态」从文件重新派生再注回；压缩前那半仍归 precompact-gate 拦一次让人先固化。**本条推翻旧结论**（原写「PreCompact 只能 decision:block、不支持 additionalContext，压缩后不触发新 SessionStart，此洞无轻量解法」）——官方文档现把 `additionalContext` 列为 `UserPromptSubmit` / `SessionStart` / `PreCompact` / `PostCompact` / `Stop` / `SubagentStop` 六事件共有，PostCompact 更是直接把「边界后回注」给成了原生落点。别再凭那条旧结论判定压缩洞无解。（2026-09-02 按 code.claude.com/docs/en/hooks 逐节核证）
- **打/补 git tag 前必须先查远程**：`git ls-remote --tags origin` 查远程在先，不能只查本地 `git tag`——本地无 tag ≠ 远程无 tag，只查本地会误判并打出与远程冲突的 tag。（2026-06-14 踩坑：v1.0.1 本地误判"首个 tag"，实际远程早有 v1.0.1 → 59f207c）
- **.ps1 hook 在 Windows 跑的是 powershell.exe（Windows PowerShell 5.1），不是 pwsh 7.x**（2026-09-06 起 hook 全 `.mjs`，本条只对剩余的 `.ps1` 安装器与脚本适用）（setup.ps1 注释明写 powershell.exe）。5.1 下 native 命令（git/npx 等）写 stderr 会生成 ErrorRecord 进 PS Error 流，`$ErrorActionPreference='Stop'` 把它提升为 terminating error，`*>$null` 拦不住 → 脚本崩、`$LASTEXITCODE` 守卫被绕过 → 错误泄漏到 UI。凡 .ps1 里调可能写 stderr 的 native 命令：用 `--quiet`/`2>$null` 让命令本身不写 stderr，或 `try/catch` 包，或局部 `$ErrorActionPreference='Continue'`。`$PSNativeCommandUseErrorActionPreference` 是 PS 7.3+ 变量，5.1 无效，别用它修。（2026-06-14 真机 trace 验证）
- **验收五步闸（禁跳步）**：做任何「完成」声称前必走：①想清要跑的命令 → ②跑全量全新（无缓存）→ ③读完整输出 + exit code → ④确认输出支持结论 → ⑤才开口。禁用"应该/大概/看起来"措辞替代实测。（2026-06-15 纪律增强；引用本框架两次翻车案例：PS 5.1 git fatal 误判、v1.0.1 远程 tag 误判）
- **接收审查/反馈禁表演式认同**：禁"你说得对/好建议/这就改"开场。改为：复述确认（"你说的是X，对吗？"）、或先问清再表态、或有异议顶回去、或直接动手不废话。（2026-06-15 纪律增强）
- **.ps1 hook 补中文触发词须用 \uXXXX Unicode 转义，不能直接写中文**（2026-09-06 起 hook 全 `.mjs`，`.mjs` 里可直接写中文；本条只对剩余 `.ps1` 脚本适用）：Pinned 既有「.ps1 纯 ASCII（PS 5.1 GBK 读中文崩）」的延伸。tdd-gate.ps1 第 21 行漏中文「编码实现」触发词非疏忽，是 ASCII 约束下的历史取舍；修复用 编码实现 之类 Unicode 转义匹配（PS -match 走 .NET 正则支持 \u），保持纯 ASCII。Explore 探查漏点破此层。（2026-07-29）
- **v2 / v3 改造期每个 Phase 的收口闸（`feat/v2-engine` / `feat/v3-tiered` 有效）**：selftest 全绿 + `harness-golden.mjs --check` 零差异 + `run-all.sh` 全绿 + 该 Phase 新增测试，四项齐才算完。现有 15 个子命令的 stdout JSON 与退出码是**对外契约**（stop-gate / pre-commit-check / harness-async-verify 三个 hook 在消费），零行为回归。每条新闸必须**同批**产出一条反向验证（注入缺陷 → 闸真响）——只跑正向全绿的闸等于没有闸。
- **.ps1 hook 读 stdin 须先设 UTF-8 InputEncoding**（2026-09-06 起 hook 全 `.mjs`，node 读 stdin 无此问题；本条只对剩余 `.ps1` 脚本适用）：中文 Windows pwsh 默认 GB2312/936，读 UTF-8 JSON 会乱码（2026-07-29 真机 codepage 936）。#15 已统一 10 个读 stdin 的 .ps1 hook（含 tdd-gate）加 `[Console]::InputEncoding=UTF8`；新 .ps1 hook 照抄，勿漏。

## Done
- 2026-09-22: **#78 / #79 修复：嵌套引号扫描限解释器类动词 + 命令载体（含 shell -c / eval / parallel）引号参数递归重扫 + 管道级转发判据；git 元数据子命令二级放行（log 带内容选项不放）、grep 模式位不判、冒号后段判**——evidence 与经过见各 TODO 已完成段；hook 339 → 756 行、floor 69 → 120 条，三轮红锁三次复核收口，run-all rc 0。残留 #83。
- 2026-09-22: **#74 修复：可选包安装走主循环同一套保护（skip/update 报告、ps1 留副本、排除表、rules 只顶层）**——evidence 见 TODO #74 已完成段；新增 test-setup-optional.sh 23 条进 run-all；残留 #81 / #82。
- 2026-09-22: **#69 修复：deployer 动手前先读 release-builder/SKILL.md（探针证实预加载进不来）；#80：glob 候选去掉 `credentials.json`**——evidence 见各 TODO 已完成段。
- 2026-09-22: **#76 修复：读密钥闸判密钥路径不判读取器名（含命令替换动词位、glob 线性匹配）**——evidence 与经过见 TODO #76 已完成段；hook 87 → 339 行、test-hooks-floor 13 → 69 条，三轮红锁两轮复核收口，run-all rc 0。
- 2026-09-22: **#77 修复：静态自测失败汇总点名到套件**——evidence 与经过见 TODO #77 已完成段；只改 `run-all.sh` 一个文件（+27/−16），全量回归 rc 0，注入失败的隔离验证看到点名。
- 2026-09-21: **PR #2 合入（`d735258`）：U4 假红与 README 安全文档过度承诺各修一处**——① `test-ui-audit.sh` 的 U4（缺引擎必须 rc 3）在装了 playwright-core 的机器上恒红、属假红：`.claude/scripts/ui-audit.mjs:52` 的回退解析是 `createRequire(join(process.cwd(), 'noop.js'))`，认 cwd 不认文件位置；U4 只把脚本拷到仓外、没切 cwd，回退仍从仓库根解析到引擎。修法是一行 subshell 切 cwd（同文件第 24、109 行本来就是这个手法，U4 漏了），并把原先预判错原因的注释改对，实现本身无错。② README 第 89 行 ask 列表里写着 `Bash(git push*)`，而 `.claude/settings.json` 的 ask 里没有这条、`.claude/CLAUDE.md:32` 也写明 git push 不在必停之列（用户 2026-09-06 指令）——实现是有意的，是 README 没跟上，已改成如实描述。**为什么此前没人见过这个红**：本机与 CI 都没装 playwright-core，U4 走的是「本来就没引擎」那条路天然通过；只在装了引擎的环境里才执行的那条测试分支，等于从没被测过——本机这次重跑同样没覆盖到那条分支，修复的那条路径只有 PR 作者的沙箱验过。**影响面**：run-all 是 fail-fast 的，U4 一红第 2 段即停，后面 medium / low 套件在默认路径上全不执行。分支 `fix/ui-audit-u4-cwd-isolation`，作者为仓库主人凭证下的云端沙箱 agent（Node v20.20.1，装有 playwright-core），主 Agent 审查后用户 2026-09-21 批准合并。evidence：commit `15c5f0a`（+7 −5，两个文件）、合并提交 `d735258`；本机重跑 `bash .claude/tests/test-ui-audit.sh` PASS=5 FAIL=0 SKIPPED=0；合并前 PR 五个 CI 检查（ubuntu/windows × node22/24 + ps1）全 SUCCESS。
- 2026-09-20: **#75 落地：progress 归档交给脚本 `progress-archive.mjs`，保留线压低，已关闭的 TODO 纳入归档**——evidence 与经过见 TODO #75 已完成段；黑盒测试 27/0（400 行），三个变异两杀一等价。当天用它对本仓真文件跑了第一次，结果见当前断点。核心测试比例 6090 / 11087 = 0.549，超 Pinned 上限 0.5 约 5 个点，按 2026-09-15 的口径由用户看范围拍板。
- 2026-09-20: **#73 修复：指令审计白名单在 CRLF 签出下不再失效，#70 在 Windows 上补齐**——evidence 与经过见 TODO #73 已完成段；红锁 17/0、run-all 默认档 rc 0、doctor 通过、manifest 129 条一致。
- 2026-09-19: **需求与设计侧对照 mattpocock/skills 第二轮：`grilling` 七个点全部对完账，另从它旁边的 `domain-modeling`、`to-questionnaire` 取了两条落地，只动正文、流程未变**——① 甲，`arch-designer`：ADR 入口由「关键决策 ≥3 条」改为三条都占才记（撤回代价高 / 不交代背景后人会纳闷 / 当时真比过候选），缺一条的在模板 §8 新增的表里留一行结论加一句理由；风格 / 存储 / 通信三样仍必须有结论；revisit-if / reversal / 执法方式与 `adr-check` 校验一字未动。改了 SKILL.md 四处（决策留痕、设计维度清单、验收清单、反例加一条）与模板 §8。影响面先核过：配额只活在 SKILL.md 两行里，`adr-check` 不数条数，`predev-lint` 只校验写出来的 ADR 字段、零条不判错。② 乙，`product-spec-builder`：新增 `references/third-party-questionnaire.md`（能答的人不在场时替用户给那个人出问卷：只问用户发给谁、要拿回什么；一题一事、最要紧的放前、问过去那一次、写明「不知道」也有用、内部记号不出现；落项目根、答复回来走迭代模式），SKILL.md「向谁问」与 [收敛条件] 各一句指针、文件树一行。`docs/guide/04` 同步两处。查过后划掉的两条怀疑：否定句密度（每百行 6–11 处，逐句看基本都配了该怎么做）、步骤缺完成判据（`workflow-0-1.md` 每步有出口判据）。未落：丙（深挖菜单加「可点样机」，对方 `prototype/LOGIC.md`）——主 Agent 建议先在真项目上手工试一次，好用再写；`to-spec` 的测试切入点归开发侧那一轮；`writing-for-agents` 值得单开一轮对 `skill-builder`，会碰 CLAUDE.md 触发词，属用户划出去的流程层，未动。evidence：skills-lint 18 个 findings 0、scan-instructions rc 0、test-routing 一致、test-predev-lint 10/0、test-skills-lint-wording 2/0、manifest 128 条一致、doctor 通过。
- 2026-09-19: **TODO #70 修复并验完——白名单补第三条 entry，`--with-harness` 装出的下游项目不再被指令审计误判 error**：`.claude/rules/harness-large-repo.md:63` 那条 sha256 / context 与 `harness/ext/rules/harness-large-repo.md` 的既有条目逐字复用（两份文件整文件 sha256 同为 `898cfe61…`）。红锁由独立 tester 写进 `test-distribution.sh` 的 D-7h（+52 行，三条断言），主 Agent 亲见红 `PASS=25 FAIL=2 exit 1`，修后亲跑 `PASS=27 FAIL=0 exit 0`。验收五步闸全过：JSON 合法 3 条 / 本仓 `scan-instructions` 仍 rc 0 errors 0（只多一行预期内的 `allowlist-entry-orphan` note）/ `gen-manifest --check` rc 0 128 条 / diff 范围干净。
- 2026-09-19: **#70 的证据等级本轮升级，两条此前的「未实证」保留已消除**：原写「setup.ps1 侧结论全为读码、本机无 PowerShell」与「后果一段是读码取证、未端到端实跑」——本机实有 pwsh 7.4.10，`setup.ps1 -WithHarness` 真跑 rc 0、行为与 bash 侧逐项一致；沙箱 commit 后跑全量 tracked 扫描 rc 1、唯一 error 就是那条白名单缺口，下游开 githooks 每次提交被拦已坐实（不再是推断）。
- 2026-09-19: **#70 反向验证（主 Agent 作为验收方补做，原本无人被派）**：沙箱里改第 63 行本身 → 豁免失效 error=1；只改第 62 行邻行、63 行字节不动 → 豁免同样失效 error=1；还原 → 恢复豁免。证明新条目**绑内容不绑路径**，没开静默口子。
- 2026-09-19: **上个 session 的 code-reviewer 误写路径已归位，manifest 128→130→128**：战术记忆被多拼一层前缀写进 `.claude/.claude/agent-memory/code-reviewer/`（mtime 2026-09-18 07:02），本轮 `gen-manifest` 因此把两个文件扫进官方清单（128→130 条）。已归位：`review-hotspots-installers.md` 搬进 `docs/agent-notes/code-reviewer/`（该角色记忆已 49304 字节、封顶 50KB，按 memory-systems.md 走溢出位），`MEMORY.md` 加一行相对路径指针（照该文件既有先例），`.claude/.claude/` 删除，清单重生成回 128 条、零 agent-memory 条目。
- 2026-09-18: **需求与设计三个 skill 吸收 mattpocock/skills `grilling` 的三条，只动正文、流程未变，2026-09-19 随 `037908e` 入库**——① 提问先后依赖：`interview-principles.md` 铁律第三条由「一次只问一个问题」改为「先问上游，能并才并」（开放题仍一次一个；互不依赖的选择题同轮编号问完），同步 `product-spec-builder` 覆盖扫描、两份 workflow、`design-brief-builder` 四对二选一轴、`arch-designer` 风格先于存储与通信；没照搬对方的整轮齐问，对方文档自认有争议。② 事实自己查、决定才问人：`product-spec-builder` 底线「问题有成本」与迭代第三步、`workflow-iteration.md` 追问规则补读代码查现状、用户说的与代码对不上并排摆出；`arch-designer` 本有「棕地先读代码」未动。③ 反失败自检加「一路点头」：连着三问无反对或补充即停，挑代价最高的一条改开放题让用户用自己的案例讲，讲不出降 `[推断]`——「三问」是主 Agent 定的数，对方没给。顺手把 SKILL.md 文件树注释里早已过期的「七种失效」（实为八条）改成不带数的「反失败自检」；`docs/guide/04` 同步三句。evidence：`skills-lint` rc 0 findings 0、`scan-instructions` rc 0 error 0、`test-routing` 与 `test-skills-lint-wording` 通过；`doctor.sh` 的 7 条清单不符正是本次改的 7 个文件，按 pre-commit 头注释的口径清单发版时重生、不逐次刷新，HEAD 上为 0 条。未改：`domain-rulings/SKILL.md:32` 的「一次只问一个」是它自己的分诊对话，不在需求与设计范围内。
- 2026-09-15: **使用指南 docs/guide/ 十五章 + 索引落地，共约 4360 行**——入门（总览 / 安装 / 第一个项目）主 Agent 直接写；进阶四章（需求与设计 / 开发精讲 / 审查测试修复 / 发布）、精通八章（记忆 / 闸门与档位 / Sub-Agent 派发 / 大仓包 / 自测与 CI / 定制扩展 / 故障排查 / 术语表）派三个 general-purpose 并行写，每章带「解决什么 / 读完能做什么」开头与「常见坑」结尾，命令与输出都是真跑出来的。主 Agent 验收：相对链接与锚点零断链、无四级标题、术语全书统一、写手「未核实」清单逐条抽查 14 项（task 子命令七字段 / predev-lint 占位判据 / release fast 判 FAIL / feedback 文件名 / catalog budget 字段 / 未装 ext 时 spec-lint rc 3 等）全部对得上，修了两处（ch05 误写 hook 由 setup.ps1 装成 PowerShell 形态、ch14 引了维护者私有 memory 路径）。README 顶部加指针。指南随仓库走，不随 setup.sh 装进目标项目。
- 2026-09-15: **重构提交 `9ee1692` 推送后 CI 四格红在「审计 instructions」，修复提交 `6602147` 五格全绿**——根因：harness-large-repo.md 启用条件段改写后，被豁免的 gate-disable-instruction 那行从 62 挪到 63，白名单按行号绑定即失效；本地 pre-commit 只跑密钥与语法，没人跑它。修：条目重签到 63 行；`.claude/githooks/pre-commit` 补 `scan-instructions --staged`（test-githooks 6/0）。证据：`gh run view 34945476225` 五个 job 全 success。
- 2026-09-15: **五项重构整批落地（未提交前状态见 Decisions 2026-09-15 各条）**——① 闸：tdd-gate 挂 PreToolUse(Agent)、两小时标记时效、advise 记账；no-direct-code-guard 放行 agent_id；stop-gate standard 降 advise；三张档位表对齐并加 TD-1 相等断言；postcompact-reinject 没装引擎时自派生回注（跳过已取代 Pinned）；catalog 在、ext 缺时三道大仓闸当场提示未验、doctor 判 bad。② 流程：Task 三档表进 dev-workflow-details / subagent-dispatch / dev-builder / code-review，implementer 与 tester 默认 sonnet。③ 记忆：Decisions 归档 126 条、正文 30 条；旧断点日志 72 行归档；Pinned 15 条；六份 agent memory 搬 docs/agent-notes；progress.md 286KB → 约 177KB。④ CLAUDE.md 189 行 35KB → 126 行 19KB（估算 7.7K → 4.1K token），十条铁律各标闸名或「靠自觉」，删光 feedback 交叉引用。⑤ harness：14 个引擎模块 + 两份细则搬 `harness/ext/`，harness.mjs 惰性 import、缺包 rc 3，`setup.sh --with-harness` / `setup.ps1 -WithHarness`，排除表四份同步，engineFiles 同时枚举 lib 与 ext。测试：六文件退休约 30 条用例（三批被打回并恢复：fix-platform ② 臂、U7、密钥扫描器三条），核心测试 5673 → 5463 行。证据：`run-all --level all` rc 0（92s，0 FAIL）、selftest 141、doctor 通过、manifest 127 条一致、test-installer-parity 17/0、CLAUDE.md 路由测试 8 agents / 18 skills 双向一致、scan-instructions 无孤儿。审查：code-reviewer HIGH 档一轮，H-1 / H-2 / M-1 / M-2 / M-3 / L-1 / L-2 / L-3 全部处置（见 Decisions「整批 code-review 的裁决」）。
- 2026-09-12: feedback 待处理从 **12 条压到 6 条**：5 条（副产品配闸 / 三类依据 / 采集口与入库口 / 分类跟领域走 / git diff 对新建文件失效）正文早已落进 rules 与 CLAUDE.md，补标 graduated 并在 FEEDBACK-INDEX.md 加 ✅ 前缀；第 6 条 `subagent-dispatch-minimal-context-not-self-derivation.md` 写「派单 ≤ 8 次工具调用」，与用户 2026-09-06 原话「6轮我都嫌多」及落地规则（CLAUDE.md 与 rules/subagent-dispatch.md 均为 ≤ 6）不符，已把正文两处与索引描述改正为 ≤6 并标毕业。验证：`CLAUDE_PROJECT_DIR=$PWD node .claude/hooks/check-evolution.mjs` 输出「6 条待处理（共 43 条）」。
- 2026-09-12: 框架实践项体检（只读，数据为准）：43 条 feedback（已毕业 31 / 待处理 12 → 6）；gate-audit 八个闸有战绩（stop-gate 38 / three-file-sync 29 / tdd-gate 19 / secret-exfil 16 / dangerous-pkill 8 / tier 8 / precompact 2 / release-gate 1）、三个零记录；`rules-audit` 446 条规则机器可判 80 条（17.9%）、提示词 6 条、幽灵 0、未分类 360 条（80.7%）；测试 6461 行 / 有效代码 21653 行 = **0.298**，低于 Pinned 的 1/3 下限；`test-age` 报 79 条跑过 ≥20 次从未红过的退休候选。
- 2026-09-12: gate-audit 判据错已红锁——tester 补 3 条断言（去掉「疑似死闸/黑箱」判词 / 标题点明前置条件不在本仓与威慑生效两种无害解释 / 列完零拦停闸后打印本仓能力上下文 catalog·tsconfig·py 三项 + 提示查下游账本），主 Agent 亲验红 `bash .claude/tests/test-gate-audit.sh` → `PASS=12 FAIL=3` rc=1，红因是现实现仍打旧判词；已派 implementer 修绿。另：六条积压 feedback 全部裁定完毕并落地——product-spec-builder [四条底线] 加「先问是地基还是锦上添花」（含重计算签字前成本预估）、dev-planner 排期「一次一个功能串行收口」、dev-builder [质量门槛] 加「长跑批处理带超时兜底 + 输入侧预检」与四步走后「大批量改造收口另扫重复代码」、release-builder 发布清单加确认项、CLAUDE.md 两处（派静默 subagent 那条扩「确认挂死立即止损禁观望」、闸靠数据留那条扩「验证验证者类机制不进发版链」）；六条 frontmatter 与 FEEDBACK-INDEX 同步，闸重数 **43 条里待处理 1 条**；skills-lint 18 个零 findings、doctor 143 条 SHA 一致。
- 2026-09-11: 采集闸两处修正入库 `17ce7f1` 并推上远端：文案改第二人称、判据改开头词判；CLAUDE.md 两处 + ARCHITECTURE.md 一处口径改准。验证：亲验红 `test-hooks-node PASS=25 FAIL=6`（六条红因：两条措辞 + 四条括注误判）→ 修绿 `PASS=31 FAIL=0`；`run-all --level all` rc=0（账本 30 个文件零非 PASS，真触发 case 按 opt-in 未跑）；doctor 143 条 SHA 全一致。
- 2026-09-11: 该 hook 用例 25 → 31 条：None 六臂（裸 / 中文括注 / 英文括注 / 裸「无」/「无」括注 / N/A 括注）、真发现两臂（普通 / 以「无」起头）、无此栏一条。每条新增臂都被一个被否候选单独打死（`\b` 版被裸「无」与「无」括注打死，纯前缀版被「无线…」打死）——这是「闸靠数据留」的实证，不是凭感觉加。
- 2026-09-11: progress.md 过 100 条阈值归档收口：搬 51 条进 progress.archive.md（Done 21 + Notes 30），正文两段各余 50 条，九个区块齐全。
- 2026-09-11: 清账收尾——`gen-manifest` 重生成（142 文件）、`doctor.sh` rc=0、`run-all --level all` rc=0（账本 30 个测试文件 254 条断言零非 PASS，两个真触发 case 按 opt-in 未跑属预期）；提交 `a38f8e2` 推送 origin/main；CI 五格全绿（run 34551292426：ubuntu/windows × node 22/24 + windows ps1）
- 2026-09-11: 「领域口径库」纠正批（见下方 Decisions 同日七条）落进四份家底文件：①③④⑤⑥ 落进 `.claude/rules/domain-rulings.md`（顺带把文中 feedback 计数 39 改正为 38，实测 `ls` 排除 INDEX 得 38）；⑥ 落进 `.claude/rules/subagent-dispatch.md` 回执信封 Domain findings 栏；⑤⑥ 落进 `.claude/agents/domain-recorder.md`；⑦ 落进 `.claude/skills/red-blue-review/SKILL.md` 的 Judge 裁定步
- 2026-09-11: 新建 `/domain-rulings` skill（`.claude/skills/domain-rulings/SKILL.md`）：定位为沟通过程的沉淀工具，不进四步走、不设卡点、不在验收链路上；核心职责是四象限分诊（问「这条当初是谁知道的」：人知 AI 不知 → Spec 规则与例外；AI 知人不知 → 技术方向/决策依据；双方都知 → 功能需求；双方都不知、需共同查证才得出 → 口径库，唯一入口），外加查现行值/列待复核/看被谁依赖/手工收录四件；已注册进 CLAUDE.md（新增 [领域口径库] 指针段 + Skill 清单一行）、`rules/dev-workflow-details.md` 执行方式、`rules/file-structure.md` 两处树；skills-lint 18 个零 findings，description 69 字
- 2026-09-10: 升级下游脚手架 /home/z00632348/code/digifiber-conflation-claude（跨代：.sh/.ps1 双份 hook → 22 个 .mjs 单运行时）。先备份（22MB/1750 文件，验过可解压）→ 摸清只有 3 个框架文件被本地改过 → 采六条闸的升级前基线 → 空跑计划 → 真装（新建 154 / 更新 52 / 冲突 1）→ 换引擎 → 同步清单。结果：六条闸与基线逐条一致，他们接线的 `fitness --rules-file` 子集闸从 rc 1 变 rc 0（旧引擎 3040 行单文件里的测试夹具字符串被自家密钥规则扫到，拆 lib 后消失）；旧 .sh/.ps1 由安装器自调 fix-platform 清掉 35 个；清单 140/140 一致；他们的 module-catalog / fitness-secpriv-rules / skill-description-lint / 5 条自有 rules / 7 个自有 agent 全部保留
- 2026-09-10: 前期提升第一批五项落地（docs/predev-upgrade-plan-2026-09.md §7 第一批）：① 界面通病数字化 + `scripts/ui-slop-scan.mjs` 纯静态扫描 391 行（零依赖、不需浏览器引擎，补上本机缺 odc / playwright 导致的断链）；② ADR 补 `revisit-if:` 与 `reversal:` 两必填字段，`adr-check` 机器校验、单向门单列清单；③ dfx 延迟与重试预算表 + `predev-lint` 算术校验（各层之和 ≤ 端到端、重试层数、单位换算）；④ `plan-lint` 需求↔计划双向覆盖（接现成 REQ id 约定）+ 缩进写坏的静默漏查警告 + 模板补可抄示范；⑤ design-maker 无 odc 出提示词、product-spec-builder 收尾三行判定。测试：两个独立 tester 补 274 行分级用例（predev-lint 18 / plan-lint 13 / ui-slop-scan 6，plan-lint 此前零覆盖也不在回归里），`selftest` 135 → 141，两人各做变异验证证明断言会咬。主 Agent 亲跑全量 `--level all` rc 0（1 分 26 秒）。顺带清掉 `.github/workflows/gate.yml` 里指向已删 golden 步骤的悬空引用
- 2026-09-10: [#65] 开发阶段（计划 / 编码 / 修复 / 审查 / 测试 / 发布）吃透与改造：研究报告 docs/dev-study-2026-09.md（4.0 → 5.0 刀法、5.0 十条事故反模式、Superpowers / BMAD / Spec Kit / Anthropic 官方一手引用、删 / 留 / 吸收 / 不采纳四表）；六个 skill + 两模板、四个 agents、两份 rules、CLAUDE.md 两段重写，2667 → 1126 行，底线八条逐条指到原句；行为验收：售后派单范例 Spec 走新 dev-planner 出 DEV-PLAN 过 plan-lint（evidence：commit bc1e9d4，已推送 origin/main）
- 2026-09-10: 全仓测试减重 + 分级 + 老化（TODO #64，11:33 三实现者并行派出，12:10 收口）：有效代码 15400 行、测试 6596 行，比例 0.43（改前 1.06，区间 0.33～0.50）；selftest 308 → 135 条 lane（4890 → 1895 行）、test-harness 1442 → 490、审计三件套 1308 → 608、发布两支 1301 → 481、evidence 695 → 223、hooks 三件 2726 → 490 + 地板闸独立文件 test-hooks-floor.sh 213 行、档位 1243 → 300、安装器 1468 → 541、githooks 571 → 148、小套 1392 → 500；run-all 加 `--level high|medium|all`（默认 high，CI all），账本 `.claude/evidence/test-ledger.jsonl`，`scripts/test-age.mjs` 出退休候选；33 套全部打级（high 10 / medium 6 / low 14，另 3 个真触发 case 与 test-routing 打 low）；主 Agent 顺手修掉 test-age.mjs 里一个字面 NUL 分隔符（让 grep / file 把它当二进制）改为 `\u0000` 转义（evidence：三实现者回执、/tmp/run-high.log、/tmp/run-all-5.log）
- 2026-09-10: 验证体系减重第一批（10:50 派出，11:31 提交推送 f6f51f2，run-all 4 分 51 秒 rc 0，新钩子下推送 18 秒）：pre-push 重写 62 行（纯文档推送 8 毫秒放行，代码推送只跑 selftest + 密钥扫描约 21 秒）；pre-commit 只留密钥与语法；golden 基线整套删除（harness-golden.mjs + 9 份基线 + run-all / gate.yml / README 引用）；run-all 两个真触发 case 改 opt-in `CCBASE_RUN_CLAUDE_CASES=1`，全量 5 分 47 秒；tdd-gate 与 three-file-sync-gate 三档 advise；doctor / test-setup 的 manifest 比对改提示；test-predev-lint 842 → 222、test-ui-audit 553 → 262（合计 484，区间 325～487 内）；selftest 今天新增 lane 16 → 6（总 298）；接受实现者四条取舍：iso_run 与 U19b 不补、TR-20 改为 rc 0 + stderr 非空、pre-push 62 行不再压、历史文档不改（evidence：/tmp/runall2.log、test-githooks 50/0）
- 2026-09-10: feat/predev-upgrade 快进合并到 main 并推送（origin/main = b4b2fb5，pre-push 全量回归放行；分支保留，可由用户删除）
- 2026-09-10: [#56][#57][#58][#59][#60][#61][#62] 前期阶段改造收口：四 skill 重建 / 增量 + 五份范例 + 两支机器闸 + spec.mjs 规则 + 接线；十一批红锁、十轮复审（第十轮 PASS）；全量 run-all rc 0、golden lane 308 一致、manifest 138 文件；分支 feat/predev-upgrade 提交推送待用户合并（evidence：/tmp/run-all.log、docs/predev-study-2026-09.md §8）
- 2026-09-09: [#56][#57][#58] 三份机器闸首版转绿（主 Agent 亲跑：selftest ok 295 条 0 红 / test-predev-lint 25/25 / test-ui-audit 5/5 + U5 跳过；golden 差异仅 selftest 计数 292→295，待重录；evidence：/tmp/verify-selftest.json、/tmp/verify-predev.log、/tmp/verify-uiaudit.log）；第二批红锁（连字符记号 / 复合值引用 / 度量列表头定位 / 引擎失败原因 / 对比度判红）与对抗审查已派出
- 2026-09-09: **开发前阶段「吃透」完成 + 需求与界面两 skill 重建落盘（分支 `feat/predev-upgrade`，未提交）**——逐行读完 `~/code/other/` 全部材料（PM 5.0 两版 5.2k 行、4.0 4.1k、3.0 1.4k、插件经理三版同源 14k 中前两阶段与脚本、script-breakdown、微信规则）与业界一手源（原文 curl 到 `/tmp/predev-study/`：Anthropic frontend-design 71 行、Vercel AGENTS.md 155 + command.md 190、Spec Kit 四模板 + clarify / checklist / analyze / specify 命令、Google design.md spec 377 + PHILOSOPHY、BMAD v6.8 ux / prd / spec / architecture skill 与 71 条 elicitation 方法库、UI-UX Pro Max 119 条规则与 88 风格清单、MADR、Cursor 的 DESIGN.md），报告 `docs/predev-study-2026-09.md`。产物：product-spec-builder SKILL 重写 + references 六份 + 模板四标记 + 售后工单范例；design-brief-builder 从 27 行残件重建 + references 六份 + 两份模板 + 一对范例。当场发现两处硬伤（design-brief-builder 截断于 `5f7a4c9`；bridge-A 模板与 spec-lint 矛盾）并纳入本轮修复。tester 红锁在写，实现者未派。
- 2026-09-08: **「人机沟通桥梁」改造 A→C→B→D→E 落地（用户 09-08 指令：从产品本质审视脚手架，业务理解与工程执行同样强大）**——先审后改：三个只读 agent 按用户标准逐 file:line 审 17 个 skill / 7 个 agent / 记忆链，结论进当日 Decisions。提交链：`0caf53e`(A 需求侧重写) → `b3cfa14`(C 记忆与纠正闭环) → `75f6d2d`(B 下游三态出口) → `5f7a4c9`(D 去重瘦身)。**E 检验（可观察的前后行为）**：同一句需求「内部报销审批工具，员工提交发票、主管审批、财务打款」分别喂给 v2.0.0 worktree 与 HEAD 的 `claude -p`（各 --max-turns 6，日志 /tmp/e-before.jsonl、/tmp/e-after.jsonl）。v2.0.0：先甩竞品与「泼盆冷水」，一口气 4 问（现状 / 规模 / 打款三选一表 / 发票手填否），第 4 问主动推销 OCR、验真、查重、合规预检四项 AI 功能。HEAD：先摆一条查证事实并说明它决定后面的规则（税务总局不开放查验 API，只能接第三方），明说「涉及钱和发票，做错代价高，多问几轮」（判档可见），只问一件真实发生的事（最近一次从交单到打款怎么走、谁先动手、用什么、钱怎么出）+ 一个追问（上次出岔子哪一单），无推销，带来源。**三次历史跑偏对照新机制**：① 派单 60 轮 / 6 轮（09-06）→ 派单包 Business Context + 回执业务假设栏，fresh 实例不再靠猜；纠正当场落地回显铁律（CLAUDE.md 总体规则）。② 突变矩阵烧 65 分钟（09-06）→ 交互深度分档「不确定 × 代价」+ AI 专家责任「分歧说一次、用户坚持照做」，元测试不进链（feedback）。③ 「不是缩减功能，我反而想加功能」（09-05，AI 把推断当目标）→ 三标记 `[确认]/[推断]/[待定]` + 案例复述替代批准仪式。skills 3286 → 2695 行，方法内容增加（对话示例 / 反例 / 收敛 / 交接）。
- 2026-09-06: **v2.0.0 已发版**——tag `v2.0.0` → `dbbb35a`（远端 `git ls-remote --tags` 实查在），GitHub Release draft=false、createdAt 2026-09-06T15:12:50Z、资产 `cc-base-v2.0.0.zip`（1,051,693 字节，255 项，含 tests/ 供 `--with-tests`；无 progress / docs / research / agent-memory / 私人 feedback），URL https://github.com/zylimit/cc-base/releases/tag/v2.0.0。发版证据：CI 五格绿（`dbbb35a`，仓库转 public 后 Actions 不再吃额度）、`release` 装配 7 PASS + gate-fresh DEGRADED、全量 run-all rc 0（`f2eee5c`，其后三个 commit 只删突变矩阵与改 progress）。本版 = D（hook 单运行时）+ A（三档位）+ B（分发面 / 排除表真相源）+ C（golden 瘦身）+ E（CLAUDE.md 瘦身）+ 提炼三批与 `-uall` 盲区修复 + 突变矩阵删除。
- 2026-09-06: **突变矩阵测试整套删除（用户裁定：「除了这个狗屁矩阵测试，其他的先保留」）**——`harness-golden.mjs` 1652 → 1273 行（`--mutate` 模式、S6b 尺子段、`--mutants/--only/--scenario` 三个值标志、`checkMatrix` 的 scenarioFilter/quiet 旋钮全去；场景命令的 `mutate` 沙箱钩子与 adapters 的 `mutation-stryker` 名字与此无关、保留）；删 `tests/golden/MUTANTS.json`、`test-golden-mutate.sh`，run-all 接线去掉；gate.yml 删每周 mutate 步与其日志 artifact（-44 行，`on: schedule` 留着照环境腐烂），汇总判定 8 → 7 项；rules/harness-large-repo.md 去引用。复核：`--mutate` 报 unknown mode rc 2；golden `--check --strict` 8352 逐字一致、基线未重录；manifest 重生成。跑到 9/13 的那次全部 killed，无盲区遗留。其余自测按用户指示先保留，砍不砍另议。
- 2026-09-06: **重复代码提炼三批落地 + 顺带堵住一个真盲区**——① `harness/audit/lib.mjs`（143 行，只依赖 node 内置）收走 check-syntax / scan-instructions 逐字重复的 CLI 脚手架 11 个函数（`makeCli(name)` 绑脚本名），scan-secrets 只并等价函数；audit 三脚本 1985 → 1870 行（-272 +14），test-audit-scripts / test-audit-defects / test-scan-secrets-userinfo 全过、三扫描器 rc 0。② `hooks/lib/io.mjs` 新增 `porcelainZ(root, extraArgs)`（`git status --porcelain -z` 记录解析，R/C 双记录）与 `classifyChange(p)`（改动归类六态），precompact-gate / three-file-sync-gate / hooks/lib/tier.mjs 三处改调用，两 hook -38/-59 行；precompact-gate 补齐 `.tdd-exempt / .red-verified` 排除（PG-14 红锁，HEAD 版实测 FAIL）。③ `catalog.mjs loadCatalogFlag(flags)` 替掉 20 处惯用句（另两处 `catalogFilePath()` 回退是不同构造，保留）；selftest 292、golden `--check --strict` 8352 逐字一致（主 Agent 亲跑）。**盲区**：tester 写对照组时撞出——两 hook 的 `git status --porcelain -z -- .` 没带 `-uall`，未跟踪的新目录折叠成一条 `src/`，目录名匹配不上扩展名表，整个新模块逃过「改了代码没记 progress」的 C2（tier.mjs 的 dirtyPaths 早就带 `-uall`，注释里写明了同一个坑）。红锁：PG-16 + test-three-file-sync-gate 一条（主 Agent 亲见两红：实得空 stdout 放行）→ 两处调用加 `-uall` → 280 / 7 全绿；PG-17 锁 `-uall` 反噬面（展开后的 node_modules / .claude/evidence 仍被 ignore 表接住）。manifest 117 → 118 条（多 audit/lib.mjs）。**推上去后 CI ubuntu 两格红、本地全量 run-all 同样在 test-githooks 停（PASS=40 FAIL=8）**：夹具 `use_real_audit` 只拷三只审计脚本不拷 `lib.mjs`，脚本起不来 node 退 1、pre-commit 把它读成「有命中」阻断——提炼的第一个 blast radius 就是逐文件拷脚本的夹具；一行补上 lib.mjs，48/0。顺带看到 pre-commit 分不清「审计脚本自己崩（node rc 1）」和「真有命中（rc 1）」，两者都阻断，方向是安全的（fail-closed），不改。**第二次红**：`-uall` 补注释在 `gen-manifest.sh` 之后、提交之前，两只 hook 的清单哈希是旧的，test-setup ⑨ 红——一天里第二次栽在「改了文件没重生成清单」（第一次是 E 收口）。补机器闸：`gen-manifest.sh --check`（只比对不写、漂移列路径退 1、坏参数退 2）挂进 `githooks/pre-commit`（`manifest` 步，lint 契约，只在框架仓本体启用），正反向都验过（漂移 rc 1 点名文件 / 一致 rc 0），test-githooks 48/0。
- 2026-09-06: **Phase C「golden 瘦身」收口——基线 29,236 → 14,524 行、`--check --strict` GOLDEN OK（9 场景 + in-repo，8352 断言）**——`harness-golden.mjs` 记录侧对每条命令的归一化 stdout 折成 `stdoutSha256`（按体积：payload ≤ 400 字符保留原文，超过一律哈希；`--probe` 不折、`stdoutText` 与 `stdout` 合成一个哈希），校验侧不符时打印实际 stdout 归因；record 783 次运行、9 份基线全部重录。目标 ≤ 8000 行未达：每条记录的 id / argv / exitCode / stderr 骨架就占 783 × ~10 行，剩下的是 400 字符以内的小输出（`impact` 36 行最大）——再往下要改序列化格式（argv 单行），不值。顺手：E 提交（`05970e3`）漏跑 `gen-manifest.sh`，`FRAMEWORK-MANIFEST.txt` 里 CLAUDE.md / 三份 rules 的哈希是旧的（`release` 装配 manifest 项会 FAIL），本次一并重生成（121 条）。`--mutate` 留到整批最后与全量 run-all 一起跑。
- 2026-09-06: **Phase A「档位」收口——三档 fast / standard / strict 落地，CI 五格全绿（`89727eb`）**——提交链（均在 main）：`e9b798e`(A-T 红锁 71+21) → `becd0e9`(A-1a hook 侧：profile.json + hooks/lib/tier.mjs 单一解析器 + 22 hook 三态) → `917893a`(测试迁到 tier.json) → `25ff8dc`(A-1b 引擎侧：tier 子命令、三处 fast-mode 解析收编、薄壳、statusline、release/invariants/dod 接线、golden 重录) → `d438d88`(文档改档位口径) → `de2c553`(ps1 F 组) → `f590787`(合 main) → `9307a8f`(沙箱夹具跟上 tier 依赖 + A-R 红锁 21) → `89727eb`(A-R 修复：selftest 顶层读盘改惰性、validate 拒地板 override / settings 不可读 rc 3、读侧 8h 夹、banner 认 off)。收口证据：`test-tier` 71/0、`test-tier-hardening` 46/0、`test-hooks-node` 277/0、`test-hooks-settings` 26/0、`test-fast-mode` 16/0、`test-ps1-behavior` 10/0、selftest 292、golden 20513 零差异且退出码零变化、`--mutate` 13/13；reviewer A-R（P1 1 / P2 3 / P3 6 全修，突变 9/10 → 补红锁后 10/10）+ P1-1 单点复核 PASS；CI `6f67a18` 与 `89727eb` 五格全绿。**现在可用**：`tier status|set|explain|validate`、`fast-mode.sh on 4` 薄壳、家底改动自动 strict、fast 8h 硬上限读写两侧都夹、地板五闸任何档不动。**未验证面**：Windows 真机上 `fast-mode.ps1` 薄壳只有 CI 的 ps1 格 Parser + F 组（本机 pwsh 已跑）。事故与教训：sub-agent 撞 60/100 轮上限四次，用户纠正后定「每单 ≤ 6 次工具调用、≤ 5 行主 Agent 直接改」（Decisions 09-06）；A-R 修复四处约 40 行由主 Agent 直接改、零 sub-agent、一次过。
（2026-08-16 及更早的 Done 条目（v1.0.x~v1.10.0），以及 2026-09-01～2026-09-03 期间的 21 条 Done 条目，2026-09-19 再搬 2026-09-03 当日的 14 条、同日晚再搬 2026-09-04 的 1 条，已归档到 progress.archive.md；正文保留最近 50 条）
（归档指针：累计搬出 17 条，最近一次 2026-09-20 搬 17 条；原文见 progress.archive.md「Archived Done」）

## Decisions
- 2026-09-22: **地板闸的判据从「枚举危险动词」翻成「判危险对象 + 封闭放行表」**——依据：#76 实测 `tac .env` / `paste .env` / `jq -R . .env` 全绕过旧 R1/R2，读取器表列不完、只碰元数据的放行表列得完；同类「规则钉在精确形态上」缺陷本仓第六次（#70 / #73 / #75）。适用范围：secret-exfil-guard 的 R1/R2（R3/R3b 整命令级规则保留）；今后其它闸新增判据时同样先问「危险面能不能枚举完」，能枚举完的才用枚举。取代：2026-09-21 TODO #76 里「建议修法方向」一句（已落地），旧 R1/R2 命令名枚举表删除。边界（写进 hook 头注释）：只对命令行里字面出现的密钥路径负责，代码里拼路径、变量间接、纯通配 `*` 不判。
- 2026-09-22: **用户授权主 Agent 自行逐步修本仓挂账缺陷，顺序 #76 → #69（先做实机探针）→ #74 → #77，#77 最小先行；#71 / #66 不动**——依据：用户 2026-09-22 原话「搞ccb-base 算个热身了，你也可以自己逐步修修cc-base」，此前同日对主 Agent 给出的顺序答「按你建议的顺序走」。适用范围：progress.md TODO 里已挂账的缺陷，以及 docs/CROSS-POLLINATION.md「ccb-base 实战回流」里判为「吸收（待排期）」的条目；每条按本仓定档规则走（#76 属安全类地板闸，走红锁 + HIGH 档全流程），主 Agent 亲跑全量回归后才提交推送；不含发版。取代：部分取代 2026-09-20「#74 / #71 / #69 / #66 暂时开着、不花时间修」——其中 #74 / #69 改为按上述顺序修，#71 / #66 仍按 09-20 裁定开着。
- 2026-09-20: **核心测试比例 0.549 用户裁定「认了，范围合理」；#74 / #71 / #69 / #66 暂时开着、不花时间修**——依据：用户 2026-09-20 原话「测试比例 0.549 认了，范围合理，其他开着吧，不浪费时间修，暂时」。数据：6090 / 11087，比 09-15 的 0.509 高 4 个点，增量全部来自 09-19 至 09-20 的三处——`test-distribution` D-7h（装出的副本跑自带审计，+52）、`test-audit-defects` §TODO-73（CRLF，+144）、`test-progress-archive`（归档脚本，400 行，初版 610 行已打回瘦身）——都落在「装坏别人项目」与「丢数据」两类上。适用范围：本次收口；Pinned 的三分之一到二分之一区间仍是规则，要压回 0.5 约需退休 550 行老测试，留到发版前过 `test-age` 时一起做，不单独开工。四条开着的缺陷不是否决，是排期：用户接下来的精力在 ccb-base 多 Agent 集群上（切到那个目录去做）；其中 #74 属数据丢失类，记着不等于豁免，重开 cc-base 的安装器工作时它排第一。取代：2026-09-15「比例 0.509 用户裁定范围合理，不再压」里的那个数。
- 2026-09-19: **progress 归档改由脚本搬运；保留线压到触发线下面一截；已关闭的 TODO 也归档**——依据：用户 2026-09-19 晚对主 Agent 给的数回「认」。数据：recorder 没有 Bash，手工搬一条上千字的条目等于重敲一遍，当天两次归档都撞 25 轮上限没做完、各二十多万 token、其中一次丢了三条决策；原规则归档完正好 50 + 50 = 100，对「> 100」没有回差，下一条记录又触发；recap 实际读进去的约 81KB 里，TODO 占 49KB，其中早已关闭的 59 条就占 32KB。内容：Done / Notes 各留 35、Decisions 留 24，三条触发线（合计 > 100、> 30）不动，所以 recap 的成本上限不变；已关闭（只认 DONE / 完成）的 TODO 超 20 条才搬、留编号最大的 10 条，`OPEN` / `部分完成` / `明确不做` 永不搬，编号不重用（指针行记已归档最大编号）。适用范围：本仓与下游的 progress.md / progress.archive.md；recorder 发现超线只回报、主 Agent 跑 `node .claude/scripts/progress-archive.mjs`，脚本不在的老安装才手工搬、手工搬守 #72 的先写后删。取代：2026-09-15「记忆层封顶」里「正文留最近 30」的保留数，以及 progress-recorder skill「Pinned / TODO 永不搬」中 TODO 的那一半。
- 2026-09-19: **ADR 不设条数下限，三条都占才记：撤回代价高、不交代背景后来的人会纳闷、当时真比过候选**——依据：用户 2026-09-19 对主 Agent 的对照建议回「甲乙」；「≥3 条」是配额，两个人的内部工具选 SQLite 没有真备选，为它编「被拒备选」是凑数，读的人还会当真，正撞 Pinned「四个避免过度」；本仓 `arch-designer` [第一性原则] 的不变量测试本来就是「不兼容、不显然、真取舍才写」，ADR 那几行用配额跟它打架。适用范围：`arch-designer` 产出的 Architecture-Design.md，S / M / L 三档同；风格 / 存储 / 通信三样仍必须有结论，够不上 ADR 的记在模板 §8 的表里；够得上的照旧写全字段，2026-09-10 定的 revisit-if / reversal 两必填字段与 `adr-check` 校验不变。取代：`arch-designer/SKILL.md` 验收清单「关键决策 ≥3 条 ADR」与设计维度清单「至少覆盖 风格选型 / 存储 / 模块间通信」两句（2026-09-10 #60 重写时带入，未单独成文）。
- 2026-09-19: **#70 的修法定为「只补白名单条目」**——依据：用户本 session 在三选一里的拍板（不改扫描器、不改安装器）。适用范围：#70 本身。取代：TODO #70 里「修法三选一待用户定」那句。
- 2026-09-19: **「规则键在精确路径上、同一份东西换条路径出现就失效」是本仓的一类高发缺陷，不是个案**——依据：本 session 一天内撞见两例：第一例是 #70（`instructions-allowlist.json` 的 `file` 键）；第二例是 `gen-manifest.sh` 排除表里 `agent-memory/*` 这个从头锚定的 `case` 模式，对多一层前缀的 `.claude/agent-memory/...` 匹配不上，把误写的文件扫进官方清单。上一轮 code-reviewer 的战术记忆第一条就预言了这类（点名 `instructions-allowlist.json` 的 `file` 键、`FRAMEWORK-MANIFEST.txt`、四份排除表各自认路径）。适用范围：今后新增「同一文件拷两处」的分支时，逐个对这四处。取代：无（补缺）。注：第二例本轮是靠归位文件解决的，没有改排除表，排除表的锚定弱点仍在。
- 2026-09-19: **主 Agent 早先「全仓按显式 `.claude/…` 路径键住的配置只有三处」的判断不成立**——依据：那次只 grep 了 JSON 配置，漏了四份排除表。适用范围：一切「这类缺口还有没有别处」的自审，必须连排除表一起数。取代：本 session 主 Agent 自己那句未入库的口头结论。
- 2026-09-18: **借鉴 mattpocock/skills 只取 SKILL.md 正文与其 references 里的写法和内容，现有流程不动**——依据：用户 2026-09-18 原话「我关注的是Skill里面的借鉴，之前的流程不想变」，随后收窄为「先看看grill-me 在需求，设计，上有什么借鉴」。适用范围：本轮对照借鉴的全部产出；「流程」含 skill 之间的先后与路由、谁能触发、子 Agent 接线、CLAUDE.md 调度段。取代：主 Agent 同日早先四项建议里动流程的两项（release-builder 拆两层；四个手动 skill 关模型调用并去掉 CLAUDE.md 重复触发词），未入库即撤回；#69 缺陷本身仍挂 TODO，修法限定为不动流程的那种。
- 2026-09-15: **核心测试 5463 行、比例 0.509 用户裁定「测试可以了，范围合理」，不再压**——依据：用户原话；三批被证明有牙的用例已恢复，剩余基本落在泄密 / 毁数据 / 装坏别人项目 / 发错版四类地板与每文件底线上。适用范围：本轮收口；Pinned 的 1/3 到 1/2 区间仍是规则，超出个位数百分点由用户看范围拍板，不自动触发再砍。取代：当前断点里「下一刀在 test-hooks-node.sh / 两份 .ps1 测试 / run-all.sh」那条待办。 → 被 2026-09-20 取代（数字更新为 0.549，裁定口径不变）
- 2026-09-15: **主 Agent 可直接写的「文档类」扩到 rules / skills / progress**——依据：本轮用户拍板由主 Agent 直接改规则与 skill；它们是提示词文档不是业务源码，`no-direct-code-guard` 的 EXEMPT 本就放行 `.claude/` 与 `*.md`，规则写成与闸一致；code-review 2026-09-15 L-3 指出此扩张未记录，故补记。适用范围：CLAUDE.md 铁律 1 的「文档类」定义。取代：HEAD 版「仅 Product-Spec / CHANGELOG / DEV-PLAN 不受此约束」。
- 2026-09-15: **整批 code-review 的裁决**——H-1（catalog 在、ext 没装三道大仓闸静默放行）改为当场可见提示 + 细则改口径 + doctor 交叉判；H-2（密钥扫描器三条用例被退休）收回三条并把「密钥 / 危险命令 / 安装器 / 发版装配四类地板用例不退休」写回 CLAUDE.md；M-1 回注跳过已取代 Pinned；M-2 tdd-gate advise 也记账；M-3 每角色 agent memory 上限从 30KB 改 50KB（code-reviewer 15 份小模式文件 48KB 是真家底，搬空是假瘦身）；L-1 Decisions 再归档 5 条；L-2 两处旧路径修掉；L-3 见上一条。依据：code-reviewer 2026-09-15 回执，每条附沙箱复现。适用范围：本轮收口。取代：无。
- 2026-09-15: **三个闸挂到它们声称守的位置**——`tdd-gate` 改挂 PreToolUse(Agent) 按 `tool_input.subagent_type === implementer` 判（实测事件带该字段，官方文档：PreToolUse 匹配 `Agent`、exit 2 可拦）；`no-direct-code-guard` 事件带 `agent_id` 即放行（子 Agent 内 hook 同样触发，实测 implementer 写 src/app.ts 被 rc 2 拦下）；`.red-verified` / `.tdd-exempt` 只认两小时内的 mtime，过期当场删；`stop-gate` standard 降 advise、strict 保留 block。依据：账本 19 次 tdd-gate 拦停按构造全是 Bash 文本误伤、stop-gate 放行契约是被约束方自己 `echo clean` 加三振放行（2026-09-03 一天 38 次）、一枚 9 月 11 日的空标记让闸静默四天。适用范围：本仓与所有下游安装。取代：2026-09-10「tdd-gate 三档全 advise」（那是给误伤止血，不是修因）。
- 2026-09-15: **每个 Task 先定档再派，三条链取代一条流水线**——LOW（< 50 行、不碰契约 / 解析器 / 鉴权 / 迁移 / 支付 / hooks、不引新依赖）只派 implementer、主 Agent 对 diff 与运行器输出验收；MEDIUM 加一轮 reviewer 只跑 Stage 0 + 1；HIGH 全套 + tester。implementer / tester 默认 sonnet，HIGH 派单传 opus；code-reviewer / deployer 仍 opus。依据：同一套重流水线套全档，小 Task 成本 3 到 5 倍，换来的是 Pinned 自己都承认不承重的同模型复审；用户全局规则「sonnet 默认、opus 必要时」。适用范围：dev-builder 的 per-Task 链；Phase 四步走不变。取代：2026-09-10 前「每 Task implementer → reviewer 三阶段 → 修 → 复核」与「四个执行角色一律 opus」。
- 2026-09-15: **记忆层封顶**——Decisions 超 30 条归档（正文留最近 30，指针一行）、Pinned 封顶 15、追加 Decision 前必做取代检查、recap 不读归档；agent memory 单文件封顶 5KB、每角色 30KB，超了搬 docs/agent-notes/。依据：progress.md 286KB 约 6 万 token 一次 recap 吃掉三分之一上下文，149 条 Decisions 只有 3 条标过取代；tester 一份 43KB 的「记忆」让 fresh 不 fresh。适用范围：本仓与下游 progress-recorder skill。取代：2026-09-05「超 100 条归档」只管 Notes / Done 的口径。
- 2026-09-15: **CLAUDE.md 压成「每条铁律写明谁在守」**——189 行 35KB 约 7.7K token 压到 126 行 19KB 约 4.1K token；每条规则要么点名闸、要么标「靠自觉」；删光「细则见 feedback/…」交叉引用（已毕业的 feedback 走 FEEDBACK-INDEX）；Skill 调用规则去掉「1% 即调」改为「匹配即调、拿不准问一句」；矛盾清单当场消除（≤ 6 次工具调用只对 LOW / MEDIUM，maxTurns 是熔断线；fresh 实例与 agent memory 的关系写进 memory-systems）。依据：十处铁律六处靠自觉七处必须先读，同一规则五层重复，前沿模型对少量精确有闸的不变量执行更稳。适用范围：主控与各 rules。取代：2026-09-02「CLAUDE.md 索引化」那版的段落结构。
- 2026-09-15: **大仓治理引擎拆为可选包**——`.claude/harness/ext/` 收 14 个引擎模块 + 两份细则，核心只留 harness.mjs + lib/{core,tier} + profile.json + exclusions.json + audit/；harness.mjs 惰性 import，没装以 rc 3 报 not installed；`setup.sh --with-harness` / `setup.ps1 -WithHarness` 才装，manifest 与安装器排除表同步；CI 不拆格（selftest 8 秒，不值得加 paths-filter）。依据：15.5K 行、39 子命令本仓默认关闭却占有效代码七成、拖 80KB 必读规则；近 60 天 184 次提交 180 次在改框架自身。适用范围：分发与安装；引擎契约（子命令 / stdout / 退出码）零变化。取代：无（v2 起「单文件引擎放在 lib/」的布局）。
- 2026-09-12: **`occurrences` 计数字段删除，evolution 的毕业判据从「数次数」改成「核现状 + 看实证」**——依据：用户拍板「删了」；实测该字段 43 条里 39 条恒为 1（含明说「以后还会踩」的 git-diff 那条），从没人维护，而 evolution-engine 有三条判据挂在它上面（毕业候选 occurrences>=3、Skill 优化信号合计>=5、新 Skill 候选>=5），等于三条判据全年无法达标、靠 evolution-runner 绕过阈值做实质判断才跑得动（2026-09-12 那次六条裁定就是这么出来的）。新判据：① 逐条现状核查——去 CLAUDE.md / rules / SKILL.md / agents 实际 grep，已成文的以既成事实毕业并点名行号，没成文的再往下判；② 实证门槛——提落地前问「这条实际造成过什么代价」，只有用户当场纠正而无代价记录的判「暂不成文」并把理由写进 frontmatter 与索引，下次不重裁；③ 跨条同族按主题聚类，不按条数堆。适用范围：evolution-engine 三步筛选、evolution-runner 角色定义、feedback-writer 去重流程（同主题第二次改为「补这次的触发场景」而非计数器 +1）、feedback-observer 回执格式、EVOLUTION.md 第二层、feedback 话题模板。取代：原按单文件计数器的三条阈值判据；EVOLUTION.md 第二层原「重复出现 3+ 次」的单文件计数口径（跨文件模式聚类那半保留）。注：`harness/lib/evidence.mjs` 里的 occurrences 是证据账本里「被豁免的 check 出现次数」，与本条无关，未动。
- 2026-09-12: **三个「零记录」hook 一个不砍，改的是度量口径**——依据：实测——`no-direct-code-guard` 与 `harness-async-verify` 在本仓 gate-block.log 零记录，但在下游 digifiber-conflation-claude 分别拦了 **28 次**与 **5 次**；本仓 tsconfig 0 个、`.py` 0 个、`module-catalog.json` 不存在，三个闸的前置条件压根不在本仓，下游有 343 个 .py + 1 个 tsconfig + 启用了大仓治理。另查明 gateLog 只在拦停时写账本（调用点 pre-commit-check.mjs:169/172、no-direct-code-guard.mjs:29、harness-async-verify.mjs:76/85），「零记录」= 从没拦过、不是从没跑过，脚本没有「跑过 N 次」这个分母。适用范围：gate-audit 的零记录判定与一切据它做的留/砍决策。取代：本日早先主 Agent 自己提的「三个零记录闸按闸靠数据留的规矩砍掉」那个候选动作（未落进 progress.md、未执行即撤回）。
- 2026-09-12: **框架仓审自己的闸会系统性低估「为下游而存在」的闸**——这是 gate-audit 的结构性偏差，不是个别误报。依据：gate-audit.sh 第 26 行本来就有同类防误报先例（信息类 hook 不调 gateLog、必然零记录，故不纳入统计），这次是同一种错的第二个面（见上条实测）。适用范围：本仓一切「某机制有没有用」的自审；判某闸该不该退役前必须查下游项目的 gate-block.log。取代：无（补缺）。
- 2026-09-12: **feedback 的「58% 从没被引用」这个指标被主 Agent 自己推翻，不作为行动依据**——依据：该指标混了两类——① 待处理却没人动（真问题，本日从 12 压到 6）；② 已毕业、细则文件没被任何规则按文件名链接（基本无害，规则正文已在 CLAUDE.md 里生效，细则只是事故存档）。据此撤销原拟的两个动作：给 7 条已毕业条目在宿主规则末尾补「细则见 feedback/xxx.md」指针（撤销理由：只换来细则可达，代价是往一部已有 360/446 条规则无机器判据的宪法再加 7 句，方向反了）；删除 3 条以退休形式毕业的条目（撤销理由：它们的 graduated 注释里记着为什么退休，删了这段历史就没了，而留着成本为零——删是负收益；用户 2026-09-12 也判「作用不大」）。适用范围：feedback 维护的优先级判断。取代：本日早先「25/43 从没被引用是个问题」的表述（未落进 progress.md 的口头评估）。
- 2026-09-12: **递归深度那条裁定暂不成文（不是未处理）**——依据：六条积压里唯一无实证，feedback 正文只记了用户当场纠正、没有由此产生的返工或耗时代价，按 [开发测试规则]「闸靠数据留，不靠感觉留」不够资格进规则，且机器判不了、误报率高。适用范围：该条已在 frontmatter 与 INDEX 写明理由，下次 evolution 扫描不重新裁定；再撞一次、有了实际代价再落进 rules/subagent-dispatch.md 派单纪律段。取代：evolution-runner 给它的「建议落地」提案。
- 2026-09-11: **采集闸提醒的读者改成子 Agent 自己**（用户 2026-09-11 在四象限选项里拍板「改措辞，按子 Agent 写」）——依据：实测两例——tester 与 implementer 都在回执里引用了该 hook 注入的原文，而主 Agent 这侧两次都没收到；官方文档（https://code.claude.com/docs/en/hooks）Stop 与 SubagentStop 两处措辞相同、只写"Added to Claude's conversation"，不判归属，所以以实测为准。适用范围：subagent-acceptance-reminder.mjs 的文案，以及 CLAUDE.md 两处 + ARCHITECTURE.md 一处原先写「注入提醒主 Agent 验收」的口径。取代：原「执行类 Sub-Agent 一返回就把验收铁律注回主 Agent」的表述（散见 CLAUDE.md 与 ARCHITECTURE.md 正文，未单独成文入库）。连带结论：主 Agent 侧的验收没有机器提醒兜底了，靠读回执正文 + 五步闸自觉。
- 2026-09-11: **Domain findings 判据改按开头词判，不按整串相等；撤回「约束回执写裸 None」那个取舍**——依据：tester 在自己身上撞出 `**Domain findings**: None（本轮全是框架自身测试基建）` 被整串相等判法翻面判成「有发现」，两个不同子 Agent 的自然写法 2/2 命中，误报率高到抵消上一轮反转的意义；原先选「字面严判」的理由是「放宽会吞掉写在括注里的真发现」，在文案改成子 Agent 自检之后这条理由不成立——漏提一次几乎没代价，回执正文照样进主 Agent 上下文。适用范围：该 hook 的 hasDomain 判据与 subagent-dispatch.md 回执信封那句。取代：本日早先由主 Agent 自行拍定、未单独成文入库的「A 方案：维持字面严判 + 回执写裸 None」。
- 2026-09-11: **中文判据不能用 JS 的 `\b`，定稿 `!/^(none|n\/a)\b|^无(?![一-鿿])/i`**——依据：主 Agent 第一版给的 `^(none|n\/a|无)\b` 经 tester 实测 + 主 Agent 用 node 独立复跑八种写法证伪——`\b` 是 ASCII 词边界、对汉字不成立，「无（括注）」判不中，且会让现在判对的裸「无」退化；同时纯前缀判法 `^(none|n\/a|无)` 会把「无线接入侧 VLAN 按 OLT 槽位算」这类以「无」起头的真发现吞掉。适用范围：本 hook；凡在中文文本上做词边界判断的闸同理。取代：主 Agent 第一版的 `\b` 方案（从未入库）。
- 2026-09-11: **口径库改定位为「需求分析/澄清/方案设计的副产品」，不是开发流程的一环**（用户 2026-09-11 原话纠正）——依据：口径库目标是辅助 AI 理解、换来更准的需求规格、更稳的架构、更好用的前端设计；有最好、没有也不强制报错；条目可以跟项目有关，也可能扯很远，会持续增加。适用范围：整个 domain rulings 机制与其全部接线点（domain-rulings.md、domain-recorder.md、domain-rulings skill、subagent-dispatch.md 回执栏等）。取代：09-11「立领域口径库机制」条里把它当流程组成部分的隐含前提（机制本身七栏/四类/三变更/四老化/三触发源不变，见下方该条）。
- 2026-09-11: **采集闸方向反转：回执里确实有领域发现时才提醒主 Agent 判一句要不要收，不是缺栏就点名**（用户 2026-09-11 纠正）——依据：副产品不该催，催出来的是凑数，凑的没有依据，进库即噪音——这正是规则本身要防的东西。适用范围：`subagent-acceptance-reminder.mjs` 的提醒文案与判定条件。取代：09-11「采集闸现在就上」条中「回执缺 Domain findings 就点名」的设计（「检索校验闸仍押后」的判断不受影响，见下方该条）。
- （更早的 132 条 Decisions，2026-06-14 至 2026-09-10，2026-09-15 首批 119 条、2026-09-18 再搬 1 条、2026-09-19 再搬 4 条，原文搬迁至 ./progress.archive.md「Archived Decisions」。仍在生效的硬约束已在 Pinned；主 Agent recap 默认不读归档，按需查。）
（归档指针：累计搬出 7 条，最近一次 2026-09-20 搬 7 条；原文见 progress.archive.md「Archived Decisions」）

## 单模型 vs CCB（诚实定位）
- 客观轴（TDD/测试/静态闸/证据验收）：与 CCB 持平，模型无关。
- 审查轴：对抗式 QA + 三阶段（Stage 0 静态闸/Stage 1 规格/Stage 2 质量）+ CoVe 证据锚定，比温和 QA 强，但**同模型**——「第二个脑子挑盲区」补不了，是 CCB 唯一硬优势。
- 换来：轻、跨平台、无 CCB 运维脆弱（绑定/pkill/通知失效/daemon）。单用户 Windows 场景划算。

## 当前断点（2026-09-20，#70 / #72 / #73 / #75 全部收口）

**状态（2026-09-19，已推送）**：用户指令「push」，#70 一组与 09-18 grilling 那组按收口 reviewer M4 合成一个提交 `037908e`（19 个文件，清单与两组同批，中间无红提交），auto-push hook 推出、`ls-remote` 核远端 = 本地 HEAD；CI run 35435061531 五格全 success。推前本地 `run-all --level high` rc 0（44 秒，本机有 pwsh 后 test-ps1-behavior 真跑 10/0）。用户回「push」即视为 7 个 skill 文件措辞认可。提交标题 101 列超 commit-msg 建议的 72，只告警，已推送不改写。待用户定两件：① #73 CRLF（收口 reviewer 的需求存疑）——拍板「只补白名单」时 Windows `autocrlf=true` 那一格在不在考虑内、要不要修；② 下一步做什么——主 Agent 建议先修 #72（归档先删后写，今天真丢过、若丢的是未提交的新决策就无法恢复），其次 #74（ps1 重装不留 `.bak`），再是 grilling 第 3、5 条或转开发侧。
**状态（2026-09-20 凌晨）**：09-19 一天收了四条——#70（--with-harness 装出的副本缺豁免）、#72（归档先写后删）、#73（CRLF 下白名单失效）、#75（归档交给脚本 + 保留线 + 已关 TODO 纳入），另按用户「甲乙」落了两条 skill 借鉴（ADR 三条都占才记、能答的人不在场时出问卷）。本条之后的第一件事就是用新脚本对本仓真文件跑第一次归档，结果随同一提交入库。测试比例 0.549 用户已认（见 Decisions 2026-09-20）。仍开着、用户定了暂时不修的：#74（可选包分支绕过三层保护，ps1 重装不留 `.bak`，数据丢失类）、#71（gen-manifest 排除表锚定）、#69（deployer 预加载 release-builder 进不了上下文，先做只读探针）、#66。借鉴线其余（可点样机 / 开发侧 / writing-for-agents）用户定了等实战打磨，不主动推。用户接下来要合计的是多模型分工：主 Agent 已摸过 `~/code` 下十三个 `*-base` 的家底（有自己纠正记录的只有 cc 一脉、zcode、cursor；Muse 在被告知不再带它之后仍自行把 skill 从 19 个砍到 3 个），等用户回三个问题——接下来打什么仗、哪个模型在哪个网络能用、长期养哪几个。

**状态（2026-09-19 晚）**：用户回「修」，#72 已修并入库 `3280d1a`（SKILL.md 先写归档后删正文），随即拿当天欠的一次真归档做行为验证：recorder 再次撞轮次上限，但这次零丢失（全文见 TODO #72）。正文现为 Done 50 + Notes 50、Decisions 30 加一行指针。同日审了 Muse Code 照本仓搭的 `~/code/musecode-base`（结构性照抄、零真实使用），给了一份第二轮审查提示词；用户结论是不从头带 Muse，主力继续本仓。随后按用户「甲乙」落了需求与设计侧两条借鉴（见 Done 同日条与 Decisions「ADR 不设条数下限」）。**用户 2026-09-19 晚已拍板（进行中，完成后转 Decisions / Done）**：#73 修；#75 认——Done / Notes 保留线各 35、Decisions 保留线 24，三条触发线不动；已关闭的 TODO 也归档（超 20 条才搬、留编号最大的 10 条，只认 DONE / 完成，指针行记已归档最大编号防重号），搬运交给脚本 `.claude/scripts/progress-archive.mjs`；借鉴线其余（丙 / 开发侧 / writing-for-agents）等实战打磨，不再主动推。以下为拍板前的旧待定，留作对照：① #73 CRLF 修不修；② #75 归档改脚本 + 阈值加回差；③ 之后先 #74（ps1 重装不留 `.bak`）还是接着看 grilling 第 3、5 条。

**状态（2026-09-15，五项重构已提交推送 CI 全绿；使用指南 docs/guide/ 十五章已落地）**：`9ee1692`（重构本体，84 个文件）+ `6602147`（白名单重签 + pre-commit 补指令扫描），远端 main 已核对，CI run 34945476225 全 success。下一步由用户定：要么继续压核心测试比例（下一刀位置见下），要么开始用新流程跑一个真项目验证三档表与新闸在真实派单里的手感。
**剩余账（明确未做）**：核心测试比例 5463 / 10726 = 0.509，用户 2026-09-15 裁定范围合理、不再压；ext 那一对 3031 / 11352 = 0.27 低于 1/3，ext 是可选包，暂不补。CI 未按路径拆 harness 格（selftest 8 秒不值得）。Done / Notes / TODO 未归档（没到阈值）。

## TODO
- [P2][DONE][#77] **`run-all.sh:324` 失败汇总写死一句「静态自测失败（安装器/路由一致性不过）」，四个套件共用一句会带偏排障方向**（2026-09-21，来源 PR #2 正文题外话，主 Agent 核实属实）：同一段循环（`:164`）里实际跑着 test-predev-lint / test-ui-audit / test-plan-lint / test-ui-slop-scan 四个套件，哪个红了都报这一句——PR #2 的作者就是被这句话带去查安装器和路由，真凶其实是 ui-audit 的 U4 假红（见 Done 同日条）。建议修法（未实施，待用户排期）：汇总点名到具体失败的套件；**不建议**把 fail-fast 整体改成「跑完再汇总」——快失败是有意设计，点名到套件已解决八成问题。**已完成（2026-09-22）**：`mark_static_fail <套件名>` 一个函数收口 13 处失败点，汇总行改为「失败套件：A、B」；主 Agent 亲跑 `run-all.sh --level all` rc 0（08:24–08:26），另在 /tmp 隔离克隆里把 test-routing.sh 换成必败桩跑 `--level high`，汇总行输出「静态自测失败（失败套件：test-routing.sh）…停止」且第三段未跑（fail-fast 未变）。implementer 用了 24 次工具调用 / 72 分钟，远超 LOW 档 ≤ 6 次的预期，见 Notes。
- [P2][DONE][#76] **`secret-exfil-guard.mjs` 的 R1/R2 按命令名枚举判定，换个读取器命令就能绕过**（2026-09-21，审查兄弟仓 ccb-base 移植同款闸时回头实测本仓地板闸发现）：R1（直读密钥文件）与 R2（拷走密钥文件）分别只认 cat/less/more/head/tail/strings/xxd/od/bat/grep/rg/awk/sed 与 cp/scp/rsync/mv（`.claude/hooks/secret-exfil-guard.mjs:22-29` RULES）。实测：向该 hook 喂 `tac .env`、`paste .env`、`jq -R . .env` 三条命令，全部 rc=0 放行（期望 rc=2 拦截）。与 #70（路径前缀锚定）/ #73（CRLF）/ #75 审查中发现的标题 startsWith / 子串去重 / 纯数字 id 正则是同一类——规则钉在精确形态上、同一件事换个形态就失效，这是第六次。建议修法方向（未实施，待用户排期）：判据从「命令是不是读取器」翻成「参数里有没有密钥路径」——任何命令只要 argv 里有 token 命中密钥路径就拦，只对一张很短的、封闭的「只碰元数据不读内容」的命令表（ls/stat/test/chmod/chown/rm 一级）放行；允许表是封闭集合能枚举完，读取器表不是。需先派 tester 出红（`tac .env` 类），属 red-locks-the-bug 适用范围（地板闸、安全类）。把路径拼出来的读法（如 python 里 chr() 拼路径）hook 层原理上拦不住，修时要在闸的说明里点名这条边界。风险定性：安全类、地板闸；用户 2026-09-20 裁定「其余缺陷暂时开着、不浪费时间修」针对的是 #74/#71/#69/#66，本条是其后新发现，是否排期由用户定。README.md 第 89 行现写着「任意子进程绕读由 secret-exfil-guard hook 补拦」——这句承诺得比实际强（本条实测 `tac .env` 一类读法放行），与 PR #2 去掉 `Bash(git push*)` 那句是同一类「安全文档过度承诺」；修本条时把这句一并改成与实现相符的表述，修之前不单独动它。**已完成（2026-09-22）**：判据翻面为「任何动词 + 参数里有密钥路径 token 就拦，只对封闭的元数据放行表（ls/stat/test/[/[[/file/du/chmod/chown/chgrp/rm/unlink/touch/mkdir/rmdir/cd/pushd/popd/basename/dirname/realpath/readlink/which/echo/printf/find）放行」，引号感知切分切词、嵌套引号子串与 key=value 右值与重定向都算参数、动词位自己也判、glob 用线性通配匹配（不构造 RegExp，畸形字符类按字面、模式 > 512 按字面、纯通配不算命中、带目录前缀按文件名部分匹配）。三轮红锁：tester 出 SE-14…SE-39（红 16）→ 修 → reviewer 三 Stage 出 HIGH（`$(which tac) .env` 命令替换当动词）+ Medium glob → SE-40…SE-50（红 6）→ 修 → 复核出两条新 HIGH（`[z-a]` 抛异常穿透 fail-open 整条放行、200 星灾难回溯挂死）→ SE-51…SE-57（红 5 + 目录前缀 glob 1）→ 换线性匹配器 → 第二次复核「收口」（7 条畸形边界探针无异常无超时、星号 20→400 耗时平在 40ms）。变异测试 M1–M9 有牙（M4 为等价变异）。主 Agent 亲跑 floor 69/0、node 42/0、settings 4/0、tier-hardening 4/0、`run-all --level all` rc 0（10:09–10:10）。审查与二次复核均为 sonnet（opus 两次 API 529/500 在第一步中断）。残留见 #78 / #79。
- [P3][DONE][#78] **`secret-exfil-guard.mjs` 嵌套引号扫描把「引号里提到文件名的纯文本」当参数命中（过度拦截）**（2026-09-22，#76 审查 Medium）：为识别 `python3 -c "open('.env')"` 这类解释器载荷，`tokenHits` 对 token 内的嵌套引号子串整体判路径，但不区分「被引用的文本是不是真的在被当路径打开」——`git commit -m "see '.env' later"` 这类只是在引号里提到文件名也被拦；审查时 tester 的探针脚本、主 Agent 写 progress 的 heredoc 都撞过。修法方向（未实施）：只对动词是解释器 / 求值类（python* / node / ruby / perl / php / eval / sh -c）的 token 做嵌套引号扫描，其余动词只判整 token 与 `=` 右值。**已完成（2026-09-22）**：嵌套引号扫描只对解释器 / 求值类动词（python* / node / ruby / perl / php / deno / bun / eval / sh -c 一族）做，其余动词只判整 token 与 `=` 右值；命令载体（ssh / su -c / sudo / timeout / watch / nohup / xargs / parallel / docker exec|run / kubectl exec / podman exec，以及 shell 动词只隔短选项后的 `-c <引号参数>` 与 `eval <引号参数>`）的引号参数递归当命令重扫、深度封顶；管道级规则：前段有密钥路径、后段 xargs / parallel 转发非放行动词 → 拦；收尾引号只与剥掉的 `sh -c "` 配对剥。三轮红锁：tester SE-62…SE-73 出红 → 修 → reviewer 出 H1 `git log -p .env`、H2 `git ls-files .env | xargs cat` 管道洗白、H3 `grep -- -x .env`、H4 收尾引号裸剥（`node app.js --config "x '.env'"`）、H5 `ssh host 'cat .env'`、M1 `-c` 任意位置即开扫 → SE-74…SE-87 → 修 → 复核出 H6（`bash -x -c "cat .env"` / `eval "cat .env"`：`-c` 前夹选项不展开、单层引号无第二层可配）、H7（`parallel 'cat .env'` 不在载体表）→ SE-88…SE-105 → 修 → 第三次复核「收口」（H6 六条 + 补测 `bash -xe -c` / `bash -o pipefail -c` / `sh -c` / `zsh -x -c` / `eval cat .env` 全 rc 2，H7 两条 + `parallel -j4` / `parallel --jobs 4 cat ::: .env` 全 rc 2——后者靠裸 token 字面匹配抓住、不靠载体递归，以后若给 parallel 加参数选择性跳过会失去保护；控制组 `bash -x -c 'ls -la .env'` / `eval "ls .env"` / `parallel 'ls .env'` / `bash script.sh -x -c "cat .env"` 全 rc 0；H1–H5 / M1 回归持平；畸形输入无异常、长管道 / 深嵌套 ~117ms；diff 只四处全在 H6/H7 范围）。主 Agent 三轮各亲跑：floor 85/0 → 101/0 → 120/0、`run-all --level all` rc 0（scratchpad runall79 / 79b / 79c）。hook 339 → 756 行。审查与三次复核均为 sonnet。边界不变：代码里拼路径、变量间接（`for f in *; do cat $f`）、纯通配不判。
- [P3][DONE][#79] **git 元数据查询与 `cat .env` 一视同仁被拦（需求存疑）**（2026-09-22，#76 审查探针实测）：`git ls-files .env`、`git check-ignore .env`、`git status --porcelain | grep .env` 全 rc 2——它们只读元数据、不读内容，本该像 `ls .env` 一样放行；但 `git diff .env` / `git add .env` / `git show HEAD:.env` 会碰内容，git 整体进放行表不成立。修法方向（未实施）：放行表支持「动词 + 子命令」二级项（`git ls-files` / `git check-ignore` / `git status` 放行，其余 git 子命令照拦）；`grep` 的情况（`… | grep .env` 只是在过滤输出）与 #78 同源，可一起看。**已完成（2026-09-22，与 #78 同一批）**：放行表加「动词 + 子命令」二级项：`git ls-files` / `check-ignore` / `status` / `log` 放行，`log` 带 `-p` / `--patch` / `-u` / `-L` / `-G` / `-S` / `--full-diff` 不放（reviewer H1 反例：`git log -p .env` 打印完整内容），`--stat` 只打统计仍放；其余 git 子命令（diff / add / show / cat-file）照拦；grep 一族第一个非选项参数是模式不判、认 `--`（H3 反例 `grep -- -x .env`）、`-e` / `--regexp=` 定模式后 `--` 不再消耗名额、`-f` / `--file` 照判；冒号后段也判（`git show HEAD:.env`、`scp host:.env`、`docker cp ctr:/app/.env`，`://` 与 `echo a:b` 不算）。红锁与复核同 #78。
- [P3][DONE][#80] **glob 判据把 `*.json` 当密钥通配拦了（`credentials.json` 做候选太泛）**（2026-09-22，#76 落地当天主 Agent 自己撞上：`grep -rln … --include=*.json .` 被拦，`=` 右值 `*.json` 匹配到候选 `credentials.json`）：**已完成（2026-09-22）**：glob 候选 `TYPICAL_SECRET_NAMES` 去掉 `credentials.json`，字面 `credentials.json` 仍按 `SECRET_PATH` 拦；`*.pem` / `*.ppk` 仍算。红锁 SE-58…SE-61（`--include=*.json` 与 `jq . *.json` 放行、字面与 `cp *.pem` 仍拦）。
- [P3][OPEN][#81] **可选包的覆盖语义与主循环仍不对等**（2026-09-22，#74 审查 Medium）：主循环对用户改过的文件不覆盖、落 `.framework-new`；可选包现在是留 `.bak` 后覆盖（单代备份，第二次重装会把上一代 `.bak` 再覆盖掉）。契约①只要求「有保留物 + 报告一致」，本轮达标；要真正一致得让可选包也走 `.framework-new` 不覆盖，但原注释口径是「框架自测 / 引擎不是用户文件、升级直接换新」——两种口径选一个，待定。
- [P3][OPEN][#82] **可选包的排除判断是手写的，和 `exclusions.json` 生成块并存会漂移**（2026-09-22，#74 审查 Medium，reviewer 可复现）：`state/*` / `.DS_Store` / `*.bak` 在 setup.sh / setup.ps1 的可选包分支里各手写一份，`gen-exclusions.mjs --check` 只管 `@exclusions:begin…end` 那段，以后往 json 加条目不会同步到这里。修法方向：让生成器同时产出一个可选包能调用的判定（sh 函数 / ps1 函数），或把可选包并进主循环由开关决定跳不跳（要先定 #81 的口径）。
- [P3][OPEN][#83] **`test-hooks-floor.sh` 的 `show()` 用 `cut -c1-260` 截诊断串，会在多字节字符中间切断产生非法 UTF-8**（2026-09-22，#78 第三轮 implementer 发现，原有 SE-3 用例即可复现）：不影响判定（判定用 rc 整数比较，不解析 show() 输出），但 `grep` / python 按 str 读日志会在那一行断掉，implementer 就因此改用字节计数才确认 120/0。修法：`cut -c` 在本机 coreutils 下按字节切，换成 `awk '{print substr($0,1,260)}'`（UTF-8 locale 下按字符）或 `LC_ALL=C.UTF-8` 下的 `cut -c`；顺手修，不单独派单。
- [P2][DONE][#75] **归档是机械搬运，不该让模型逐字敲；阈值还没有回差**（2026-09-19，#72 验证时带出，未做，等用户定）：① recorder 没有 Bash（`disallowedTools: Bash`），搬一条就得把上千字原文当参数重敲一遍。当天两次归档都撞 `maxTurns: 25` 没做完——上午 25 轮 / 228K token（丢三条），下午 27 次工具调用 / 237K token（#72 修后零丢失，但只做完「写归档」那一半）。主 Agent 用三十行脚本一秒收尾，且整行精确匹配、不存在改字的可能。方向：搬运交给脚本（保留最近 N 条、其余追加归档、逐行核对、再删正文，幂等），recorder 只负责发现超阈值并回报，主 Agent 跑脚本；新脚本属加家底，要配测试，按四个避免过度先过用户。② 阈值「Notes 与 Done 合计 > 100 即各留 50」没有回差：归档完正好 50 + 50 = 100，下一条记录就是 101、按规则又要归档一条，每次一轮二十多万 token。实际情况是 recorder 并不每次都查（09-11 归档后到 09-15 已 114 条、当时断点还写着「没到阈值」），规则与行为两头都不对。方向：触发线与保留线拉开（例如过 120 才搬、搬到各 40），具体数等用户定。同日晚即应验：记甲乙落地加了一条 Done、一条 Decision，两段各溢出一条，主 Agent 用同一段脚本各搬一条（先写归档、核对、再删正文，验收 PASS），没派 recorder。　**已完成**（2026-09-20，用户 09-19 晚回「认」）：新增 `.claude/scripts/progress-archive.mjs`（269 行）——先写归档、从磁盘重读核对、再写正文，任何失败正文一个字节不动；可重跑；CRLF 文件保持 CRLF。保留线 Done / Notes 各 35、Decisions 24，三条触发线不动；已关闭的 TODO 超 20 条才搬、留编号最大的 10 条，只认 DONE / 完成，指针行记已归档最大编号。recorder 的 skill 与 agent 定义、CLAUDE.md [项目记忆规则]、`rules/file-structure.md`、`docs/guide/08` 同步：recorder 发现超线只回报，主 Agent 跑脚本；脚本不在的老安装才手工搬。实现者与测试者各拿同一份契约、互不看产物。真数据副本验收：81 条 TODO 搬走 49 条已关闭的、全部逐字在归档，22 条未关闭的不动，留下的 10 条是编号最大的，第二遍无事可做。**变异测试抓到一个真缺口**：tester 初版的失败安全用例（同名目录顶替归档）在「读归档」那步就退出了，根本没走到「写 → 核对 → 写正文」；主 Agent 把写失败吞掉、把核对关掉，21 条全过——脚本最要紧的那段顺序没有任何测试守着，tester 回执里的「已变异自证」不成立。补了只给测试用的故障注入点 `PROGRESS_ARCHIVE_FAIL_AT`（`archive-write` / `verify`，误设的后果只是什么都不改就退出），用例拆成 A5a / A5b / A5b2，重做变异：两道防线都拆 → A5a、A5b 红；只关核对 → A5b 红；只吞写失败不红，因为核对那道兜住了，属等价变异。reviewer（MEDIUM 档，无 HIGH）另查出两条已修：区块标题按 `startsWith` 认会被 `## TODO 归档说明` 这类标题劫持（收紧为整名或紧跟括号，用例 A10）；判重用子串包含、核对用整行相等，前缀重合的条目每次重跑都卡在退出码 1（抽成同一判据函数，用例 A11）。测试初版 610 行、是被测脚本的 2.5 倍，打回瘦到 330，补完四条臂后 400 行 27 条。
- [P2][DONE][#73] **指令审计白名单在 CRLF 签出下全部失效，#70 只在 LF 签出下修好了**（2026-09-19，收口 reviewer M1 + 主 Agent 实测）：`scan-instructions.mjs:493` 按 `text.split('\n')` 切行，切行前（485-491）只剥 BOM、不去 `\r`，行哈希（341）连 `\r` 一起算 → Windows 上 `core.autocrlf=true`（Git for Windows 安装默认）签出的仓库里，每一条白名单条目都失配（ext 旧条目、#70 新条目、`settings.json:5` 那条都一样）。实测：沙箱里两份 harness-large-repo.md 各转 237 个 CR，两条路径都从 `error 0 allowlisted 1` 变成 `error 1 allowlisted 0`。老问题，不是 #70 引入；出事时是大声拦停，不是悄悄放行。CI 测不出：`gate.yml:59-61` 在 Windows checkout 之前关了 autocrlf。仓里没有 `.gitattributes`，setup 也不给下游装；只有 `docs/guide/14-troubleshooting.md:163` 在排障章里写了关 autocrlf。修法样板在本仓：`setup.sh:241` / `setup.ps1:137` 算清单哈希前先 `tr -d '\r'`（注释原话「LF 归一化后 sha256，抗 autocrlf」）。要点：只归一 `\r\n`，孤立的 `\r` 不能剥——它能在终端里盖掉一行的前半截，是隐藏字符检查该抓的东西。改的是扫描器，和用户为 #70 否掉的修法②是同一处；修不修、怎么修，等用户定。　**已完成**（2026-09-20，用户 09-19 晚回「修」）：`scan-instructions.mjs` 切行前把 `\r\n` 归一成 `\n`，孤立 `\r` 原样留着、照样进哈希（被动过的行对不上旧签名，宁可错拦）；打印给用户的重签命令 `REHASH_CMD` 同序改为「剥 BOM → 归一 → 切行」；整轮只记一条 `crlf-normalized-before-scan:files=N` 汇总 note，不逐文件刷。红锁 `test-audit-defects.sh` §TODO-73 三臂（C1 同内容换 CRLF 豁免须照样生效，修前红；C2 CRLF 下改邻行须失效；C3 行内混进孤立 `\r` 须失效，防「一删了之」），主 Agent 亲见红 16/1 → 修后 17/0。端到端：09-19 下午那个两份文件各 237 个 CR 的安装沙箱换上新扫描器，两条路径都 `error 0 allowlisted 1`；`REHASH_CMD` 对「BOM + CRLF」与「无 BOM + LF」两份同内容文件签出同一组哈希；reviewer 全仓 196 个文件转 CRLF 重扫，finding 集合与 LF 完全相同、两条豁免照样命中。打回过一次：implementer 在注释与 README 里写孤立 `\r`「由 hidden-characters 规则兜底」，该规则字符表里没有 `\r`，是一道不存在的保护，已改成真实理由。注意该红锁文件标 `risk: medium`，本地默认档跳过，只有 CI 全量档跑。未验：Windows 真机 `autocrlf=true` 端到端。
- [P2][DONE][#74] **可选包分支（`--with-harness` / `--with-tests`）绕过主循环的三层保护**（2026-09-19，收口 reviewer M2 + L2 + L3）：`setup.sh:349-356`、`setup.ps1:314-328` 直接逐文件拷，不走 manifest 分层，也不走排除表。① M2：带开关重装会覆盖用户改过的 ext/ 与 rules/ 副本，sh 侧留 `.bak`，**ps1 侧用 `Copy-Item -Force`，什么都不留**；dry-run 还把覆盖报成 `create`（主循环对同类已存在文件报 `skip`）。reviewer 实测：用户追加的一行重装后两侧都没了，ps1 侧 `.bak` 0 个。② L2：ext/ 下的 `.DS_Store` / `*.bak` / `state/*` 会被一起拷出去。③ L3：ext/rules/ 下嵌套的 .md，sh 的 case 模式 `*` 会跨 `/`、把它压平拷进 rules/，ps1 的 `[^/]+` 不会，两侧不对等（目前仓里没有嵌套文件，未触发）。`--with-tests` 分支早就是这个写法，`9ee1692` 的 `--with-harness` 照搬了一遍。属数据丢失类：记残留不等于豁免。**已完成（2026-09-22）**：红锁 `.claude/tests/test-setup-optional.sh` 23 条（PASS 13 / FAIL 10：sh 红 R2 dry-run 报 create、R3 排除表、R4 嵌套 .md 压平；ps1 红 R1 覆盖不留副本、R2、R3）→ implementer 改 setup.sh（+40）与 setup.ps1（+62/−22）：两侧可选包分支走 `plan_pair` 语义（已存在相同报 skip、不同报 update）、ps1 侧补 `.bak` 等价保留物、`.DS_Store` / `*.bak` / `state/*` 排除（手写等价判断，未改 exclusions.json）、sh 的 rules 只顶层 .md 进 rules/（不再跨 `/`）；主 Agent 亲跑 optional 23/0、test-setup 过、test-distribution 27/0、test-ps1-behavior 10/0、gen-exclusions --check rc 0、run-all --level all rc 0（11:30–11:32）；reviewer Stage 0+1「可合并」，另实测嵌套任意深度的 .DS_Store / *.bak / Thumbs.db 两侧均排除；Windows 真实反斜杠路径未在本机验（pwsh 跑在 Linux 上）。残留见 #81 / #82。
- [P2][DONE][#72] **progress-recorder 的快照归档不是原子的：先删正文、后写归档，中途被截断就丢决策**（2026-09-19 实际丢过一次）：本 session 派它做增量合并，Decisions 30 → 33 超阈值顺带触发归档；它把 2026-09-10 三条（升级下游先接本地补丁 / ADR 新增 revisit-if 与 reversal / 闸加严修夹具不修断言）从正文删了，没写进 progress.archive.md 就撞到 `maxTurns: 25` 停下、没交回执。主 Agent 核对正文 0 处、归档 0 处，从 `git show HEAD:progress.md` 逐字节恢复（前后 sha256 同为 `c6a604df…`）。**这次是侥幸**：三条是已提交的旧条目；被挤出去的若是同一个未提交 session 里刚写的决策，git 里没有，就永久丢了，而且没人会发现——recap 默认不读归档。根因：`skills/progress-recorder/SKILL.md` [快照归档] 第二步只写「原文搬迁」，没规定先后，也没规定核对。修法方向（未做，改 skill 属家底，等用户拍板）：先追加归档，按条数核对搬过去的都在，再删正文；核对不过就不删。不另立新闸。　**已完成**（2026-09-19，用户回「修」）：`skills/progress-recorder/SKILL.md` [快照归档] 第二步加两条——先写归档、按「日期 + 标题」逐条搜到才删正文、缺一条则一条不删；归档里已有的不重复追加，截断后可接着搬；自检要点 5 同步；`agents/progress-recorder.md` 归档触发条件补上 Decisions > 30；`docs/guide/08` 搬迁规则同步。入库 `3280d1a`。**行为验证用的是真事故重演，不是安排出来的场景**：当天正文 Done 64 + Notes 58 = 122 超阈值，派 fresh recorder 做这次真归档（派单里故意不提先后顺序），它又一次撞到 `maxTurns: 25` 停在半路——停下时归档里已多出 14 条 Done + 8 条 Notes、与正文逐字相同，progress.md 一字未动，最后一句是「先核对归档里的插入，再碰 progress.md」。上午同样的截断丢了三条，这次零丢失。剩下半截（删正文 22 条、改两行段末指针）由主 Agent 用脚本按整行精确匹配收尾；验收脚本先过六个突变（原样 / 正确搬迁放行，丢条目 / 改一个标点 / 归档少一行 / Pinned 动一个词四种咬住）再用，结果 Done 64→50、Notes 58→50、22 条全部逐字在归档、归档历史零缺失。本条完成事项没有另写 Done 条目：正文现为 50 + 50 = 100，再加一条就是 101 又过阈值，见 #75。
- [P3][OPEN][#71] **`gen-manifest.sh` 排除表的 `case` 模式从头锚定，对多层前缀路径失效**（2026-09-19，#70 同类缺陷的第二例，本轮靠归位文件绕过、未修根因）：排除表里 `agent-memory/*` 这类从头锚定的 `case` 模式，对多一层前缀的路径（如 `.claude/agent-memory/...`）匹配不上，会把本该排除的文件扫进官方分发清单。本轮实例：上个 session 的 code-reviewer 把战术记忆误写进 `.claude/.claude/agent-memory/code-reviewer/`，`gen-manifest` 因此把它收进清单（128→130 条），靠把文件搬回正确路径解决，排除表本身未改。触发前提是有人把文件写错路径，属低频，但后果是把垃圾扫进分发清单，非空谈。同类高发缺陷见 Decisions 2026-09-19「规则键在精确路径上」条。 修法样板就在本仓：`make-release.sh:73` 打包后泄漏扫描拦的是同一条规则，用子串 `"/agent-memory/" in n`，对多层前缀照样命中——同一规则两处实现，锚定的漏了、子串的没漏。
- [P1][DONE][#70] **`--with-harness` 装出来的下游项目，会被框架自带的指令审计判 error**（2026-09-18，code-reviewer 审 `9ee1692` 安装器改动时查出，主 Agent 已复现）：安装器把 `harness/ext/rules/*.md` 另拷一份进 `.claude/rules/`（`setup.sh:346-350`、`setup.ps1:318-329`），而 `instructions-allowlist.json:14` 的豁免只键在 `.claude/harness/ext/rules/harness-large-repo.md` 这一条路径上，`scan-instructions.mjs:73-78` 两处路径都扫。复现：`bash setup.sh --with-harness <临时目录>` 装完，在装出的树里扫 `.claude/rules/harness-large-repo.md` → rc 1、error 1、allowlisted 0；同一份字节扫 ext 源路径 → rc 0、allowlisted 1。`quality-attributes.md` 两处都干净。后果：下游开了 githooks 即提交被拦；不开也是手跑与 CI 侧红。本仓测不出来——`test-distribution.sh` D-7 只判文件装没装齐（PASS=24），不在装出的树里跑审计。修法三选一，用户拍板「只补白名单条目」（见 Decisions 2026-09-19）。属地板类（装坏别人项目）。　**已完成**（2026-09-19 核验）：白名单补第三条 entry（`.claude/rules/harness-large-repo.md:63`，sha256/context 与 ext 源条目逐字复用，两文件整文件 sha256 同为 `898cfe61…`）；红锁 `test-distribution.sh` D-7h（+52 行）主 Agent 亲见红 `PASS=25 FAIL=2` → 修后 `PASS=27 FAIL=0`；反向验证（改第 63 行本身 / 只改邻行 62 行）均证豁免绑内容不绑路径，没开静默口子；此前两条「未实证」保留——「setup.ps1 侧结论全为读码、本机无 PowerShell」与「后果段未端到端实跑」——已消除：本机实有 pwsh 7.4.10 真跑 `setup.ps1 -WithHarness` rc 0，沙箱全量 tracked 扫描 rc 1、唯一 error 坐实下游会被拦。**收口**（2026-09-19）：收口 code-reviewer 一轮（opus，Stage 0 / 1 全做、Stage 2 做了一部分）无 HIGH，`.claude/.needs-review` 已清；M / L 见 Notes 同日残留与 #73 / #74。注意 #70 只在 LF 签出下修好，Windows `autocrlf=true` 那一格见 #73。入库 `037908e`，CI run 35435061531 五格全绿。
- [P1][DONE][#69] **deployer 预加载的 release-builder 按官方文档根本进不了它的上下文**（2026-09-18，对照 mattpocock/skills 的「用户触发的 skill 谁也够不到」不变量时查出，未修、未做实机探针）：`agents/deployer.md` 写 `skills: release-builder`，而 `release-builder` 设了 `disable-model-invocation: true`；官方文档两处明文——skills 页该字段说明「Also prevents the skill from being preloaded into subagents」，sub-agents 页「You can't preload skills that set disable-model-invocation: true, since preloading draws from the same set of skills Claude can invoke」。deployer.md 正文只说「使用 release-builder skill 执行」，没有读文件的退路，等于发版链上 skill 里的回退策略与产物清单它看不到。同组合只此一处（另一个设了该字段的 branch-finisher 没有 agent 预加载它）。修法限定为不动流程的那种（例如 deployer.md 正文加一句先读 .claude/skills/release-builder/SKILL.md），待用户拍板。修之前先派一次只读探针核实机行为。**已完成（2026-09-22）**：实机探针坐实——派一个 deployer 子 agent 只凭上下文自省（零工具调用）作答：release-builder 正文不在上下文、它能看到的 skill 清单（完整抄录）里没有 release-builder、`disable-model-invocation` 字样只出现在项目 CLAUDE.md、自己的 agent 定义在。修法按「不动流程」：`agents/deployer.md` [任务] 段加三行，动手前先完整读 `.claude/skills/release-builder/SKILL.md` 并写明原因与探针日期；frontmatter 的 `skills: release-builder` 保留（删家底待用户拍板，留着无害）。同组合只此一处，branch-finisher 没有 agent 预加载。
- [P2][DONE][#68] **`occurrences` 字段是死字段**（2026-09-12，本日 feedback 体检发现）：43 条里 39 条恒为 1，含明说「以后还会踩」的 git-diff 那条，没有任何数据能回答「哪条教训真在复发」，而复发是判一条规则该不该升级成机器闸的唯一客观依据。两条路选一：让 feedback-writer 遇同主题时加 1 而不是新开文件，或把该字段删掉。→ 2026-09-12 用户拍板「删了」，字段已从 43 条 feedback + 话题模板共 44 个文件删除，连带把挂在它上面的三条阈值判据换掉。
- [P1][DONE][#67] **采集闸（`subagent-acceptance-reminder.mjs`）按新方向改造 + manifest 重生成**（2026-09-11，领域口径库纠正批的收尾动作）：**已完成**（2026-09-18 核验）：hook 现为「回执真有领域发现才追加取证句」、判据 `!/^(none|n\/a)\b|^无(?![一-鿿])/i` 与文案第二人称写给子 Agent，均与 09-11 三条 Decisions 一致；`test-hooks-node.sh` PASS=42 FAIL=0，其中 SA 段七条覆盖普通写法、以「无」起头的真发现、裸 None、加粗栏名 + 中文括注、英文括注、中文「无」+ 括注；HEAD 上 `doctor.sh` 清单比对 0 条不符，manifest 已随后续提交重生。原文——hook 本体还是旧方向「回执缺 Domain findings 栏就点名」，需改为「回执里确实有领域发现时才提醒主 Agent 判一句要不要收」（见上方 Decisions 同日「采集闸方向反转」条）；tester 的红锁与实现者的修绿尚在进行，验证完成前不算数；另本批新增/改动的 `.claude/rules/domain-rulings.md`、`subagent-dispatch.md`、`agents/domain-recorder.md`、`skills/red-blue-review/SKILL.md`、`skills/domain-rulings/SKILL.md` 等文件尚未同批重生成 manifest。
- [P2][OPEN][#66] **地板闸 dangerous-pkill-guard 只认一种写法，防的事防不住**（2026-09-11 升级下游时冒烟发现，暂不动）：它为「防止杀掉正在跑的活儿」而建、来自真事故，但正则只认 `-f` 紧跟命令名。实测 `pkill -f node` rc 2 拦住；`pkill -9 -f node`（最常见写法，杀进程一般都带信号）、`pkill --signal 9 -f node`、`pkill -if node`、`killall -9 node` 四种全 rc 0 放行。修的范围只到「自然会打出来的写法」，不追 `e=.en; cat .${e}v` 这类构造——文本匹配防的是事故不是对手。连带项（同批可做可不做）：secret-exfil-guard 漏 `base64 .env` / `tar czf - .env` 两种自然写法（九种探测拦住五种）；地板测试 test-hooks-floor.sh 213 行六条用例全用同一字面命令，测的是闸的契约不是威胁的面——若修，顺手在 test-builder 写一句「地板闸按自然写法的面测，非地板闸按一条字面测」
- [P1][DONE][#61] **接线与收口**（2026-09-09；09-10 07:00 收口完成——manifest 重生 138 文件、全量 run-all rc 0（selftest 308 / golden 8361 一致 / 二十余套静态回归全绿 / 两个真触发 case 通过 / test-ui-audit 按约定 SKIPPED 记注记）、分支提交并推送，合并由用户拍板；历史「已做 / 未做」：dev-workflow-details 五处、file-structure、CLAUDE.md 四行、dev-builder / code-review / dev-planner 参照顺序、run-all 挂两测试并把 rc 3 记 SKIPPED；未做：第三批红锁 → 实现者修绿 → 复审 → golden 重录 → manifest 重生 → 提交推送）：dev-workflow-details 设计阶段改两产物 + 签字闸跑 predev-lint；file-structure 加 DESIGN.md；CLAUDE.md skill 行只做最小改动（design-brief-builder 触发词、文件结构一行）；dev-builder / code-review / dev-planner 的视觉参照顺序补 DESIGN.md 与 ui-quality-floor；gen-manifest 重生；skills-lint / test-routing / selftest / golden `--check --strict` / test-predev-lint / test-ui-audit 全过；派 progress-recorder 落账；分支提交推送，合并等用户。
- [P2][DONE][#62] **范例与模板残留**（2026-09-09，09-09 深夜收口：范例三处裸「待定」改口径、changelog 模板补「分类与回退」、DESIGN 范例与模板正文只引用原始值记号、motion 改内联数值、Brief 范例「Q-2 未定」）：after-sales-dispatch.md 第 41 行「见待定表」与第 143 行「待定表 Q-2」改口径；changelog-template 补「分类与回退」行（上次 cwd 错没打上）；新范例跑 spec-lint 与 predev-lint 双绿后才算完。
- [P2][DONE][#63] **feedback-writer SKILL.md 两处模板路径写成相对路径**（2026-09-10 开，当日收口）：原判「模板不存在」有误——两份模板一直在 `.claude/feedback/templates/` 下，是写入流程第 1、4 步写成 `templates/xxx.md`，会被当成 skill 目录下的路径；[文件规范] 段的两行本来就是全路径。改法：两处补全为 `.claude/feedback/templates/...`，不新增文件、不删引用。
- [P1][DONE][#64] **全仓测试减重 + 分级 + 老化**（2026-09-10）：按 Decisions 的预算表把测试砍到有效代码的 1/3～1/2（目标约 6800 行）；每套留下的测试打 `# risk:`；run-all 加 `--level` 与用例账本；新脚本 `scripts/test-age.mjs`（≤ 80 行）出退休候选；分两三批派实现者，每批砍完跑一次 run-all --level all 验绿；11:35 起三个实现者并行：A 机制（run-all --level、账本、test-age）、B 引擎 / 审计 / 发布测试、C hooks / 档位 / 安装器 / 小套并打级
- [P3][OPEN][#55] **`scripts/gate-audit.sh`（读 hook 侧 `gate-block.log`）与引擎 `gate-audit`（读 `harness/state/ledger.jsonl`）同名异账本**（2026-09-06 B3 盘点）：合并 = 引擎 `gate-audit` 也读 `gate-block.log` 并按 settings 的 `args[0]` 列注册闸，报「零记录死闸」；脚本退役、run-all / test-gate-audit / golden 改口径。约 40 行引擎 + 红锁一条，v2.0.x 做。
- [P3][OPEN][#54] **`tier validate` 对「合法 JSON 但非对象」的 profile 报 `UNREADABLE`（文案「could not be read as JSON」与事实不符）**（2026-09-06 P1-1 复核带出）：`hooks/lib/tier.mjs` 的 `loadProfile` 把非对象也走 corrupt 分支，引擎 `lib/tier.mjs:412` 统一报 UNREADABLE；同文件 `NOT_AN_OBJECT` 码在文件读取路径上取不到。行为仍 fail-closed rc 1，只是可读性。修法：`loadProfile` 返回 `corrupt: 'not-object'|'bad-json'`，validate 据此分码。
- [P3][OPEN][#52] **pre-push 钩子拦停理由不落盘**（2026-09-05 推 v1.14.0 tag 时暴露）：`.claude/githooks/pre-push` 的判定只写 stderr，调用方一截短输出理由就没了；`gate-block.log` 只收 hook 侧闸，git hook 侧不记。补法：pre-push / pre-commit 的最终 VERDICT 与 WHY 追加进 `.claude/evidence/gate-block.log`（照 `lib-gate-log.sh` 的行式格式），`gate-audit` 顺带能统计 git hook 拦了几次。另：pre-push 对 tag 推送也跑全量 run-all，与 09-04「本地不跑 run-all」裁定冲突，是否默认 `CCBASE_PREPUSH_FULL=0` 待用户定。
- [P3][OPEN][#29] **`evidenceSha256` 掩码在 npm 走后可能已多余**（2026-09-03 由第三层修复的实现者上报，未派）：该掩码当初存在是因为 npm 输出里带 `.npm/_logs/<iso>-debug-0.log` 这类随机内容；夹具改用 `false` / `sh -c 'exit N'` 后**所有夹具 check 的输出都确定了**，理论上可以取消掩码、把断言收紧一格。未做的原因是取消会连带改 `catalog-rich` / `catalog-pass`，超出当时「差异只落在 catalog-good」的边界（注释已如实改成描述现状）。**这是一处可以变严的闸，值得排期**。
- [P3][明确不做][#30] **pwsh 5.1 仍未验**：CI 的 windows runner 只有 **pwsh 7**，而真实用户环境跑的是 **powershell.exe 5.1**（`setup.ps1` 注释明写）。5.1 的已知坑（GBK 读无 BOM UTF-8、`$ErrorActionPreference='Stop'` 下 native 命令 stderr 变终止错误、`Join-Path` 三参形态要 PS6+ 的 `-AdditionalChildPath`——最后这条在写 `test-ps1-behavior.ps1` 时**真的踩到并当场改掉**）都不在 CI 覆盖内。**「pwsh 7 跑绿 ≠ 5.1 安全」已写进脚本头 / README / CI 注释三处**，但没有任何机器闸能验它——本条是承认这个缺口存在，不是待办某个具体修复。　**合账**（2026-09-05，用户当面确认「我不会使用 powershell 5.1」）：这条记的是「5.1 无机器可验」，而目标环境根本不跑 5.1——不存在的缺口不必挂着。**注意不要连带放开纯 ASCII**：用户同日明确「永远不要在 PowerShell 里写中文注释，后面要考虑兼容性」——那条 Pinned 保持铁律，理由从「5.1 按 GBK 读会崩」扩成「兼容性面还会变，不赌当前环境」。`#requires -Version 7.0` 那条路（deepseek-base 走的）本仓仍不走。
- [P3][OPEN][#34] **`resolveThroughLinks` 对不可达 UNC 路径逐级 realpath 会各吃一次 SMB 超时**（reviewer S2-5，2026-09-04）：`\\server\share\x` 整条不存在时循环对每级各打一次 `realpathSync`，不是死循环（`path.win32.dirname` 不动点实测收敛）只是延迟；零实例零覆盖，记着。
- [P3][OPEN][#36] **`repoRelative` 混合拼法仍回显绝对路径**（复审 2 R-1，2026-09-04）：根用软链名（`CLAUDE_PROJECT_DIR=<W>/lnk`）而入参用真实拼法且途经仓内指向仓外的软链时，拼法问与身份问都判仓外、回显机器绝对路径；已证明非本批回归（换回原始实现答案逐字节相同），任何按拼法的规则都躲不开根与入参拼法不同源。记着，不建议改逻辑。
- [P3][OPEN][#44] **`--mutate` 被 SIGTERM 时的还原待证**：reviewer 观察到 SIGKILL 后突变留盘、SIGTERM 被 `--check` 子进程吞满 120s，中间因果链未证（NEEDS_MORE_EVIDENCE）；另 shellcheck -S warning 对本轮改动的 .sh 报 1 error + 4 warning（static-check 的档位没抓到）。
- [P3][OPEN][#50] **authorship 记账吃 fast-mode 豁免，会在台账上开洞**（2026-09-04）：`record-authorship.sh` 照其它 hook 的惯例在 fast-mode 开着时静默退出，那期间的编辑不入账，之后评审落进 `unattributed`。引擎会诚实说出来，但记账不是闸、本可以不吃这个豁免——语义上该不该豁免待定。同批：`review.mjs:1110-1115` 的 docstring 仍写着「hook wiring 有意不属于这一层」，已过期。
- [P3][OPEN][#51] **`applyWaiver` 成了只有测试在调的死代码**（2026-09-05 由 #20b 带出）：事后改写路径被事前声明取代后，生产代码里已无调用点，只剩 `selftest.mjs` 6 条 lane 在调它。留着是把枪（下一个人可能以为它还是活的路径），删要连那 6 条 lane 同批删否则 import 崩。
- [P3][OPEN][#48] **⑧-7 因锁改原子而弃权，确定性闸失效**（2026-09-04）：该用例往「判存在与写锁之间」注入 sleep，#41 修完那个窗口不存在了，于是它打印「本轮没验到，注入点不在了」并 PASS——闸还在但不守任何东西，原子性只剩 ⑧-8 的概率信号在守。重锚配方 implementer 已跑通（`/tmp/lock-fix.log`）：锚 `if (set -o noclobber;` 那行、在其前插会合点，旧两步式 12 轮 10 次违规、新实现 12 轮全对。另：锚点找不到时应 fail 而非弃权 PASS。
- [P3][OPEN][#46] **`worktrees` 进了 DENY 但那一面零测试**（2026-09-04）：批 6 把 `.claude/worktrees/` 加进 `core.mjs` 的 DENY 正则，六份表里其余五份都有行为锁，只有 DENY（context-pack 拒绝面）没有——删掉那段正则没有任何测试会红。
- [P3][OPEN][#19f] **`instructions-allowlist.json` 的分发语义**：它是项目私有配置（cc-base 自己那条绑 `.claude/settings.json:5` 的 sha256，装到别的项目里对不上会变成 `allowlistUnused` 噪声），应按空模板分发，与 `feedback/*.md` 走私有层同理。同类：`.claude/tests/golden/harness/*.json` 是 cc-base 自己的基线，多数场景做了隔离沙箱与宿主仓无关，但 `adapters list` 断言的是本仓 adapters.json。两条都进 P6 分发层。
- [P2][OPEN][#26] **用户报「一跑就报错」但未复现（信息不足挂起）**：2026-09-02 用户称某功能坏了一跑就报错，未指明功能、未给命令、未贴 stack trace。主 Agent 在本仓做只读全面扫描**一处未复现**（selftest 152 / golden 10320 / doctor / 22 子命令冒烟全绿，证据见当日 Done）。已回问三项：哪个功能（本仓 harness？某 hook？某 skill？还是 `project-mcp` 等别的项目）/ 怎么触发的原始命令 / 完整报错原文。**用户补齐前不动手**——缺这三样修出来的只能是运气。注意报错也可能压根不在 cc-base 仓内。**2026-09-02 二次尝试（用户原话真实再发一次，与当日 Notes 那条 sub-agent 编造叙事措辞几乎相同，recap 时勿混——那条是虚构的，这条是用户真输入的）**：主 Agent 独立复跑仍**一处未复现**——`doctor.sh` 全绿、静态自测批 11 项全 PASS（selftest + test-setup/routing/fix-platform/hook-parity/gate-audit/three-file-sync-gate/fast-mode/supervisor + test-harness + `harness-golden.mjs --check --strict`）。唯一新线索是当日 Notes 的 `trace` 条（文档承诺了未实现的子命令，**会真报错**，但无法确认就是用户所指）。三项回问原样待答。
- [P1][部分完成][#20b] **P1 未交付的三项（有意拆出，非漏做——checked by code-reviewer 提醒后补记）**：① **waiver 三重收紧**（跑过并 FAIL 的永不可豁免 / waiver 绑 diffHash / critical 认领的 check 申请即拒 / 零认领属性缺口不可被遮）——拆出是因为它**改既有 `verify` 行为**，混进纯新增批会让 golden 重录的差异不可解释，而「差异只有预期那处」是重录纪律唯一站得住的判据；② **`PreToolUse` Scope 越界拦**（按活跃 task 的 Scope 拦写入，dsh 矩阵 #39 判 Rejected 因它没 hook）；③ **`TaskCompleted` 证据闸**（没有 PASS 门 + 新鲜回执不许标完成）。②③ 属 hook 接线层，与引擎侧分批。三项均见总纲 §四 P→M 转换表与 §七 P1 行。　**核心已完成**（2026-09-05）：waiver 从「事后把跑过的 FAIL 改写成 SKIPPED」改为「事前解析计划、命中即不执行」，执行过的结果不可变；受保护类（security/safety/privacy）命中 waiver 记 `waiversBlocked` 并照常执行。**另两项仍不做**：`PreToolUse` Scope 越界拦、`TaskCompleted` 证据闸——只在大仓模式下有用，本仓没开 catalog。
- [P3][OPEN][#10] headless skill 路由行为烟囱测试（借鉴 grok-base test-skill-behavior.sh 模式）：opt-in env 开关默认 skip，`claude -p` 真跑提示词 grep 验路由（如 bug 描述应进 bug-fixer）——补 test-routing 静态检查测不到的真实行为面；注意 token 成本。
（归档指针：累计搬出 51 条，最近一次 2026-09-20 搬 51 条；原文见 progress.archive.md「Archived TODO」，已归档最大编号 #60）

## 明确不做（防过度工程）
- **condenser LLM 摘要压缩**：progress.md「超100条归档+摘要指针」已够用，不值得为它每次多跑一次 LLM。
- **trajectory 存储/回放、Action-Observation 事件流结构化**：引擎级数据结构，markdown 框架硬套自找麻烦；已有 progress.md+外部memory+claude-mem 三层。
- **反馈→改进闭环**（参照 OpenHands enterprise/storage/feedback.py）：cc-base evolution-engine 反而领先——OpenHands feedback 表只存 polarity+trajectory，无聚合分析无改进驱动；不需要对标。
- **自建 agent benchmark**（参照 OpenHands SWEBench 77.6）：成本极高，现阶段不做，记「将来事」。

## Notes
- 2026-09-22: **#76 审查结果（sonnet，opus 两次 API 529/500 零工具调用后改派；用户「降低模型也没事」）：修 HIGH 后合。** HIGH＝命令替换当动词绕过（`$(which tac) .env` / 反引号形态，探针 rc 0）；Medium 两条＝glob 绕过（`tac .en*` / `paste .??v` / `base64 .ssh/*` rc 0，而 `cp *.pem /tmp` 因 `\S*\.pem` 正则副作用 rc 2、命中不一致）、嵌套引号扫描把「引号里提到文件名的纯文本」当参数命中（过度拦截）；需求存疑一条＝git 元数据查询（`git ls-files .env` / `git check-ignore .env` / `git status | grep .env`）与 `cat .env` 一视同仁被拦（旧规则里 grep 那条本来也拦）；Low 两条＝R3 管道规则只认单跳直连（`env | tee x | curl` 放行，预存在、非本轮回归）、`stripWrappers` 剥尾引号造成形态不配对（实测未漏判）。implementer 四条假设与三条反例（ssh-keygen / openssl 生成密钥、git diff .env 被拦）reviewer 均判在契约意图内、不改。主 Agent 裁决：HIGH 与 glob 一起在本轮修（地板闸、用户要求设计完备），同一 tester 先补红（SE-40 起），implementer 修，同一 reviewer 复核一次收口；嵌套引号过度拦截与 git 元数据误拦记 TODO 随后续顺手修。
- 2026-09-22: **LOW 档派单实际超预算四倍：#77 的 implementer 用了 24 次工具调用、106k token、72 分钟，单子写明 ≤ 6 次**——改动本身只有 43 行、一次到位、范围没外溢，超的部分花在反复只读核对与「不能亲跑 run-all」的犹豫上（它把「只读方式验证」理解成了每一处都要 grep 重跑）。下次 LOW 单子把验证命令写死成两条、并写明「跑完这两条就交回执，别再做别的核对」；连着两次超预算就要看是不是 agent 定义里的自检要求与 LOW 档预算打架。
- 2026-09-20: 残留（收口 code-reviewer 对 #73 / #75，全为 LOW，均安全、未修）：`progress-archive.mjs:42` `--root` 后紧跟另一个开关时把开关当目录值吞掉，不报用法错（实测 `--root --check` → root 成了 `--check`，该目录下没有 progress.md，rc 0 退出，不会误写）；`:108` progress.md 是目录时 `readFileSync` 不在 try 里，打 node 栈而不是说清哪一步（rc 1，正文无从谈起）；`updateLastUpdated` 只扫前 10 行，时间戳行落在第 11 行以后就静默不更新（本仓在第 3 / 第 2 行）。需求存疑一条已随修复收掉：核对口径原为「整行出现在归档任意位置」，宽于写入口径，现与判重共用同一判据、限定在对应段内（仍按行集合判，不要求整条连续）。
- 2026-09-19: 残留（收口 code-reviewer，无 HIGH；M1、M2 已挂 #73、#74）：**M3** 普通升级（不带 `--with-harness`）不刷新 ext/ 与 rules/ 两份副本，却会刷新白名单，将来哪一版让被豁免那一行挪了位置，下游就被拦——reviewer 造了个 v2（插一行、两条条目按第 64 行重签）实测两条都 ERR；ext 条目同样中招，是老问题；`doctor.sh:206-223` 只做单向比对，看不见这种错位。**M4** 提交耦合，见当前断点。**L1** `test-distribution.sh:250` D-7h2 的标签「防两边一起坏掉的假绿」写反了：两边一起坏时它是绿的，兜底的其实是 h1，它真正防的是「一边修了另一边没修」，只需改注释。**L4** 下游没装 tests/ 时，装出的 `rules/harness-large-repo.md:45`、`:180` 引用的测试文件不存在，`rules-audit` 报幻影；该命令没接进任何闸，只影响信息展示。**L5** 孤儿与陈旧的白名单条目只产 note、不进 ok / rc，设计如此。**L6** `static-check.mjs:13` 的 shellcheck 只覆盖全仓 46 个 .sh 里的 2 个，老问题；本轮新增的 shell 已手动补跑，rc 0。
- 2026-09-19: **stop-gate「三振带欠账放行」实现正确，别去改**：主 Agent 一度看到 `.stop-gate-strikes` 从 `count=3` 跳回 `count=1`，以为放行没生效；查 `gate-block.log` 证伪——09:06:20 确实按上限放行，随后新一轮从 1 起算（`hooks/stop-gate.mjs:131-138`）。计数器读数跳回 1 是正常行为，不是缺陷。
- 2026-09-15: 残留（LOW，code-reviewer 复核）：`test-audit-scripts.sh` 第 122 行造的 `.env.example` 到第 139 行才清，中间的 NUL 用例带着它跑，白名单一坏两条同红且红因指向别人的夹具；改法是把清理提到 NUL 用例之前。code-reviewer agent memory 49.3KB，距 50KB 上限只剩约 1.9KB，下一条该搬 docs/agent-notes 不是抬阈值。
- 2026-09-12: **主 Agent 两次把 githook 与 Claude Code hook 搞混**：口头断言「今天每次 commit 都打印了 pre-commit-check 通过，所以它活着」，实际那行输出来自 git 的 `.claude/githooks/pre-commit`，与 `.claude/hooks/pre-commit-check.mjs` 是两个东西；对后者是否跑过其实零证据。教训：同名不同体的两套 hook，引证据时必须点明是哪一套。
- 2026-09-12: **派单重到撞上限，是派单人的错不是执行者的错，今天犯了三次**：给 evolution-runner 的单要 6 条 feedback 各做 4 项核查（65 轮撞 30 轮上限）；给你的落账单一次塞 3 条 Decisions + 3 条 Done + 2 条 Notes + 1 条 TODO（两次撞 25 轮上限）；归档那单也撞过一次。本仓自己的规矩是「每单 ≤ 6 次工具调用，单次预期 >60min 说明任务分解不合理」——违的是我自己定的规矩。正解是一条一单或分两批，不是让它长跑。
- 2026-09-11: **归档丢过东西，靠逐行对拍捞回来**：第一轮归档（撞 25 轮上限中断）丢了 3 条 Done 条目——从 progress.md 删了却没写进 archive（`v2 P0.1 收口并入库` / `v2 P0.1 golden 基线锁建成并证会响` / `审计脚本层建成 + 10 条缺陷红锁到位`），另把 `codewhale-base 已勘察` 那条的日期戳从 2026-09-01 擅自改成 2026-08-20。两者都是用 `git show a38f8e2:progress.md` 与当前两份文件做**全行级对拍**查出来的，已原文补回、日期改回；现在对拍只剩时间戳与被替换的指针两行差异。教训：归档这类「删一处写一处」的搬迁必须先写目标再删源，且完事要做一次全行对拍，不能只看回执自述的条数；主 Agent 当时凭文件 mtime 判「两个文件都还没被写」也是错的——那份快照已过期，mtime 不能替代 git 对拍。
- 2026-09-11: **负向断言只盯新措辞会造假绿**：tester 第一版四条 None 断言全 PASS，实则现实现漏出来的是**旧**措辞的追加句，而断言只检查新措辞的标记，天生不命中 → 三条假绿，唯一线索是 GOT 里孤零零一个「另」字。修法是加一个跨两版措辞都成立的泄漏探针（`领域发现`）与新标记合取。凡改文案类契约，断言要同时钉「新的必须出现」和「旧的不许出现」。
- 2026-09-11: **新通道的第一条发现,以及它暴露的路由空缺**。domain-recorder 实现者通过新增的 Domain findings 栏报回一条,我已独立复现:对本 session 新建、尚未入库的文件跑 `git diff`,得到空输出 + rc 0,与「文件没被改」完全无法区分。我今天多个派单里写过「贴 diff 证明只动了这几个文件」,对新建文件那是个看起来像通过的空结果。**裁定不进 domain/**——它是框架验证手段的缺陷,不是某个业务领域里事情怎么算,按定位该走 feedback 或直接改派单实践。派单实践即刻改:新建文件的改动证明用 `wc -l` + 内容锚点 + `git status` 确认 `??`,不用 `git diff`。同时暴露 rules/domain-rulings.md 的一个空缺——该通道会收到三类东西(领域口径 / 框架实践问题 / 需求存疑),规则只写了第一类怎么办,路由规则待补
- 2026-09-11: 口径库派单里我引的 feedback 数字有误，实现者当场实测纠正：不是「38 条里 13 条没被引用」，而是 **38 条里 20 条**没被 CLAUDE.md / rules / skills / agents 按文件名引用过（其中 graduated:false 的 7 条、graduated:true 但也没被引用的 13 条）。我把两个不同的量混成了一个。规则文件里已按实测值写并注明了计法与日期
- 2026-09-10: **地板闸 dangerous-pkill-guard 有四种绕过形态**（升级冒烟时顺带发现，cc-base 与下游同一份 md5，是上游本来就漏、不是升级弄坏）：正则只认 `-f` 紧跟命令名的写法。实测 `pkill -f node` rc 2 拦住；`pkill -9 -f node`、`pkill --signal 9 -f node`、`pkill -if node`、`killall -9 node` 四种全 rc 0 放行。这个闸是任何档位都关不掉的地板、由真事故（误杀主 Agent 自身进程）换来，待修。附带发现：该闸对命令文本做匹配，分不清代码与叙述——我写这条记录时被自己拦了一次
- 2026-09-10: 第一批审查抓出三条 HIGH，全部是我最忌讳的两类，主 Agent 逐条独立复现后修掉：① ui-slop-scan 的正文判据对整条选择器做词匹配，`.prose h2` / `article h1` / `.card-body` / `.copy-btn` 全被当正文——放真实项目上会刷屏，改成只看 CSS 真正作用的最后一段；② predev-lint 的重试列写成「3（指数退避）」解析不出就静默当 0，两层重试直接放行，而写「2 次」才报——同样业务事实换个写法就漏，改成三态并新增 RETRY_UNREADABLE 出声；③ plan-lint 的覆盖判据取「计划全文出现过」，于是「已知风险：REQ-CORE-002 本期不做」被算成有人做，而失败消息与模板规则写的都是「Task 里引用」——三处口径不一致、实现最宽，收窄到只认 Task 条目行。三条修完各自带对照组验证规则没被删掉
- 2026-09-10: 第一批过程中的四次纠偏，三次是子 Agent 顶回我：① 我断言 `body{line-height:1.2}` 不该报 error，实现者拿夹具指出 body 就是正文选择器、我错了；② 我把重试规则写成「必须恰好一层」，实现者报反例——正当的零重试链路（人工重提交 + 幂等键）会被逼编造假重试层，而我们自己的范例就是那种设计；③ 我说裸「以后」该判空话，实测实现只收「以后再」，保守是对的；④ 我自己的验证夹具坏了（把 awk 输出的对齐空格当成文件内容，替换没落到实处），六个例子全绿、与实现者回执对不上，查下去错在我——**对不上的时候先查夹具，别先怀疑对方**
- 2026-09-10: 前期能力调研四路（需求 / 架构 / 前端 / 效率容错），源头是 spec-kit 模板与 /clarify /analyze、Shape Up、Story Map、Amazon Internal FAQ、OpenSpec、ATAM/ALMA、DSM propagation cost、MADR v4 与 log4brains、dependency-cruiser、arc42、v0 与 Lovable 原始系统提示词、OpenAI frontend guide、DTCG 2025.10、axe-core、Material color utilities、Lighthouse budget、web.dev、Google SRE Workbook、Azure WAF RE:03、IETF 幂等键 draft-07；计划落在 docs/predev-upgrade-plan-2026-09.md。核清了三件事：① design-maker 依赖的外部 CLI odc 与 ui-audit 需要的 playwright-core 本机都没有，界面产稿链当前是断的；② predev-lint 只查单文档内部结构，跨文档一致性无闸；③ 我们的 ui-quality-floor 已经覆盖 WCAG 2.2 最常漏的三条（命中区 24px、焦点不被 sticky 遮挡、拖拽有单指替代），style-vocabulary 已点名奶油底与紫色渐变那套——前端真正缺的是把定性清单变成数字与一条纯静态扫描（不需浏览器引擎，正好绕开断链）
- 2026-09-10: 同类框架横向调研（用户「去 Github 上找 10 个类似的框架，学习学习」）：11 个仓，四个 agent 并行读真文件不读 README，报告 docs/github-frameworks-2026-09.md。三个流派——外挂编排型（Ruflo 71.9k / Gas Town 18k / Multiclaude 566）、纯提示词型（SuperClaude 23.9k / Agent OS 5.4k）、纪律层型（cc-sessions 1.6k 半死 / barkain 84）；另三个堆量型（ECC 25.5 万 star、toolkit 2.6k 停更、showcase 10k）在同一处塌方：没 manifest、没覆盖保护、没淘汰机制，toolkit 自称的数字与实际 tree 对不上、装完 25 个 hook 一个不生效。没有一个值得整体照搬，挑出 11 个机制；被独立验证的有「模型推理 / harness 强制」的分工、隔离的是对话不是文件系统权限、策略要带依据与管辖边界、运行态不进 git。star 数与 push 日期均用带认证的 GitHub API 当场复核
- 2026-09-10: 进化闭环体检（命令核实，处置待定）：① `.claude/feedback/` 38 份，25 份 graduated:true（其中 18 份被 CLAUDE.md / rules / skills / agents 按文件名点名）、13 份 false 且**一份都没被正文引用**；② occurrences 分布 34 份 =1、3 份 =2、1 份 =3，按 evolution-engine SKILL.md 第一步的「occurrences ≥ 3 且未毕业」筛，**候选恒为 0**，这条毕业闸从来没响过；③ EVOLUTION.md 第 15 行写的是「同族跨文件 3+ 次按模式聚类计数」，SKILL.md 第 22 行只按单文件计数——文档与实现口径不一致，聚类那条从没实现；④ 38 份里只有 5 份填了 scores，第三层「评分持续偏低」无数据可判；⑤ 无 `.claude/evolution/` 目录（signals.jsonl / proposals.md 是 5.0 的设计，我们没有），历史上 graduated 翻转都夹在普通重构提交里手工改的，不是 runner 提议走的；⑥ check-evolution.mjs 每次 SessionStart 报「N 条待处理、建议派 evolution-runner」，它数的是索引里没打「已毕业」的行，与 runner 自己的判据不是一个口径——于是每次开工都提示派一个必然回「无进化建议」的 agent
- 2026-09-10: gate-audit 战绩（命令核实）：有记录 stop-gate 38 / three-file-sync 26 / tdd-gate 19 / secret-exfil 16 / tier 8 / dangerous-pkill 7 / precompact 2 / release-gate 1；零记录三个——harness-async-verify、no-direct-code-guard、pre-commit-check。注意零记录 ≠ 死闸：后两个是「没拦过是因为一直合规」的威慑型，不能照「闸靠数据留」直接退休。另：(a) 表里 `tier` 不是注册 hook 却占一行，导致「注册 10 / 有记录 7」与表格 8 行对不上，P3 显示口径问题
- 2026-09-10: 开发阶段改造批 2 / 批 3 交付并亲核（16:40）：bug-fixer 170 → 49、code-review 220 → 56、test-builder 136 → 51、scaffold 98 → 51、release-builder 230 → 57；四个 agents 各 ≤ 35（角色封顶结论词进 Non-goals）、subagent-dispatch 51 → 31、dev-workflow-details 开发 / 发布 / 修订三段约 90 → 14、CLAUDE.md [Sub-Agent 调度规则] 与 [开发测试规则] 只留铁律与指针；十五文件合计 2667 → 1126 行（skill + agents + dispatch 一组 2150 → 684）。裁决：调度表删 Allowed Skills 列（agents frontmatter 的 skills 字段才是真绑定）；code-review 留 Playwright 一句；主控两段没压到一半（七行表格 + 六条铁律是硬下限）。主 Agent 顺手对齐三处口径：test-builder 引 [隔离原则]、red-lock 补「契约」、stop-gate 提示语「两阶段审查」→「审一轮」。闸：skills-lint 0 / test-routing 过 / rules-audit phantom 0 / run-all high rc 0（53s）。行为验收（16:55）：fresh 实例照新 dev-planner 拿售后派单范例首轮写出 DEV-PLAN 即过 plan-lint rc 0（5 Phase / 27 Task，[推断] / [待定] 全落到 Phase 假设）；它报的 12 条「靠猜」里 7 条一句话堵上（小项目粒度 3-5、[默认] 也算假设、Spec 矛盾走需求存疑回流、不能提问时按「不答先按什么做」并记开工前置段、联网预算先核框架与数据层、首骨架 Phase 可超粒度上限、缺 DFX 时非功能 P0 折进验收），模板加「开工前置」段（无内容整段删）；不接的 5 条：Spec 与 DEV-PLAN 的 Phase 编号重映射、项目目录命名、假设 bullet 机器闸、plan-lint 加自检闸（不为 skill 文档写机器闸除非能说出挡什么）、粒度与地基冲突再细化
- 2026-09-10: 开发阶段改造批 1 交付并亲核（16:35）：dev-planner 314 → 84、模板 220 → 77、dev-builder 476 → 91；skills-lint 零发现、test-routing 过；实现者抓到旧模板照抄出的 DEV-PLAN 过不了 plan-lint（模板从没给「Task 清单」栏）→ 模板补该栏；拍板：充足度六条保留、Task 清单保留；主 Agent 补一句「编码由主 Agent 派 implementer、只写单验收」点明底线。批 2（四个 skill + scaffold）与批 3（agents / dispatch / workflow 段 / CLAUDE.md 两段）文件不重叠，并行派出
- 2026-09-10: 第十二批红锁主 Agent 亲验（09:00）：test-ui-audit 24/1+U5 跳过，U22 恰红 fitness 两条空 catch（第 48 / 73 行），指纹与提交 0d664dc 一致；tester 提醒修复时包解析原因用独立变量、不覆盖 U6 断言的 launchError；已派一单修绿。tester 另提两点记为议题：① fitness 只扫已跟踪文件，新脚本要到提交后才被 dod 抓到——是否让 run-all / pre-commit 层对新增未跟踪的 .mjs 也跑一次 fitness 归 v2.0.x；② 调外部检查器当判据必须同时断言「确实扫到了东西」（U22 首版 scannedFiles=0 假绿）
- 2026-09-10: 收口时 pre-push 钩子阻断（07:20）：钩子内嵌的全量回归在 dod 装配的 `fitness --all` 报 rc 2——`no-silent-failure` 规则抓到 ui-audit.mjs 第 48 / 73 行两个空 `catch {}`；fitness 只扫已跟踪文件，所以提交前的 run-all 全绿、提交后才红。本地重跑 run-all 复现一致。处置：第十二批红锁（U22 fitness 零命中）→ 一单修绿 → 重新全量 run-all → 追加提交并推送；提交 0d664dc 保留，不 amend
- 2026-09-10: 复审第十轮回执（fresh reviewer，只审 ui-audit，06:55）：**PASS**——Stage 1 / Stage 2 全过，20 组自造攻击输入无 Medium 以上；三条 Low 记为残留不改：末位 `--out` 缺值时预扫回退到前一个 `--out` 而解析通道回退默认目录、POSIX `--` 终止符不在预扫词汇表、`--json` 在非 0 出口 stdout 为空（机器消费须看 rc；`--json > file` 会截空）；一条测试存疑（U18b 只断言 rc、默认目录作废半边由 reviewer 沙箱补验正确）。review → fix 闭环到此收口：十一批红锁、十轮复审
- 2026-09-10: 第十一批亲验绿（06:45）：ui-audit 24/0+U5 跳过，predev 66/66、selftest 308 不变，无诱饵目录泄漏；已派第十轮 fresh code-reviewer 只审 ui-audit
- 2026-09-10: 第十一批红锁主 Agent 亲验（06:35）：test-ui-audit 22/2+U5 跳过（U20 单横线值、U21 等号写法两通道同判），predev 66/66 与 selftest 308 不受 iso_run 改 cwd 影响；tester 实测本机 node 24 parseArgs 对 `--out --strict` 抛歧义、`--out=--strict` 解析成功，写进 U19 注释；已派一单修绿
- 2026-09-10: 复审第九轮回执（fresh reviewer，06:30）：predev-lint 与 spec.mjs 零发现；三条 Medium 全在 ui-audit 参数边角——`--out -x` 单横线值被当目录、`--out=--strict` 在解析失败 / 成功两通道作废不同目录、`flagOut` 分支在本机 node 是死代码（parseArgs 对 `--out --strict` 抛歧义）且零覆盖。处置：第十一批红锁（只 ui-audit）→ 一单修绿 → 第十轮复审只审 ui-audit；若再无核心行为的 Medium 以上即收口
- 2026-09-10: 第十批全部亲验绿（06:15）：predev-lint 66/66、ui-audit 22/0+U5 跳过、selftest 308、范例 dogfood 过；golden 按 lane 308 第七次重录并严格对照；已派第九轮 fresh code-reviewer
- 2026-09-10: 第十批红锁主 Agent 亲验（06:05）：test-predev-lint 64/2（P54 围栏内反引号槽、P54b 两闸对拍）、test-ui-audit 21/1+U5 跳过（U19 `--out --strict` 缺值；U19b 防回归位绿）、selftest 308 全绿（新 lane 钉住 spec.mjs 现状）；tester 加 iso_run 让默认目录落临时目录、套件不写仓内文件；已派两单并行修绿，golden 按 lane 308 第七次重录中
- 2026-09-10: 复审第八轮回执（fresh reviewer，05:55）：第九批修在根因；FIX_REQUIRED 两条 Medium——围栏内 predev-lint 先剥行内反引号再扫槽（spec.mjs 不剥）导致四份非 Spec 文档漏报；`--out --strict` 把下一个 flag 吃成目录名、默认目录旧 pass:true 幸存；两套契约都没钉住围栏内反引号这条线（突变对调套件仍全绿）。处置：第十批红锁 → 两单修绿 → 第九轮复审
- 2026-09-10: 第九批全部亲验绿（05:40）：predev-lint 64/64（P53 两闸行号对拍绿）、selftest 307、ui-audit 20/0+U5 跳过、范例 dogfood 过；golden 按 lane 307 第六次重录中；已派第八轮 fresh code-reviewer
- 2026-09-10: 第九批红锁主 Agent 亲验（05:35）：test-predev-lint 60/4（P49b 期望按新口径改、P52 未闭合围栏、P53 围栏按用途扫槽含两闸对拍、P53b Brief 围栏示例）、test-ui-audit 18/2+U5 跳过（U18 末位 --out、U18b 空值）、selftest 307 恰红 2 条 lane，指纹与派单基线一致；tester 自修一处假绿洞（对拍在 spec 报空时被跳过→哨兵值）；已按文件拆三单并行派实现者
- 2026-09-10: 复审第七轮回执（fresh reviewer，05:25）：第八批七处突变全被抓、修在根因；FIX_REQUIRED 一条 Medium——未闭合围栏吞掉文件尾（合法范例末尾加一行 ``` 与四个占位 rc 0）；Low：`--out` 两次预扫首位胜 / 解析末位胜、`--out=` 空值落到 cwd、开栏行自带槽漏报、DESIGN 围栏内 Vue/Jinja 插值会假红（隐患）、小数宽度未验；需求存疑：围栏扫槽按文件名切不对，DESIGN 模板整份包在 ```markdown 围栏里、Brief 照抄就能藏槽。处置：第九批红锁 → 三单修绿 → 第八轮复审
- 2026-09-10: 第八批全部亲验绿（05:15）：predev-lint 60/60、范例 dogfood 过、DESIGN 模板原样当文档 79 条 PLACEHOLDER + 2 条 MISSING_TOKEN；ui-audit 18/0+U5 跳过；selftest 305；实现者顾虑「坏参误跑作废默认目录报告」沿用 04:40 拍板；已派第七轮 fresh code-reviewer，tester 顺手改准测试脚本旧注释
- 2026-09-10: 第八批红锁主 Agent 亲验（05:05）：test-predev-lint 57/3（P50 前言槽不双报 / P51 DESIGN 围栏内扫槽 / P51b 模板槽一个不漏）、test-ui-audit 15/3+U5 跳过（U16 未知参数 / U16b 缺值与 --out=X 预扫 / U17 半坏宽度主题 rc 2）、selftest 305 全绿（本批不动 spec.mjs）；八条跨批防回归位全绿；已派两单并行修绿
- 2026-09-10: 复审第六轮回执（fresh reviewer，04:55）：第七批修在根因（五模板槽数 75/48/51/3/0 与 findings 逐行对拍一致、两闸对称差为空、14 条槽语法注入两闸全同、5×4 残渣矩阵全同、10 条突变全抓）；FIX_REQUIRED 两条 Medium 都在 ui-audit 参数出口——未知参数 / `--widths` 缺值不作废旧报告、`1280,abc` 静默丢档照报 pass:true；四条 Low（双反引号代码段假红、`{{未闭合` 不报、前言 `{{px}}` 双报、崩溃出口旧 PNG 不动）；需求存疑：DESIGN.md 模板 76 处槽在围栏里而围栏不扫。处置：第八批红锁 → 两单修绿 → 第七轮复审
- 2026-09-10: 第七批全部亲验绿（04:40）：ui-audit 15/0+U5 跳过（U13–U15 三条出口作废旧报告）；实现者顾虑「无参误跑会作废默认目录下的真报告」拍板接受——报告是单次运行的证据，任何新调用都取代它，design-maker 流程显式传 --out。已派第六轮 fresh code-reviewer
（2026-09-02～2026-09-03 期间的 30 条 Notes 条目，2026-09-19 再搬 2026-09-03～2026-09-09 期间的 8 条，已归档到 progress.archive.md；正文保留最近 50 条）
（归档指针：累计搬出 16 条，最近一次 2026-09-20 搬 16 条；原文见 progress.archive.md「Archived Notes」）

## 将来事（低概率/成本高/暂不划算）
- **自建 agent benchmark**：OpenHands 用 SWEBench 客观衡量「框架变好没」，cc-base 全靠人肉判断。自建 benchmark 成本极高，现阶段不做，将来项目规模大到需要客观回归指标时再考虑。（2026-06-15）
