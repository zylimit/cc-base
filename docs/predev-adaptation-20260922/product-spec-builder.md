# product-spec-builder 回向适配来源表（2026-09-22）

依据：`codex-base` 分支 `improve/predev-synthesis-20260922` @ `d1a3287`（只读克隆路径见派单）；T4 判据见 `docs/predev-skills-diff-20260922.md` 第 1 节「product-spec-builder」；来源标注对照 `docs/predev-synthesis-20260922.md` 对应节。本表只记录本仓 `.claude/skills/product-spec-builder/SKILL.md` 的章节级来源，不代表已获授权修改本仓其它 Skill。

| 本仓原章节 | 适配后章节 | 来源（K 本仓 / C codex-base / 合并） | 一句理由 |
|---|---|---|---|
| [任务] | [任务] | 合并 | K 的破题句与 0-1/迭代模式框架保留；迭代模式段落并入 C 的「区分需求变化与 Agent 理解错误、纠正传播到下游」，并把「直到挑不出错」收窄到当前在谈的范围，不拖住已谈清楚的部分。 |
| [依赖检测] | [依赖检测] | 合并 | K 的启动时机保留；判据换成 C 的「沿用实际 PRD/Spec/会话结论，`Product-Spec.md` 只是无既有来源时的新建默认名，缺它不当迭代前置阻断已选路径」，并指回 [启动检查] 的多文件扫描避免重复。 |
| [文件结构] | [文件结构] | 合并 | K 原树形、注释与按需读取说明不动；新增三个 C 教学材料条目（discovery-methods / discovery-examples / reservation-case）及何时读取的说明段。 |
| [四条底线] | [四条底线] | K | 未改动——四种事实标签、矛盾并列、问题成本、引导不指路、地基优先判定是 T4 判「K」领先的内容，本来就有。 |
| [交互深度分档] | [交互深度分档] | 合并 | K 四档判据、问题预算不动；委托档追加 C 的「可逆细节委托后直接执行，不因进了复述/迭代阶段又翻出来问」一句。 |
| [向人学业务] | [向人学业务] | 合并 | 四条线、追问句、怎么理解答案、答案怎样改变下一步均为 K 原文不动；仅收尾一句衔接改指向新增的 [从任务收敛到完整结果]，内容判据未变。 |
| （新增） | [从任务收敛到完整结果] | C | 整段搬入 C 的四个子方法：把价值与代理指标分开、最小范围保留结果链、区分对象意图与状态、AI 用途成立后再定义质量；判据、顺序未改写，末尾加一句指向 references/discovery-methods.md 对应分支。 |
| [覆盖扫描] | [覆盖扫描] | K | 未改动。 |
| [AI 的专家责任] | [AI 的专家责任] | K | 未改动。 |
| [对话示例] | [对话示例] | K | 未改动。 |
| [复述理解] | [复述理解] | K | 未改动。 |
| [收敛条件] | [收敛条件] | 合并 | K 原五条收敛判据（待定表清零、来源标记、矛盾清零、spec-self-review、复述通过、分档即收敛）不删不改；追加 C 的「收敛看当前范围、结果链在范围内走通、非阻塞未知不拦已走通部分」为首条。 |
| （新增） | [签字前覆盖清单] | 合并 | 从 codex-base 合成版 `.agents/skills/product-spec-builder/SKILL.md` 的 [签字前覆盖清单] 整节搬入（该节本身在合成版标「合并」）；九个覆盖格的「判定所依据的实际内容」改写为指向本仓既有章节（[向人学业务] 四条线、[收敛条件]、[从任务收敛到完整结果]、[覆盖扫描]、[文档生成]、[AI 的专家责任]、[四条底线]、[工作流程（迭代模式）]），格子本身与判据不变。 |
| [文档生成] | [文档生成] | K | 未改动。 |
| [下一步判定] | [下一步判定] | 合并 | K 三行数字阈值（核心功能 ≥6、数据实体 ≥4 等）一字不删不改；追加 C 的「数字是快速信号不是唯一判据，结果链显示跨模块协调时即使不达标也按『做』，达标但结果链单一自洽可在跳过理由里说明」一句，不替换原阈值只加校正条件。 |
| [工作流程（迭代模式）] | [工作流程（迭代模式）] | 合并 | K 原六步骨架保留；第二步嵌入 C 的「先分清需求变化还是 Agent 理解错误」；在原「改 Spec」与「追加 CHANGELOG」之间插入 C 的纠正传播为新第五步（向 Architecture-Design.md / DFX-Spec.md / Design-Brief.md+DESIGN.md / DEV-PLAN.md 传播，只更新 owned 范围，其余交主 Agent 同步），原第五、六步顺移为第六、七步。 |
| [交接] | [交接] | K | 未改动。 |
| [启动检查] | [启动检查] | K | 未改动。 |
| [初始化] | [初始化] | K | 未改动。 |

新增 `references/discovery-methods.md`、`references/discovery-examples.md`、`references/reservation-case.md`：来源 C，codex-base `d1a3287` 教学材料整文件搬入，文件头加声明「codex-base `d1a3287` 教学材料；预约例是虚构案例，其中数字、命令、路径不自动成为本项目要求」；唯一宿主适配是 discovery-methods.md 末节「当前 Codex 会话」改「当前 Claude 会话」，方法正文未改写；[文件结构] 已同步登记路径与读取时机。

## 拿不准的合并决定

- [从任务收敛到完整结果] 的落点选在 [向人学业务] 之后、[覆盖扫描] 之前（对应 C 原书顺序：从现行工作理解需求 → 从任务收敛到完整结果 → 需求维度）；cc-base 没有独立的「需求维度」章节（对应内容分散在 [覆盖扫描]/[文档生成]），故新章节直接衔接到 [覆盖扫描]，衔接句是本次新写，不在 C 原文里。
- [下一步判定] 的数字阈值判据本身不在 T4 本节「判 C」的清单里（T4 只在 [从任务收敛到完整结果] 一行提到「避免由功能数量阈值自动决定架构/DFX」），因此保留了 K 的原阈值不动，只追加一句校正条件，未整段替换判据——如果这个理解偏窄（应该更大幅度改写 [下一步判定] 本身），请指出。
- [签字前覆盖清单] 九个格子的「判定所依据的实际内容」是本次重新撰写（指向本仓章节名），不是 C 原文逐字翻译，因为 C 原表指向的是它自己的章节结构；格子名称（覆盖格第一列）保持与合成版一致。

## 第二轮修正（2026-09-22，Codex 审 + 线 4 对等）

依据：`docs/handoff/codex-review-cc-adaptation-20260922.md` 第 1 节「product-spec-builder」HIGH P1；ccb 线 4 验收表对第三方问卷 `[确认]` 判据的对等要求（对照 codex-base `665dfe9` `third-party-questionnaire.md`）。只改 `.claude/skills/product-spec-builder/`，不动其它 Skill、hooks、rules、tests。

**1. HIGH P1：实际来源只停在 [依赖检测] 入口，没贯穿迭代执行**

原判：Codex 审查指出第一轮只在 [依赖检测]（原 15-16 行）允许用户指定实际来源，但 [工作流程（迭代模式）] 第三步仍写死「加载 Product-Spec.md」（原 205 行）、第五/六步仍把更新目标写死为 `Product-Spec.md` / `Product-Spec-CHANGELOG.md`（原 207-208 行）、[启动检查] 第二步扫描没有「已指定来源则跳过」（原 221-223 行）、`references/workflow-iteration.md:7` 仍把「项目已有 Product-Spec.md」当成迭代情境的唯一触发描述。

改法：把「选定的需求来源路径」与「其变更记录路径」定为贯穿全程的一对变量，只在没有既有载体时才落回默认名。具体改动：
- SKILL.md [依赖检测]（现 15 行）：迭代模式确定来源的同时，一并确定它旁边的变更记录路径（来源同名加 `-CHANGELOG.md`，或用户指定），并写明这一对「来源」与「变更记录」贯穿 [启动检查]、[工作流程（迭代模式）]、[交接]，中途不切回默认名；`Product-Spec.md` / `Product-Spec-CHANGELOG.md` 只在没有既有来源与变更记录时才是新建默认名。
- SKILL.md [工作流程（迭代模式）] 第三步（现 205 行）：「加载 Product-Spec.md」→「加载 [依赖检测] 确定的来源」。
- 第四步（现 206 行）：「改 Spec」→「改来源文档」，避免读成固定文件。
- 第五步（现 207 行）：「本 skill 只更新 owned 范围内的 Product-Spec.md 与 CHANGELOG」→「……的来源与变更记录」，「写进 CHANGELOG 交主 Agent」→「写进变更记录交主 Agent」。
- 第六步（现 208 行）：「追加 Product-Spec-CHANGELOG.md」→「追加 [依赖检测] 确定的变更记录（……没有既有载体时默认 Product-Spec-CHANGELOG.md）」。
- SKILL.md [启动检查]（现 221-222 行）：第一步后加一句「[依赖检测] 已确定实际来源的跳过本步，直接进第三步」；第二步改成「未指定来源才扫描项目目录找需求文档」。
- `references/workflow-iteration.md`：frontmatter `description` 与 [使用时机]（原第 7 行）「项目里已有 Product-Spec.md」→「项目里已有 SKILL.md [依赖检测] 确定的需求来源（没有既有载体时默认 Product-Spec.md）」；[更新] 节（原第 59 行）「追加 Product-Spec-CHANGELOG.md」同步改成「追加 SKILL.md [依赖检测] 确定的变更记录（没有既有载体时默认 Product-Spec-CHANGELOG.md）」——这一行 Codex 原文没有单独点名，但与第 7 行同类问题（硬编码变更记录文件名），留着会在下一轮复核里被同样拦下，顺手一并改。
- 未动的默认名残留：[任务] 开篇句（现 8 行，0-1/迭代模式共同的使命陈述，先于 [依赖检测] 出现，指的是产出物的规范名）、[文件结构] 模板注释（现 33 行，描述模板自身默认输出名）、[文档生成]（现 188 行，该节只在 0-1 模式调用，0-1 模式按 [依赖检测] 定义本就没有既有来源，`保存为 Product-Spec.md` 是该模式唯一正确行为，不是遗漏）——三处均可说清是「默认名」语义，不再改写。

**2. 对等项：`references/third-party-questionnaire.md` 的 `[确认]` 判据**

原判：ccb 线 4 验收表指出「答复回来……标 `[确认]`」的判据过松——只要问卷有回复就标 `[确认]`，未区分答者是否有权拍板；对照 codex-base `665dfe9` 同文件，其判据是「经用户或有权决定者明确确认才标 `[确认]`；非权威答复先标 `[推断]` 并写明来源」。

改法：[落在哪、怎么收] 节把原句「每条答案进 Spec 对应条目，标 `[确认]`，来源写「<谁>，<日期> 问卷答复」」改写为「来源写「<谁>，<日期> 问卷答复」，并区分亲历 / 转述 / 推测及答者对该决定的实际权限。经用户或有权决定者明确确认的事实与规则才标 `[确认]`；非权威答复先标 `[推断]` 并写明来源，不因为收到了问卷答复就自动升级」；「答复和用户之前说的对不上 → 两条并排摆给用户选」的「用户」改「有权者」，与本仓「亲历/转述/推测」既有措辞体系衔接，方法与 codex-base 一致、只是没有照搬其「`[待定]`」分支措辞（本仓 `[待定]` 语义已由 [四条底线] 统一定义，这里不重复定义一遍）。

**3. 对等项：`examples/after-sales-dispatch.md` 虚构例声明**

改法：标题下新增一行「> 虚构教学例：其中数字、命令、路径、阈值不自动成为任何项目的要求，也不可当作验收证据。」，写在原有填充范例说明之前；本仓自有范例，不标 codex-base 来源。

**验证**（原始输出）：
- `grep -n "Product-Spec.md" .claude/skills/product-spec-builder/SKILL.md`：5 处（现 8、15、33、188、222 行），逐处均可判「默认名」语义，见上条说明。
- `grep -rn "\.agents\|\.codex\|resolver\|controls\|Assurance" .claude/skills/product-spec-builder/`：无匹配（exit 1）。
- `bash .claude/tests/test-skills-lint-wording.sh`：`PASS=2 FAIL=0`，exit 0。

**更正（2026-09-22，第三轮，Codex 复核）**：提交 `025cbac` 的说明称「六份 after-sales 示例加虚构教学例声明」，计数有误——`git ls-files ".claude/skills/*after-sales-dispatch*"` 实际只有五份（`arch-designer` / `design-brief-builder` 两份 brief+DESIGN / `dfx-designer` / `product-spec-builder` 各一），均已在文件头声明虚构教学例，声明本身无缺漏，只是提交说明的计数写错。依据：`docs/handoff/codex-recheck-cc-adaptation-20260922.md`（`codex-base-ro` 只读克隆）第 24 行。此更正只改文档记录，不改任何 `.claude/skills/` 内容，也不改历史提交说明（git 提交信息不可回溯改写）。


## 2026-09-23 Matt Skills 整改同步

依据：`codex-base` `git diff 3e199f6 407c3bc -- .agents/skills`（计划 `docs/research/20260918-matt-skills-learning-plan.md`，行为对照在 `docs/research/20260918-matt-skills-study/`）。上游 `mattpocock/skills@74ca5fe`（MIT），处理方式 synthesized：只取方法，不搬 Codex 运行时概念。来源列 C = codex-base `407c3bc`，括号内为对应批次。

| 本仓章节 | 变更 | 来源（C = codex-base 407c3bc，对应批次 L1–L6；上游 mattpocock/skills@74ca5fe，MIT，synthesized） | 一句理由 |
|---|---|---|---|
| [文件结构] 按需读取说明段 | discovery-methods.md 的读取说明末尾追加「多个未知同时牵制下一步决定时，读其中的『选择下一问与记录依赖』」 | C（L1） | C 在主入口读取指针上加的同一句；本仓读取时机写在 [文件结构] 说明段而非 C 的依赖段，落点随宿主。 |
| [收敛条件] 待定表一条 | 追加：每行写明答案会解锁哪项决定（需求 / Task / 验收），答复前不依赖它的工作照常推进；没有可靠低风险默认、须有权者拍板或还得取案例才说得清的口径，不拿猜定的 `[默认]` 写成可派发开发 Task，先留作取证 / 决策依赖 | C（L1） | 对应 C [交互原则] 里「阻塞项说明答案会解锁哪项决定、尚不能写出行为与验收的部分不伪装成开发 Task」；本仓无 [交互原则] 节，落到管待定表的收敛条件，并与本仓「有可靠低风险默认才用 `[默认]`」的底线对齐。 |
| references/discovery-methods.md | 新增「选择下一问与记录依赖」一节（位于分支 1 之前） | C（L1） | 原文照搬；「沿主入口的第三方问卷分支」改为直接链到本目录 third-party-questionnaire.md，「权威需求」改「来源文档」。 |
| references/discovery-examples.md | 末尾新增「下一问解锁什么」例（技师 / 验收员 / 完成率分子分母） | C（L1） | 例子实质不改；「修订状态/统计 Task 的依赖和验收」改为「在变更记录里写明、交主 Agent 让 dev-planner 同步」，对齐本仓迭代模式第五步纠正传播只改 owned 范围的约定。 |

现有句子只在行尾追加、原措辞未改：[文件结构] 说明段「……不是项目事实。」的句号改为分号后接新句；[收敛条件] 待定表那条在原句后追加。discovery-methods.md / discovery-examples.md 文件头仍写 `d1a3287`，新增两节的来源以本表为准，文件头未改。
