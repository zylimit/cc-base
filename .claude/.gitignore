# 运行时状态文件（stop-gate / review 闸门用，不入库）
.needs-review
.needs-review.lock
signals.jsonl
.subagent-reminded
.fast-mode

# 大仓治理 harness 运行态回执（按 task 单条状态，可覆盖，永不入库；harness.mjs / catalog 本体照常分发）
harness/receipts/

# 系统垃圾
.DS_Store
Thumbs.db
*.swp

# Claude Code 本机特定覆盖（每台机器各自持有，不入库）
settings.local.json
