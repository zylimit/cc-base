---
name: arch-designer
description: 当 Product-Spec.md 已批准、需要做软件架构设计，或用户说"架构设计"、"模块划分"、"技术架构"、"分层"、"架构评审"时使用。
---

[任务]
    读取 Product-Spec.md，通过模块划分推演 + 七大设计原则自检，输出一份可执行的架构设计文档 Architecture-Design.md（模块边界 / 依赖规则 / 一致性约定 / 运行与部署包络 / 关键决策 ADR / 押后决定 / 扩展点）。中大型项目同步产出 `.claude/harness/module-catalog.json` 骨架——让架构约束进 arch-check 机器闸门，防腐不靠自觉。
    文档只写**不变量**：两个各自独立开发的单元会在这上面做出不兼容选择的决定；其余（栈、目录树、完整数据形状）是种子，代码一出现就归代码。
    已有 Architecture-Design.md 或 ADR 时按需做增量——只更新这次业务变化涉及的模块边界 / 依赖 / 决策，不默认推倒重写整份文档或重新走一遍全部维度；新起项目仍走 [工作流程] 的全套推演。

[依赖检测]
    Skill 启动时第一步自动执行：

    必需：
    - Product-Spec.md → 缺失则提示先调用 /product-spec-builder；但缺 Product-Spec.md 不等于不能讨论——用户已给出具体片段、局部模块或某条 ADR 决策时，可先就这部分展开，只在要落盘完整文档前提示先补 /product-spec-builder

    可选（降级模式）：
    - 已有项目代码 → 有则先跑 `node .claude/harness/harness.mjs arch-check`（如已有 catalog）摸清现状依赖图，进入「演进模式」：先读够真实代码，尊重并 ratify 现存约定，只画增量与迁移路径，不推倒重来
    - 已有 Architecture-Design.md / ADR → 代表已声明的设计意图，不代表代码现状；连同上一条的代码现状（实现事实）与 Product-Spec 业务规则，三者分开核对——不一致处点出来，不能把目录结构或旧 ADR 当成当然还成立的业务分工
    - DFX-Spec.md → 有则把质量属性场景作为架构驱动因子（没有就在关键决策处标注 [待 DFX 补充]，并在收尾引导 /dfx-designer）
    - Design-Brief.md → 有则页面与组件数量、Class C 复杂交互（画布 / 时间线）是模块边界与 spike 的输入

[用业务事件检验边界]
    **先追演，再画模块**：选 Spec 里一条代表性业务路径，从发起者与触发事件开始，依次说清谁判断、谁修改哪个事实、谁收到结果、下游据此做什么；先用业务动词和对象画简短协作线，再映射到代码职责——部门、页面、数据库表与部署服务都不天然等于模块。Spec 里的例子够用就直接用；业务含义不清的地方，说明不同答案会怎样改变边界，问用户补足，不要求业务人员选消息队列或一致性术语。

    **定事实归属，划不能破坏的规则**：对共享的业务事实注明权威来源、允许发起修改的岗位 / 外部系统、执行校验与写入的模块、只读消费者和历史 / 派生视图。catalog（有该文件时）的模块归属与 riskTier 表示维护路径与影响半径，不能代替业务权限——两个模块都读得到同一份数据不代表都能改它。用一个可判定的不变量说清为什么要协调：同一资源不能被两次有效占用、状态变更必须满足已确认前置等；先承接项目真实规则，再决定用一次事务、版本校验还是明确的跨模块协议来守，不凭技术方便选最终一致而默改对用户的承诺。

    **在断点上追演异常与纠错**：在这条路径上挑与当前设计相关的断点——请求重复、并发更新、外部成功但本地未收到回应、步骤完成一半、撤回或更正——逐步写出当时保存了什么、谁知道什么、用户会看到什么、重试 / 人工介入如何继续。区分「结果未知」「已拒绝」「已完成」；补偿、撤销和追加更正的业务含义不同，不能统一叫 rollback，只有业务允许逆转的动作才能安排自动补偿。资料不足时保留未知及其影响，不用任意重试掩盖它。

    **拿改变决定的方案做比较**：给出推荐时按这条业务路径说清替代方案的代价——一致性、响应等待、故障隔离、维护能力、迁移与回退。原进程内的模块已能守住边界就不为画图拆服务；确需拆分时写明现实驱动和运维责任。可逆的技术细节在授权范围内直接定；会改变数据权、失败承诺、明显成本或公共契约的未知才问用户。重要决定写清适用条件、未选方案的理由与复评触发条件，不只列技术优劣。

    跨职责写入、外部结果不确定或存量迁移影响边界时，读 references/collaboration-walkthrough.md 的协作断点走查。

[以关键场景评价方案]
    **从驱动因素形成可区分的场景**：先提取必须保持的业务不变量、目标与来源、团队 / 存量 / 成本约束，以及会改变选择的未知。质量目标还没定量时也可先细化场景——谁在什么条件下触发什么、影响哪个对象、必须怎样响应、用什么结果判断；候选目标与已确认承诺分开写。按业务后果、出现条件和不确定性解释为什么先分析这些场景；这个优先顺序不因规模分级（见 [规模分级]）放宽——安全与数据丢失类要求不许因为档位小而跳过、不许 WAIVED。正常、失败、恢复和变更只展开当前有区分力的部分，不强制走完整工作坊。

    **让候选经受同一个情境**：从现状或最小可行方案开始，对每个实质候选追演相同输入、负载 / 故障条件、事实写入与用户结果，保持评价口径一致。方案少时不凑数量；存在重大未知时不能只写推荐方案的正常路径、却只给其他方案粗糙的评价。C4 等视图按读者和问题选用，图的层级齐全不证明选择正确。

    **找出敏感假设与跨属性代价**：指出哪项负载、外部能力、恢复条件或维护能力的变化会改变结论；解释改善可靠性为何可能增加等待、重复副作用、存储、隐私或运维成本。依赖共享数据库或控制面时，不假设故障相互独立。不必为每项打数字分，也不能从架构推演直接预测精确的生产时延。

    **先定反证，再选最小实验**：写出「如果观察到什么，就推翻或缩小推荐」，再决定纸面推演、契约查证、局部原型、隔离故障试验还是负载测量。记录输入 / 环境、判据、拟执行者、观察与设计影响；未运行的只写拟验证，代码结构检查不能替代业务结果实验。验证广度与执行角色仍按项目审批三档（LOW / MEDIUM / HIGH）与 DEV-PLAN 走，架构阶段不新增原型等级门禁。

    **给有条件的决定并交接**：推荐要连到驱动因素、同场景比较、未选理由、剩余未知和复评触发条件；可从已确认的事实和授权范围内的部分先落盘，未知只阻断依赖它的承诺。把最重要的未证实假设交 /dfx-designer 或 /dev-planner 安排验证，不是写完 ADR 就当全部验证完成。

    重要候选、质量取舍或敏感假设影响当前决定时，读 references/decision-evaluation.md 的评价方法与表格；清楚的单模块小改只说明归属与相关契约，不强制填全表格。

[按需要区分状态归属]
    当前范围涉及交互或异步结果时，分别辨认业务权威事实、临时 UI 状态、视图偏好、请求 / 任务状态和资源资产；说明权威来源、生命期、修改者与消费者即可，不要求五类都建独立 store / 服务。业务状态不能由 DOM、聊天摘要或请求是否结束代替；偏好也不能误当领域事实持久化。
    用时间线追演用户编辑撞上旧结果到达：提交针对对象 A 的版本 7，等待期间用户把 A 改成版本 8；版本 7 的成功只能说明原提交发生了什么，不能直接覆盖版本 8 或声称它也已保存。按真实契约决定保留、提示冲突、合并还是重新应用，并明确哪个动作形成新意图；具体技术留给 dev-builder 阶段选，业务含义未知就问用户，不能拿姓名、对象名或响应先后替人决定。
    资源上传、生成、删除或外部动作涉及时，再核对资产归属、输入信任、访问权、保留和不可逆后果；不为普通查询页默认加队列、MCP、命令总线或 Agent 执行框架。

[文件结构]
    arch-designer/
    ├── SKILL.md
    ├── references/
    │   ├── collaboration-walkthrough.md      # 用业务路径讨论架构：协作断点、候选淘汰与纠正传播
    │   └── decision-evaluation.md            # 同场景候选评价：质量场景、敏感假设、反证与最小实验
    ├── examples/
    │   └── after-sales-dispatch-arch.md
    └── templates/
        └── architecture-design-template.md

    跨职责写入、外部结果不确定或存量迁移影响边界时读 references/collaboration-walkthrough.md；重要候选、质量取舍或敏感假设影响当前决定时读 references/decision-evaluation.md；两份参考先承接 ../product-spec-builder/references/reservation-case.md 的预约例，不用示例替用户定业务规则。清楚的单模块小改按正文核对即可，不必读参考材料。

[第一性原则]
    **恰如其分**：架构的敌人是过度设计。单模块小工具不需要六边形架构——先判规模档位（见 [规模分级]），小项目一页速写，别把原型养得和支付系统一样贵。越档要写理由（模板「越档理由」表）。

    **不变量测试**：一条内容该不该进文档，只问一个问题——「如果两个独立构建的单元各自决定，会不会选得不兼容？」会、且不显然、且是真取舍，才写；否则进「押后决定」一句话带过。押后段是契约的另一半，它让文档保持薄。

    **依赖单向**：所有模块依赖必须画得出方向、说得出理由；出现双向依赖或环 = 边界划错了，回去重划。这是七大原则里依赖倒置 + 迪米特在架构级的投影。

    **决策留痕**：重大取舍记 ADR——背景与问题、决策驱动、候选与各自优劣、结论、后果、被拒备选、**执法方式**、**revisit-if**、**reversal**。够不够「重大」看三条，都占才记：撤回它代价高（reversal 写出来不是一两天的事，或者干脆是单向门）；不交代背景，后来的人会纳闷为什么这么选；当时真有拿得出手的候选，是比过才定的。缺一条就不记 ADR，在速写或一致性约定里留一行结论加一句理由——两个人的内部工具用 SQLite，没有真备选，为它编「被拒备选」比不写更糟。ADR 不设条数下限，凑出来的不算。没有被拒备选的"决策"是没想过的决策；没有执法方式的约束会漂移；没有 revisit-if，前提早就不成立了还在照着建；没有 reversal，撤不回来的选型会被当成随手能改的那种拍板。ADR 记的是架构取舍，口径记的是领域判据（这个域里某件事按什么算，见 .claude/rules/domain-rulings.md）——分不清就问「它是不是在几个候选方案里做了取舍」，是则 ADR，否则口径；一条 ADR 可能带出若干条口径，反过来口径不构成 ADR。

    **可执行优先**：写进文档的架构约束，能落 machine 闸的就落——forbiddenDependencies / layers 进 module-catalog.json，让 arch-check 每次变更盯着；只留在文档里的边界三个月后就是废纸。

    **包络不沉默**：运行与部署（环境、部署拓扑、基础设施与供应商、运维）每一项都得是「决定 / 押后 / 未定」三态之一；整段留白是评审要挡的失败，不是「以后再说」。

    **联网优先**：架构风格、框架能力、中间件选型、starter 模板 → 先 WebSearch 确认当前版本与已知问题再定；不凭过期记忆做架构决策。

[七大设计原则——推演与自检双用]
    设计模块与接口时用它推演，产出后用它逐条自检。每条给出「类级含义 → 架构级投影 → 违反信号」：

    1. **开闭原则（OCP）**：对扩展开放，对修改关闭——模块应可在不改源码的前提下被扩展。
       架构级投影：识别 Spec 里的易变轴（支付渠道 / 数据源 / 导出格式 / 规则策略），在架构上预留扩展点（策略注册表 / 插件位 / 事件钩子），新增一种 = 加一个实现文件，不动既有代码。
       违反信号：需求清单里"新增一种 XX"会导致改动扩散到 N 个既有文件。
       只对 Spec 已确认、或已出现第二种实现的变化轴建扩展点；未确认的假想扩展只记录触发条件，不为它先搭抽象层——不是每处"新增一种"都要求强制抽象，抽象本身也有维护成本。

    2. **依赖倒置原则（DIP）**：高层不依赖底层，两者都依赖抽象；抽象不依赖细节，细节依赖抽象。
       架构级投影：分层只许向内依赖（业务核心不 import 框架 / 驱动 / UI；由外层实现内层声明的接口）——即 catalog `layers` 的理论根基。以抽象为基准搭的架构比以细节为基准稳得多。
       违反信号：领域逻辑里出现 SQL / HTTP 客户端 / UI 框架 import；换个数据库要改业务代码。

    3. **单一职责原则（SRP）**：一个类（模块）应该仅有一个引起它变化的原因。
       架构级投影：模块按「变化原因」切，不按"看起来相关"切——计费规则变只动 billing、报表格式变只动 reporting。一个模块的 owner 应能一句话说清它对谁负责。
       违反信号：一个模块的变更记录里混着两拨互不相干的需求方；改 A 功能总是顺带碰坏 B。

    4. **接口隔离原则（ISP）**：客户端不应被迫依赖它不需要的接口；依赖建立在最小接口上。
       架构级投影：模块间契约按消费者裁剪（每类消费者一个窄契约），不做一个胖"总接口"喂所有人；接口方法尽量少但要适度，为可预见的变更留余量。
       违反信号：改一个接口方法，一半不用该方法的消费者也要重编译 / 重发版。

    5. **迪米特法则（LoD，最小知道）**：只和朋友交流，不和陌生人说话——一个对象对其他对象保持最少了解。
       架构级投影：模块只依赖直接协作者，禁止跨模块伸手拿第三方内部结构（A→B→C 的链式挖取）；需要跨界就升格成显式契约。catalog `forbiddenDependencies` 是它的机器化（如 analytics 永不许碰 pii-store）。
       违反信号：import 路径深入别的模块内部目录（`other-module/internal/...`）；改 C 的内部结构导致 A 崩。

    6. **里氏替换原则（LSP）**：所有引用基类的地方必须能透明地使用其子类对象，程序逻辑不变。子类可扩展父类功能，但不能改变父类原有功能：入参可更宽松、返回须更严格、不覆盖非抽象方法。
       架构级投影：契约演进不背叛消费者——同一接口的新实现 / 新版本不得收窄输入、放宽输出、改变错误语义；API 版本化遵守"新版本可替换旧版本"。它是开闭原则得以成立的基石。
       违反信号：某实现"大体兼容但有几个特殊情况"；消费者代码里出现 `if (impl instanceof X)` 分支。

    7. **合成/聚合复用原则（CARP）**：优先用组合（has-a）/ 聚合（contains-a）复用，而非继承——在新对象里使用已有对象，通过委派达到复用。
       架构级投影：服务/模块间复用走组合与编排（调用、管道、装配），不走"继承一个基座服务再改"；共享逻辑下沉为被组合的库模块，而不是共同祖先。
       违反信号：出现超过两层的继承树；"基类"每加一个字段全体子类跟着变。

[规模分级]
    先按 Spec 判档，再选流程深度（宁轻勿重，用户可显式要求升档）：
    - **S 档（单模块 / ≤5 个功能 / 单人短周期）**：轻量模式——一页架构速写（命名范式 + 技术栈 + 目录结构 + 3 条以内关键决策 + 包络一行 + 押后一行），跳过 C4 与 catalog，七原则只做口头自检。
    - **M 档（2-6 个模块 / 有明确边界诉求）**：完整 Architecture-Design.md（模块表 + C4 上下文与容器图 + 一致性约定 + ADR + 押后），catalog 骨架可选。
    - **L 档（≥7 模块 / 多人协作 / 10 万行以上或奔着 60 万行去）**：完整文档 + **必产出** module-catalog.json 骨架（modules / dependsOn / forbiddenDependencies / layers / riskTier），接通 arch-check + impact；建议紧跟 /dfx-designer 把五性档位补上。

[设计维度清单]
    在推演过程中逐项落定（顺序不拘，缺项别硬编）：

    **必须落定**：
    - 架构风格与命名范式：分层单体 / 模块化单体 / 六边形（端口适配器）/ 事件驱动 / 微服务——给 2-3 个候选风格 + 各自 trade-off，让用户选或给出推荐理由；用一个叫得出名字的范式（六边形、管道过滤器、actor…）把它的层映射到目录，一个名字带来一整套约定。默认偏保守：**单体优先，边界清晰的模块化单体是多数项目的正确起点**，微服务要有组织与运维理由。
    - 模块划分表：每模块 id / 职责一句话 / 对外契约 / 依赖谁 / 禁依赖谁 / 变化原因（SRP 检验）/ riskTier。配 C4 上下文图与容器图（mermaid），图即规则。
    - 依赖规则：分层清单（外→内）+ 越界禁令；数据是否可跨模块直读（默认禁，走契约）。
    - 数据与状态：谁拥有哪些数据（single writer）；共享数据经谁的契约暴露。
    - 一致性约定：独立构建者会各写各的那些默认——命名（实体 / 文件 / 接口 / 事件）、数据与格式（id / 日期 / 错误形状 / 信封）、状态与横切（变更方式 / 错误处理 / 日志 / 配置 / 鉴权）。一行一条，能被 code-review 对照。
    - 运行与部署包络：环境（本地 / 测试 / 生产）、部署拓扑、基础设施与供应商、运维（日志 / 监控 / 备份 / 发布回滚）——每项决定 / 押后 / 未定三选一，不留白。
    - 关键决策：风格选型 / 存储 / 模块间通信三样必须有结论；其中过了 [第一性原则] 决策留痕那三条的写成 ADR，没过的一行结论加一句理由。ADR 每条含背景与问题 / 决策驱动 / 候选与优劣 / 结论 / 后果 / 被拒备选与理由 / 执法方式 / revisit-if / reversal。后三项都被 `adr-check` 机器校验，缺一条即 fail 并点名该条 ADR：
      - **执法方式**：必须包含至少一个可识别 token——catalog check id / fitness 规则 id（如 no-secret-literal）/ harness 能力名（arch-check / layers / forbiddenDependencies / fitness / verify / receipt）/ 或显式人工标记（"人工评审"）；指向不存在的闸（幽灵引用）会 fail——读起来像被执法实际没有，比不写更糟。
      - **revisit-if**：写**什么条件出现时这条决策不再成立**，写条件不写日期（合格：日活超过 5 万 / 需要多租户 / 团队超过 3 人 / 这个第三方停止维护；不合格：三个月后再看 / 视情况 / 持续关注）。日期式空话会 fail——它到没到期跟前提变没变没关系，而前提早就不成立了还在照着建，等发现时是整片返工。判据保守，混着日期写了真条件的放过。
      - **reversal**：写**撤回它要做什么、大概多久**（合格：换掉 ORM——改 storage 模块的 12 个文件 + 一次数据迁移，约 3 天）。**写不出撤回步骤的就是单向门**：值里显式写「单向门」，不算 fail，但会单列进 `adr-check` 的单向门清单，并要在 §9 押后决定或 §11 风险里点名、交给用户拍板——不可逆的选型不该按可逆的速度拍。
    - 押后决定：有意不在这一层定的事，每条一句为什么可以等、什么条件下要回来定。

    **尽量落定**：
    - 扩展点清单（OCP）：Spec 里已知的易变轴 → 对应扩展机制。
    - 关键场景走查：拿 2-3 个最重要 / 最难的用例在模块图上走一遍（数据流 + 调用链，配一张 mermaid 时序或动态图），验证边界划得动。
    - 演进路线：本版不做但已预留的（如"先单库，分库时只动 storage 模块"）。
    - 目录结构骨架：模块 → 顶层目录的映射（给 dev-planner 直接用）。
    - 术语表：模块 / 实体 / 事件的唯一叫法，与 Spec 一致。
    - 风险与技术债：已知的、有意背的，各自的回收条件。

    **不在本 skill 范围**（别越界）：
    - UI 视觉（归 design-brief-builder）；逐 Phase 任务拆分（归 dev-planner）；具体类图 / 函数签名（归 dev-builder 编码期）。

[推演策略]
    **两条路先选**：默认 Coaching——开放式「你怎么想 X」把决定从用户嘴里拉出来，承重的选择（范式、栈或 starter、主要边界）摆出候选与倾向让用户定；用户明说要快才走 Fast——先按判断起草整份文档，推断处标 `[假设]`，用户审阅时纠正。两条路都不许静默替用户拍板。
    **后果与能力先问**：定风格前先问两件事——「哪个模块坏一小时最疼、疼在谁身上」（故障后果决定边界与隔离在哪）和「团队现在会什么、几个人维护」（团队能力是风格上限，两人团队上微服务是负债不是架构）；答案写进 ADR 背景。数据归属问「这条数据谁改、谁只看、改错了谁兜底」，single writer 由此定，不由表结构定。
    **选择题优先**：风格 / 存储 / 通信每个决策给 2-3 个具名候选 + 一句话 trade-off（"模块化单体：部署简单、边界靠纪律；微服务：边界硬、运维贵"），不问开放题。风格定了再问存储与通信——后两样的候选随风格变，风格没定就问等于白问；存储与通信多半互不牵连，同一轮问。
    **绿地推荐 starter**：栈还开着的绿地或小项目，先 WebSearch 当前公认的 starter（含版本），推荐它——一个好 starter 免费预决定一整片连贯的架构，比手搓强；版本钉死写进技术栈表。
    **棕地先读代码**：已有代码库先读够真实代码与现有约定（arch-check 的 undeclaredDependencies = 事实边界），ratify 而不是重新发明；新设计从事实出发画迁移箭头，不画空中楼阁。
    **变化驱动切分**：问"未来半年最可能变的三件事？"——易变处切细、稳定处切粗（SRP + OCP 落地法）。
    **矛盾检测**：用户既要"快速上线"又要"每个模块独立部署" → 直接指出冲突，逼排序。
    **一致性走查**：模块表定稿前，拿 Spec 的每个核心功能问"这个功能落在哪个模块？跨几个模块？"——落不进去 = 划分有洞；跨太多 = 内聚不足。
    **包络扫描**：收口前把运行与部署四项逐项问一遍，没决定的写押后并给回来定的条件，不许整段空着。

[信息充足度判断]
    必须满足才可生成文档：
    - ✅ 架构风格已选定且有被拒备选
    - ✅ 模块划分表完整（每模块过了 SRP"变化原因"检验）
    - ✅ 依赖方向可画成无环图；禁边已列
    - ✅ 一致性约定至少覆盖命名 / 数据格式 / 错误处理三行
    - ✅ 运行与部署包络四项各有三态之一
    - ✅ 风格 / 存储 / 通信三样都有结论；够得上 ADR 的每条含决策驱动、候选优劣、执法方式、revisit-if、reversal，够不上的有一行结论与理由
    - ✅ 每个不变量都能回答「两个独立单元会不会选得不兼容」；答不上的已移到押后
    S 档轻量模式只须：范式与技术栈定 + 目录结构定 + 关键决策 ≤3 条 + 包络一行 + 押后一行。

[签字前覆盖清单]
    提交 Architecture-Design.md 给用户签字前，逐格记「已讨论 / 待问 / 有理由不适用」及来源；没人碰过的格子明写「没想过」。清单只覆盖当前这版决策，不要求每次小改都填满；待问项要标明它挡住哪项决定。

    | 覆盖格 | 要核的决定 | 对应本仓章节 |
    |---|---|---|
    | 业务事件与写入权 | 发起、判断、权威事实、修改者、消费者及来源 | [用业务事件检验边界] |
    | 不变量与最小契约 | 独立构建会否不兼容；命名、错误、状态与禁止依赖 | [第一性原则]、[设计维度清单] 一致性约定 / 依赖规则 |
    | 同场景候选 | 现状 / 更小方案、关键故障和恢复、未选理由 | [以关键场景评价方案] |
    | 敏感假设与反证 | 哪个观察推翻推荐、最小验证及实际状态 | [以关键场景评价方案] |
    | 状态与资源归属 | 异步旧结果、临时状态、资源生命期（适用时） | [按需要区分状态归属] |
    | 运行与部署包络 | 环境、拓扑、供应商 / 基础设施、运维逐项三态 | [设计维度清单] 运行与部署包络 |
    | ADR 与押后 | 执法方式、revisit-if、reversal、押后条件与责任人 | [设计维度清单] 关键决策、押后决定 |
    | 纠正与下游影响 | 变化后的 Spec / DFX / 计划 / 验收及旧决定是否仍适用 | [迭代与纠正] |

[对话示例]
    **示例一：后果先问**
        用户：做微服务吧，以后好扩。
        AI：先不定风格。派单服务坏一小时最疼的是谁——客服录不了单，还是维修工收不到单？
        用户：维修工收不到单最疼，客服可以先记 Excel。
        AI：那派单链路是第一隔离边界。团队几个人维护？
        用户：两个人。
        AI：两人团队上微服务是负债不是架构：三套部署、三套日志、跨服务调试。推荐模块化单体，派单模块单独进程只在它成为瓶颈时再拆；被拒备选「微服务」和理由写进 ADR-1。你坚持微服务我照做，把运维成本记进后果。
    **示例二：不变量测试**
        用户：把所有 API 的返回格式也写进架构文档。
        AI：返回信封（错误码形状、分页字段）写——两个人各写各的会不兼容；每个接口的字段清单不写，那是代码的事，进押后一句「接口字段以代码为准」。
    **示例三：反例**
        × 一上来推荐六边形架构——先判档、先问故障后果与团队能力。
        × 部署环境「以后再说」整段留白——每项写决定 / 押后 / 未定，押后要带回来定的条件。
        × ADR 只写结论不写候选——没有被拒备选的决策是没想过的决策。
        × 为凑条数给没得选的选型补 ADR——候选是编的，读的人会当真；一行结论加一句「没有真备选，因为……」就够。
        × 执法方式写「大家注意」——必须指到 arch-check / catalog check / fitness 规则或「人工评审」，幽灵引用比不写更糟。
        × revisit-if 写「三个月后再看」——日期到没到跟前提变没变没关系；写「日活超过 5 万」这种一眼能判真假的条件。
        × reversal 写不出来就跳过——写不出撤回步骤本身就是结论：标「单向门」，进 §11 交用户拍板。
        × 把 Spec 每条功能的类图画进来——归 dev-builder。

[迭代与纠正]
    业务例子、写入权或失败承诺被用户纠正时，先定位受影响的数据归属、契约、依赖、迁移与验收范围，再更新本次改动涉及的 Architecture-Design.md / catalog（有该文件时）/ ADR；保留旧决定的历史与被替代原因（新决策标注「取代 ADR-N」），不能只改图却留着旧调用约定。改完提醒用户同步 Product-Spec.md、DFX-Spec.md、Design-Brief.md 与 DEV-PLAN.md 里依赖这条决定的部分；决策变更按主控铁律「三文件同步」进 progress.md 的 Decisions。
    小任务沿已有边界说明「为何此改动仍由该模块承担」及受影响契约即可，不新拆模块，也不为它补一套完整架构问卷。未决边界只阻断依赖它的那部分工作，不阻断其余讨论。

[压力场景]
    - **Spec 太小**（单模块 CLI / 脚本）：不硬拆模块；按 [规模分级] 判 S 档，在一页速写里说明边界，仅实际需要时才维护 module-catalog.json 或 ADR。
    - **既有仓库架构混乱**：不推倒重来；如实登记现状，只把 `arch-check --record` 记下的既有债务当基线渐进收敛（配 `arch-trend --gate` ratchet）。环、禁止依赖和保护层反向边不进基线——forbiddenDependencies 是声明边界，不是可豁免的债务。
    - **用户坚持存在设计妥协的方案**：用实际维护 / 业务后果解释并给替代方案；属于可接受权衡且用户已拍板时记 ADR 后推进，被拒备选写清楚。安全类保护属性、危险操作与不可豁免的架构违规仍按原边界处理——接受风险不等于检查通过，不许 WAIVED。
    - **循环依赖无法立即解开**：保留失败与真实依赖，ADR 写明拆环计划及阻塞项；不写进基线、不隐藏边，也不因已有历史或用户接受风险就声称架构检查通过。

[工作流程]
    [启动阶段]
        第一步：执行 [依赖检测]；读 Product-Spec.md 提取功能清单、数据实体与归属、集成点、故障后果、用户量级预期、隐含合规；已有 Architecture-Design.md / ADR 时判断这次是新起还是迭代，迭代按 [迭代与纠正] 只处理受影响范围。
        第二步：按 [规模分级] 判档，向用户报档位与将走的流程深度（用户可改档）；问 Coaching 还是 Fast。
        第三步：涉及不熟的风格 / 中间件 / starter → WebSearch 确认现状与版本再进推演。

    [推演阶段]
        先按 [用业务事件检验边界] 追演一条代表性业务路径与写入权，划出模块边界候选；重要候选、质量取舍或敏感假设影响决定时展开 [以关键场景评价方案]；涉及交互或异步结果时按 [按需要区分状态归属] 分清状态。
        按 [设计维度清单] 逐项落定，运用 [推演策略]；每轮聚焦 1-2 个决策，附候选与 trade-off。
        模块表成形后立即做 [七大设计原则] 逐条自检 + 关键场景走查 + 包络扫描，过不了回炉。

    [输出阶段]
        第一步：读 templates/architecture-design-template.md，填充生成 Architecture-Design.md（根目录）；mermaid 图必须是合法语法，不放空图；模板注释不留在成品里。
        第二步：L 档（或用户要求）→ 生成 `.claude/harness/module-catalog.json` 骨架：modules（id/paths/dependsOn/forbiddenDependencies/layer/riskTier）+ layers + global + ignored 雏形；paths 按目录结构骨架预填并注明"随编码校准"；跑 `node .claude/harness/harness.mjs catalog-lint` 报告结果（新项目多数路径未存在属正常，说明白即可）。
        第三步：跑 `node .claude/harness/harness.mjs adr-check` 校验 ADR 执法引用与 revisit-if / reversal——failing 逐条修（补执法方式或改成显式人工评审、补失效条件、补撤回代价或标单向门）到 rc 0 再交付；manualOnly 清单如实报给用户（哪些决策只有人守），oneWayDoors 清单单独报（这个项目有几扇门走出去回不来），并确认每扇门都在 §9 或 §11 里点了名。再跑 `node .claude/scripts/predev-lint.mjs`（必需段、ADR 四字段、占位符）。
        第四步：三文件同步——架构关键决策即时进 progress.md 的 Decisions（依据 / 适用范围 / 取代）。
        第五步：签字前过一遍 [签字前覆盖清单]，逐格标「已讨论 / 待问 / 有理由不适用」，没人碰过的格子明写「没想过」；被待问格挡住的决定在 §9 押后决定或 §11 风险里点名。
        第六步：引导下一步：
            "✅ **架构设计已生成！**

             文件：Architecture-Design.md[ + .claude/harness/module-catalog.json 骨架]

             接下来：
             - 调用 /dfx-designer 做 DFX 设计（推荐——把可靠性/可测试性/安全性这些质量属性定成可验收指标）
             - 调用 /design-brief-builder 确定视觉方向（可选）
             - 调用 /dev-planner 制定开发计划（Phase 将按模块边界拆）"

[初始化]
    执行 [启动阶段]
