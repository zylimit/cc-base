---
name: cc-base-testing-infra
description: cc-base 的测试基建约定——bash 脚本式测试、PASS/FAIL 逐条、run-all 汇总，以及跑测前必须实测基线而非信任派单描述
metadata:
  type: project
---

cc-base 没有 pytest/vitest；测试全是 `.claude/tests/*.sh` 的 bash 脚本，`set -eu`，`pass()/fail()` 逐条计数，末尾 `==== <名>：PASS=n FAIL=m ====` + 非零退出，`cases/run-all.sh` 统一跑。新测试照这个形状写就能被收编。

**Why:** 交付物是 `.claude/` 编排配置本身，被测对象是 hook / mjs 脚本 / 退出码契约，没有语言运行时可挂测试框架。

**How to apply:**
- 断言辅助函数固定三件套：`run()` 回填 RC/OUT_JSON/OUT_HUMAN、`jval()` 用 node 取 JSON 字段、`has()` 包 grep。零外部依赖（只要 node + git），python3/pwsh 一律当可选增强。
- 每条断言打印 EXPECT / GOT，判定不依赖措辞——主 Agent 复核时只看这两行。
- 可变样例一律 `mktemp -d` 里新建 git 仓 + `trap` 清理，对本仓只读。
- **派单里写的基线数字要当场重测**：本仓有并行 sub-agent 持有文件，工作树会在任务中途变。实测过一次 `test-audit-scripts.sh` 从 rc1/PASS=37 FAIL=1 变成 rc0/PASS=38 FAIL=0，就是并行任务改了 harness.mjs。
- **沙箱里搬被测程序要按目录整拷、路径从入口推导**：harness.mjs 已拆库（import 同级 `lib/`），只 `cp` 单文件的夹具会 ERR_MODULE_NOT_FOUND，hook 拿不到契约退出码而假绿。写 `install_x()` 助手用 `dirname "$ENTRY"` 推 lib/ 路径整目录拷，别枚举模块名——后续 Phase 还会加模块。
- **给 pre-commit-check 写测必须先守 python3**：它靠 `python3 -c` 解析 PreToolUse JSON 取命令，缺 python3 时 CMD 为空、对任何输入直接 exit 0 放行——不守卫就是一整段假绿。同理 stop-gate 无 jq 时走硬编码兜底文案，「诊断必须含 X」类断言要 `command -v jq` 守卫。
- **run-all.sh 第二段有红就进不了第三段**：机制是 `STATIC_RC` 累加（整段跑完再停），不是撞见第一条红就跳出——所以日志里后面那些 PASS 是真跑过的，别误读成没执行。红锁在库期间整仓 run-all 必然停在这里。挂新脚本进第二段要照 golden / audit 块加 `command -v node` 守卫——这些脚本无 node 时是 `exit 1` 而非 SKIPPED，裸塞 for 循环会在没装 node 的机器上报假红。
- **整仓 run-all 红了先归因再背锅**：并行 agent 的在制品会把不相干的段搞红（实测撞上 `harness/lib/**` + golden 基线改到一半，`test-harness` 与 `harness-golden` 双红，与我改的测试无关）。归因手法是 `git archive <HEAD> | tar -x -C /tmp/pristine`（对本仓纯只读，不用 stash / worktree，不惊动并行 agent）+ `git init` 后在干净树上重跑那两段；干净树绿 = 红出自工作树在制品。再把自己的改动单独覆盖进干净树跑一遍整仓，就能给出「我这份不背这个红」的硬证据。
- **本机跑整仓 run-all 要按 15 分钟以上算**：这台机器装了 `claude` CLI，第三段「真触发 cases」会真起 `claude -p`（每个 case 最长 300s，共三个），CI 上那句 SKIPPED 在本地不成立。别拿默认 timeout 直接跑，用后台任务 + 日志轮询，否则超时被杀还得回头清残留进程。
- **`spawn(cmd,{shell:true})` 起的是 `sh -c "cmd"`，本机 /bin/sh 是 dash，对单条简单命令也**不 exec**，真负载是它 fork 的孙进程**。所以「杀子进程」类夹具只 `kill -9 <child.pid>` 会把孙进程孤儿化残留（在这台机器上实测复现）。detached spawn 的 pgid == child.pid，所以 `kill -9 -<child.pid>` 整组杀就够；顺带这个等式也让「拿记录下的 childPid 当 pgid 查残留」成为精确的泄漏检测，不误伤并发跑的其它测试。
- **`pgrep -af '<模式>'` 会匹配到发起它的那层 bash 自己的命令行**（cmdline 里含该字面量）。拿它当「无残留」证据会被自匹配骗；要么 `ps -eo pid,args= | awk '$2=="sleep" && $3=="300"'` 精确判，要么明说那条命中是自匹配。
- **给 harness 子命令做端到端隔离：`projectRoot()` = `CLAUDE_PROJECT_DIR || cwd`**，所以 `cd` 进 mktemp 的 git 仓 + `env -u CLAUDE_PROJECT_DIR node <本仓>/harness.mjs <子命令>` 就能拿真引擎跑假仓，对本仓纯只读。沙箱里 `release`（含它 spawn 的 `dod`）一趟 ~1.4s，逐类造文件跑十几趟也才 25s——比拿单元函数凑合值得多。
- **`release` 的 findings 名单被 `capped()` 截断（超限只留前 N + "... k more"）**：拿「点名里有 X」当判据的对照断言，必须先把别的干扰文件清场，否则规则真坏了时 X 被挤出名单，红因会错判成「对照没红」。计数则不被截断，`unlisted` 一律从 summary 的 `(\d+) unlisted` 取，别数 evidence 数组长度。
- **「同一张表抄三处」的规则（gen-manifest.sh 的 case / setup.sh copy_claude_tree / release.mjs MANIFEST_RULES）这样测**：沙箱里跑**真的** gen-manifest.sh 生成清单 → 再造运行态文件 → 断言 `release` 的 manifest 项仍 PASS；外加一条「带运行态文件重跑生成器，产物须逐字节不变」锁两侧不分叉。手搓清单只能证明 release 自洽，证不出它跟生成器一致，而分叉正是这批规则要防的事。
- **清单已把 `tests/*` 与 `agent-memory/*` 整个排除（2026-09-11 实测 146 条，gen-manifest.sh:73/75 两条 continue）**：所以改测试脚本、写自己的 agent memory 都**不再**让 `FRAMEWORK-MANIFEST.txt` 变陈，也顶不红 test-setup ⑨ / doctor——本文件早先几条「动 tests 就变陈（228 条）」的记录已作废，按这条算。逐条比对 146 条不到一秒，比 doctor 的随机抽 3 个可靠；比出来的「不符」若全落在 rules/ agents/ skills/ CLAUDE.md，那是并行 agent 的在制品，不是自己的账。
- **给 harness 子命令补行为断言就挂 `cases/test-harness.sh`，不动 selftest.mjs**：那里已有 `install_harness()` 沙箱 + `pass/fail` 计数，加断言只涨它自己的 PASS 数，selftest 的 135 不动，golden 基线已随测试减重删除、不再有重录这回事。用 `node -e` 配 `ADRJSON=` 环境变量取 JSON 字段（本段已守过 node），别引 python3——㉔b/㉔c 那两处 python3 是历史遗留，不是本脚本的依赖基线。
- **`doctor.sh` 的 manifest 那行是随机抽 3 个，别拿它当「清单新鲜」的证据**：同一棵工作树连跑 5 次实测出「✓ 抽验 3 个一致」和「! 1/3 不符」两种结果交替，rc 恒 0。我自己就被一次好签骗过，把它写进过回执。要数字就自己遍历清单逐条比 sha（228 条跑完不到一秒），别引用抽样那行。
- **`node <脚本> | tail -N` 之后取 `$?` 拿的是 tail 的退出码**，不是 node 的。踩过一次：拿它当「golden 在突变态下仍绿」的证据，实际 `harness-golden.mjs` 裸 `--strict` / 无参 / 错参一律 rc 2（用法错），真跑要 `--check --strict`。凡「命令 rc 是几」类证据一律 `${PIPESTATUS[0]}` 或干脆重定向到文件不接管道。
- **selftest / golden 不能靠「拷引擎到 /tmp 打补丁」验红**：`loadFx()` 按引擎位置往上找 `tests/fixtures/harness/`，副本跑起来是 `fixture load failed`。这两者的突变验证只能在仓内做——`cp` 备份 + `trap '...' EXIT INT TERM` 还原（trap 必须带 INT/TERM：本机跑一次 golden 超 120s 会被挪去后台，靠 trap 才保证还原），跑完 `sha256sum` 复核。带引擎路径入参的 .sh（如 test-release-manifest.sh）才走 /tmp 副本那条路。
- **CI 平台矩阵三者待遇不同**（gate.yml）：run-all 在 Windows 格 `if: runner.os != 'Windows'` 整段不跑；golden 在 Windows 按设计 SKIPPED（只接受 rc 3）；**selftest 两个平台都真跑**。所以断言落 `.sh` = 零 Windows 覆盖，落 `selftest.mjs` = 有 Windows 覆盖但要动 golden 的 tests 计数。选落点先想清要哪一边。
- **一张表有 N 条就把断言写成 N 条的循环，别只锁触发本次 finding 的那一条**：`STATE_EXCLUDE` 10 条 pathspec，最初只守住 `.runtime`，删掉其余 9 条中任意一条仍然全绿——同一缺陷类别留了 9 个同形缺口。扩成循环后逐条删验证：10 次变异每次都恰好 FAIL=2（force-add 形态 + once-tracked 形态）且各自点名自己那条。顺带一个易被 grep 骗到的点：`.needs-review` 是 `.needs-review.lock` 的子串，拿 `grep -c "$PAT"` 数「点名数」会把两条混起来，要真的把失败行打出来看是哪条才算数（实测确认 git 的无 magic pathspec 不会把 `.lock` 也排掉，两条互相独立）。
- **路径命名契约上，realpath 只该参与「判仓内仓外」，不该参与「回显」**：无条件两侧 realpath 修好了「软链传入的仓内路径」，却带进两个回退——仓内软链指向仓外时回显机器绝对路径（契约明文禁止的形态），仓内软链指向仓内时被解开成另一个名字（调用方探测的不是这个路径）。正解是「拼法优先、身份兜底」：入参拼法落在 root 拼法内就按拼法命名，拼法在外才两侧 realpath 用身份再判一次。写这类断言要七条一起摆（两条身份态 + 两条拼法态 + 三条防砖），单看任一条都推不出规则。
- **`core.mjs` 的 diff 指纹有两条互不相交的排除路径**：tracked 半边走 `canonicalDiff()` 里 `git diff HEAD -- ...STATE_EXCLUDE` 的 `:(exclude)` pathspec，untracked 半边走 `hashUntracked()` 的 `STATE_EXCLUDE_PREFIXES`（JS 前缀过滤，**不传 pathspec**）。造 untracked 文件只测到后者——删掉 pathspec 那半张表，selftest（268）、golden（20127 断言）、原有用例全绿。要测 pathspec 必须 `git add -f` 进索引，或提交后改内容。实测 `:(exclude)` 对 `ls-files --others` 是生效的，两张表是实现选择不是 git 限制。
- **selftest 里做端到端沙箱 lane 的成本比想象低**：`newGitRepo(roots,label)` + `commitFiles` + 一个自写的 `runIn(root,argv,stdin)`（spawn 真引擎、`CLAUDE_PROJECT_DIR=root`、回 `{code,out,err}`）就够把 receipt / task / review / waiver / trend / verify / gate / trace 全部按 CLI 行为层锁住，比拿纯函数凑合能测到调用点。helper 写在文件底部（函数声明会提升），lane 里直接用。
- **selftest 的 check 命令一律写 `process.execPath + ' --version'`，别写 `true` 或 `node`**：golden 把 PATH 钉成 git 目录 + /usr/bin:/bin，裸 `node` 在 hosted runner 上解析不到会变 BLOCKED 而不是 SKIPPED，`true` 在 Windows 又不存在——绝对路径两头都成立。fast-mode 的 SKIP 判定在 `whichCmd` **之后**，命令解析不到就永远走不到 SKIPPED 分支。
- **`.claude/FRAMEWORK-MANIFEST.txt` 在 `.claude/` 下，不在仓根**（我按仓根找过一次，find 不到就误报成「清单里没有条目」，差点把「变陈」写成「无条目」）。键是去掉 `.claude/` 前缀的相对路径 + TAB + sha256（内容先 `tr -d '\r'`）。`harness/lib/selftest.mjs` 和 `tests/test-supervisor.sh` 都在里面，改任一个清单当场变陈。
- **给 harness 造「便宜的截断场景」用 catalog 的 `maxTrackedPaths`**：`trackedFiles(cap)` 拿它切 `git ls-files`，写 `{"version":1,"maxTrackedPaths":1,...}` 的 side catalog 传 `--catalog`，两三个文件的沙箱仓就能让 `trace` / `catalog-lint` 报 `truncated:true`，不用造几万文件。注意 catalog 加载失败时 cap 回落成默认 10 万，对照组要确认 `truncated:false` 才算数。
- **测「读不出来」别只会 chmod 000：EISDIR / ENOTDIR 才是跨平台那一档**。把该是文件的路径造成目录（EISDIR）、该是目录的造成文件（ENOTDIR），不要权限、root 也挡得住、Windows 同义，所以断言主体放这儿；chmod 000 只当额外一档，用 `chmodCanDeny()` 现场探能力，挡不住就打可见 SKIP。selftest 的 runner 只有 pass/fail、**没有 skipped 计数**，跳过只能往 stderr 写（stdout 是 JSON）；`.sh` 那边可以自己加 SKIP 计数器——`run-all.sh` 只看退出码，不解析 `结果：PASS=…` 那行。
- **验「SKIP 分支真的会走」必须叠在候选修复之上跑**：红着的时候 lane 在前一条断言就抛了，根本走不到后面的 chmod 块，于是「只看到 1 条 SKIP 提示」会被误读成 skip 机制坏了。正解是「候选修复 + 模拟 win32/root」两个补丁一起打（`chmodCanDeny` 里插 `if (true) return false;`），全绿且 7 条 lane 各打一行 SKIP 才算证到。同一份模拟单独跑（不带修复）则证明另一半：Windows 上这些 lane 仍是真断言、不是空跑。
- **`task complete` 的全绿夹具比传说中便宜**：committed catalog（放仓内且提交，免得自己变成 untracked 改动面）+ `task start` 六字段信封（全是字符串，数组会被 `validateEnvelope` 判 missing）+ `gate`（不带 --changed，scopeSource 必须是 computed）+ `receipt write --verdict accept`，四步就能 rc 0 blockers[]。receipts/state 都在 STATE_EXCLUDE 里，中途加坏回执**不需要**再跑一次 gate。
- **`.claude/harness/receipts` 当成「文件」造 ENOTDIR 会改 diffHash**：排除表写的是 `receipts/**` 和前缀 `receipts/`，路径本身作为一个 untracked 文件不在里面。断言别去锁 diffHash，锁 state/note/unreadable 就好。
- **给 `spec` / `trace` 造截断场景时 catalog 要放到仓外**：放仓内它自己是个没人认领的 untracked 路径，impact 直接 degraded、`narrowed` 变 false，答案被第二个原因污染，断言就说不清是截断造成的。放 `mkdtemp` 的兄弟目录传 `--catalog` 绝对路径，`narrowed:true` 才干净——顺带对照组的 view 里也不会混进 "degraded" 这种会被模糊匹配蹭到的词。
- **pwsh 里「全仓扫某类文件」必须加 `-Force`**：`Get-ChildItem -Recurse` 在 Unix 侧把点开头目录当 hidden 跳过，`.claude/` 整棵扫不到——「仓里没有多余的 .ps1」这类断言于是只看到仓根那一个文件，恒真空转。CI 跑在 Windows（点目录不带 hidden 属性）所以永远照不出来，本地 pwsh 才现形；配一条 `found.Count >= listed.Count` 的非空转闸把这种失明变成红。
- **比两组路径别用 `Resolve-Path -Relative` 切字符串**：它对点开头目录回的前缀各 host 不一致（有的 `./x` 有的 `x`），`Substring(2)` 一刀切会把整张清单判成「多余」。两侧都 `Resolve-Path` 成绝对路径再 `-notcontains` 比，Windows 的反斜杠形态也自然对得上。
- **GitHub Actions 的 `shell: bash` 默认带 `-eo pipefail`**：直接写 `bash test.sh` 后取 `rc=$?` 永远取不到——非 0 当场断步，自定义的 `::error::` 那句根本不执行。一律 `rc=0; bash test.sh || rc=$?` 再判。
- **退役一份测试文件时，先把它的「脚手架自证」一起点数**：`test-hook-failopen.sh` 的 28 条里有 6 条是夹具自检（故障引擎真的给出契约外码、假引擎真的退 7），只顾着数行为断言就会把这批漏掉，而 SG/PC 那些「契约外退出码」断言全靠它们才不空转。移进去单开一组（SF），保持全绿。
- **`.claude/tests/**` 的测试脚本自己被 D-3b 那种批量迁移波及时，基线要边做边重测**：并行 implementer 会在任务中途把目标状态落进工作树（22 个 hook 的 `.sh/.ps1` 是我做到一半才被删干净的），开工时判定「现在必红」的条目，收工时可能已经绿了。
- **退役一份 `.claude/tests/*.sh` 会把 `rules/` 里指向它的「回归锁」引用变成 PHANTOM，`dod` 的 rules-audit 当场红**：删 `test-hook-failopen.sh` 之后 `harness-large-repo.md:174` 那句「回归锁：…」就成了幽灵引用，rules-audit 判 FAIL → dod rc 2 → run-all 第二段红。删测试文件前先 `grep -rn <文件名> .claude/rules .claude/CLAUDE.md`，改不到的（不在自己 Scope）写进回执点名。
- **改清单登记的 `.claude/` 文件（rules / agents / skills / hooks / harness / CLAUDE.md，**不含** tests 与 agent-memory）会让 `FRAMEWORK-MANIFEST.txt` 陈旧，test-setup.sh 的全量比对当场红、还连带把 ⑨ 的 doctor 基线断言拖红**（一个根因两处红）。清单不在 tester 的 Scope 时，证明「只是陈旧不是真坏」的办法是整仓 tar 到 /tmp、在副本上跑 `gen-manifest.sh` 再跑 test-setup.sh——本仓零改动，还能顺带照出被清单红盖住的别的红（我这次就是这样才看见 `session-rules-banner.sh` 那处漏改）。
- 相关：[[audit-scripts-fragile-zones]]、[[red-lock-test-writing]]、[[windows-gitbash-process-tests]]
- **worktree 隔离的 agent 跑不了 `pwsh`**：guard 一律拒（「runs pwsh in a plain command … cannot be shown not to run git」），换绝对路径、去掉重定向、`dangerouslyDisableSandbox` 都不行。所以在 worktree 里给 `.ps1` 写断言只能静态自查：`LC_ALL=C grep -n '[^\x00-\x7F]'` 验纯 ASCII、`grep -n '`[[:space:]]\+$'` 抓反引号续行后的尾空格（PS 的经典语法杀手）、grep 变量名确认不与既有代码撞车，再照抄同文件已有构造（`Chk` / `Get-InstalledList` / 两参 `Join-Path`）。回执里必须把「ps1 没跑过」写进 Not verified，让主 Agent 在主树补跑。
- **给 setup.sh 类安装器写路径 / 参数用例，源码树换一棵 mktemp 里的迷你树**：`validate_target` 跑在 `mkdir -p "$target"` 之前，与源码树规模无关；真树一趟 2.2s，5 个文件的迷你树近乎瞬时，四十几个用例才跑得起（⑥b 已有同款写法可抄）。整段 ⑦–⑩ 连真安装带迷你安装共 68 条断言，实测 35s。
- **doctor.sh 对「装出来的 target」恒 rc 1**：它把仓根 `make-release.sh` 当必查项，而那文件不进安装产物——所以任何「doctor 该报错」的断言在装出来的目标上都是偶然绿。造 target 时 `: >"$T/make-release.sh"` 补个 stub 才能把基线压到 rc 0，再验「中断后 doctor 转红」才有判别力（顺带：先完整装一遍再打断，别拿半空目录当靶子，否则一堆缺失文件也会撑红）。
- **`session-rules-banner.sh` 要喂 stdin**：`HOOK_INPUT=$(cat)` 会一直等，`printf '{"source":"startup"}' | env CLAUDE_PROJECT_DIR=... bash <hook>` 才跑得完；现有六条铁律横幅里不含「未完成 / interrupted / install.marker」，拿这些词做告警断言不会被原文蒙混（配一条无 marker 的对照组，期望值写死 0 命中）。
- **`/tmp/patch.py`、`/tmp/cand` 这类通用名会和并行 agent 撞车**：实测写完 `/tmp/patch2.py` 后被别的 agent 覆盖成了它自己的补丁脚本。临时脚本 / 沙箱一律带任务前缀或 `mktemp -d` 出来的目录名。
- **被测目标是「仓根 + .claude 整棵」的脚本（setup.sh / gen-manifest.sh）时，验「修得好」的办法是整仓 tar 到 /tmp 再打补丁**：这两个脚本的路径全从 `dirname $0/../..` 推，不吃入参，拷单文件必错位。`tar --exclude=.claude/worktrees --exclude=.git` 拷一份，在副本上打候选修复，跑 `/tmp/副本/.claude/tests/*.sh`——本仓零改动，还能顺手做「只修一份表」的部分修复变异，验对拍断言点名的是不是那份。
- **`.claude/worktrees/` 是 Claude Code sub-agent 隔离建出的整棵仓副本，且默认没被任何排除表挡住**（f3cd449 实测：进 manifest、被 release 判 unlisted、扰动 diff 指纹、被 setup.sh 装进目标）。给本仓造沙箱、算清单、比指纹时都要留意它；tar/find 类操作记得排除，否则会把并行 agent 的整棵工作树卷进去。
- **整拷 `.claude/` 进沙箱必须跳过 `worktrees/`**：主树里那层装着各并行 agent 的完整工作树，`cp -r` 会把整个仓复制 N 遍。在 agent 自己的 worktree 里跑感觉不到（那儿没有这层），一进主树就炸。同理 `gen-manifest.sh` 的排除表里也没有 `worktrees/*` 这条臂。
- **给「整树校验」类脚本（doctor 的 manifest 项）写测，清单一定要在沙箱里用真的 `gen-manifest.sh` 重生成**：仓里那份天然是陈的（改 tests/ 或 agent-memory/ 就陈，没有机器闸守新鲜度），沿用它会让「未篡改时不报」的控制断言变成随机红，把 implementer 送去修一个不存在的 bug。重生成后实测 232 条 0 处不符，基线才干净。
- **`doctor.sh` 的沙箱要补一个 `make-release.sh` 桩**：它只判 `-f`，但缺了它基线就 rc=1，「有不一致时 rc 非 0」那条断言当场失去分辨力（红得对但理由是假的）。凡是拿 rc 当判据的整树自检，先把基线跑成 rc=0 再说。
- **`command -v X >/dev/null && cmd && echo yes || echo no` 这种链式探测会误报「没装」**：中间任一环非 0 都落到 `|| echo no`。实测被它骗过一次——报了 shellcheck 未装，单独跑 `command -v shellcheck` 却是 `/usr/bin/shellcheck`。工具在不在一律单独一条命令问。
- **`harness-golden.mjs` 自己的所有路径从 `path.dirname(import.meta.url)` 推**（REPO_ROOT / fixtures / golden 基线三样都是），所以它的**候选实现副本必须仍放 `.claude/tests/` 下**才跑得起来，挪去 /tmp 找不到基线——和上一条「引擎不能拷 /tmp」是同一个病、不同的文件。给它写的 .sh 收 `${1:-默认路径}` 入参就能拿候选验「修得好」，零改动本仓。实测全量 `--check` 2m52s / 20127 断言 / 9 场景 + in-repo，**单场景 ~18s**——所以「按场景切片」是给突变类工具唯一跑得动的形态，别按全量估时间。
- **`harness-golden.mjs` 的 `main()` 无条件 `console.log('===== harness-golden <mode> =====')`**：给它加任何「stdout 单行 JSON」的新模式，这行 banner 必须改走 stderr，否则机器通道第一行就是横幅。这类「老模式的人读输出撞上新模式的机器契约」只有拿候选实现真跑一遍才看得见，读 main() 很容易漏。
- **沙箱 git 夹具里，「X 改了但 Y 没同步」类断言的 Y 必须先提交**：未跟踪的 Y 在 `git status --porcelain` 里本身就是一条改动，判据恒被满足、分支永远走不到。踩过一次——precompact-gate 的 C2 用例里 progress.md 是现建的未跟踪文件，`PROG_DIRTY` 恒 1，那条断言在控制组下才暴露出来是恒绿的。基线文件一律 `git add -A && git commit` 完再制造脏改动。
- **测 ESM 库的导出不用造夹具文件**：`cd` 到 lib 目录再 `node --input-type=module -e 'import { f } from "./x.mjs"; …'`——`-e` 的相对说明符按 cwd 解析，比拼 `file://` URL 省事且在 Git Bash 的 `/c/...` 路径下也不翻车。lib 目录不存在时 `cd` 直接失败、rc 非 0，天然就是红。
- **改任何被 FRAMEWORK-MANIFEST 登记的 `.claude/` 文件，`test-setup.sh` 当场红**（`agent-memory/**` 与 `tests/**` 已不在登记范围，动它们不触发）：清单存的是 LF 归一后的 sha256，得 `.claude/scripts/gen-manifest.sh` 重生成才平，红锁在库期间它会一路红着。归因手法是 `git worktree add --detach /tmp/head-wt HEAD` 在 pristine HEAD 上跑同一个测试对拍（对本仓只读，跑完 `git worktree remove --force`）——实测 HEAD 自己就带一条存量不符（上一轮 reviewer 写了 agent-memory 没重生成清单），不对拍就会把存量红算到自己头上。
- **跑套件前后各钉一次被测文件的 sha，指纹不一致就作废本次结果**：本仓有并行 agent 在改同一批 hook，实测一次运行当中 `three-file-sync-gate.mjs` 被别人改了又还原（mtime 落在运行窗口内、跑完 `git diff` 已空），TF-17 红得莫名其妙、事后什么痕迹都查不到。`sha256sum <被测文件>` + `git diff --quiet -- <目录>` 跑前跑后各一次，比事后拿 pristine 树归因便宜得多，也能当场把「假红」和「真红」分开——别拿一次没钉指纹的运行去下结论。
- **给「还不存在的子命令」写红锁，rc 要断言成恰好某个值**：本仓引擎对未知子命令是 `die(usage(cmd), 3)`——rc **3** 不是 2。写「非 0 就算抓到」的断言会被 usage 错撑绿；更阴的是 usage 文本很长，`default` / `block` / `blocking` 这些词全在里面，任何「输出含某词」的断言在命令根本没跑成时都恒真。实测 4 条这样的断言在红锁期假绿，加一条 `rc == 0` 前置后才变红。含词类判据一律与 rc 成对写。
- **引擎子命令的人读视图藏在 JSON 的 `text` 字段里**（`invariants` / `recap` 这类）：stdout 是单行 JSON，视图里的换行是 `\n` 转义，拿 `grep '^- tier:'` 扫整份 JSON 永远锚不到行首。判据要先 `d.text` 取出来再 grep。
- **给沙箱装 `.claude/harness/profile.json` 必须赶在 `git add -A && git commit` 之前**：它落在 `raise.paths` 的 `.claude/harness/**` 里，未跟踪状态会让每个 git 沙箱被档位自动抬成 strict，别的断言跟着莫名其妙地变严。同理 `catalog` / `engine:*` 这些在提交后才写 `.claude/harness/` 的夹具臂，本身就带着「该沙箱恒 strict」这个副作用——只要断言在 standard 与 strict 下同值就无害，不同值的一定要单独造干净沙箱。
- **沙箱夹具从「不装 X」改成「装 X」时，先数一遍哪些断言的前提被掀了**：给每个沙箱补上 profile.json 那次，8 条既有断言当场红——5 条是 advise 类闸（stop-gate / three-file-sync-gate / precompact-gate / pre-commit-check / no-direct-code-guard）从「静默放行」变成「不拦但要提醒 + 记账」，2 条是横幅措辞换了口径，1 条是 `[ ! -d .claude/harness ]` 这种拿目录存在与否当判据的写法失效。判据尽量落在真痕迹（`harness/state/` 有没有被写）而不是容器目录在不在。
- **pwsh 跨行表达式的续行运算符必须落在行尾**：括号内把 `+` / `-and` 放到下一行行首，parser 直接报 `Missing closing ')'` 并把后面几十行连坐（一次改动报 14 条错、真因只有 2 处）。反引号续行在布尔合取里能用，字符串拼接一律「上一行以 `+` 结尾」再换行。交付前拿 `[System.Management.Automation.Language.Parser]::ParseFile($p,[ref]$t,[ref]$e)` 做零副作用语法闸，比跑一遍快且直接给行号。
- **给 `.ps1` 薄壳造沙箱要连 `hooks/lib/` 和 `harness/lib/` 两棵一起拷**：`harness/lib/tier.mjs` import `hooks/lib/tier.mjs`，少一棵就是 `ERR_MODULE_NOT_FOUND`——rc 变 1、stdout 成 node 堆栈，几条断言一起红成「跟被测语义毫无关系」的样子。`Copy-Item -LiteralPath <目录> -Destination <已存在的目录> -Recurse` 实测是把整棵放进去（不是摊平内容），可以直接当 `cp -R` 用。
- **`/tmp/ccbase-ps1-*` 这类残留先看时间戳再背锅**：本轮 6 次运行零残留，`ls` 出来的三个是 09-03/09-04 的历史遗留（别的 session 留的）。测试自己的 `finally` 清理是好的，拿 `ls` 结果直接写「泄漏」会冤枉自己。
- **把引擎「换个位置」的副本不能摊平放，要连 `.claude/` 那层壳一起搬**：`harness/lib/tier.mjs` 里的 `../../hooks/lib/tier.mjs` 是按**引擎自身位置**解析的，副本放成 `$TMP/eng-x/{harness.mjs,lib/}` 时那条相对路径落到 `$TMP/hooks/`，副本一台都起不来。造法照真实布局写成 `$TMP/eng-x/.claude/harness/` + 同级 `.claude/hooks/lib/`。engineHash 只摘 `harness.mjs + lib/*.mjs`，多搬的 hooks/lib 不影响「内容一致 = 同一台引擎」那条断言。
- **`cases/run-all.sh` 自己也带内联夹具，改口径时别只 grep `tests/*.sh`**：它内嵌一段 node 断言 `release` 装配项名单，Phase A 把 `fast-mode` 项改名 `tier`（并多出 `gate-fresh`）之后，那份 `want` 名单陈旧，整段 run-all 停在「静态自测失败」而每个子测试都是 FAIL=0——从子测试计数上完全看不出来，只能看 `（上面这个静态测试判 FAIL）` 那行定位。名单照引擎 `RELEASE_CHECKS` 写全，别只改错的那个词。
- **动过清单登记的 `.claude/**` 文件就要 `bash .claude/scripts/gen-manifest.sh`**（tests / agent-memory 不在内）：不重生 `FRAMEWORK-MANIFEST.txt`，`test-setup ⑨ doctor 基线` 必红（报「期望 X 实际 Y」），而它红了整个第二段就判 FAIL。重生是全仓扫描，会把并行 agent 那会儿的在制品一起收进清单——重生前先 `git status` 看一眼都收了谁，回执里交代清楚。清单只比对已声明文件的 SHA，事后新增的未跟踪文件不会造成漂移。
- **沙箱目录分配器别用「命令替换里的计数器」**：`newdir() { N=$((N+1)); mkdir -p "$TMP/case$N"; printf ... }` 配 `D=$(newdir)` 时，`N` 只在命令替换的子 shell 里涨，每次都返回 `case1`——25 个用例共用一个目录，前面用例的坏文件全留给后面。写成 `newdir() { mktemp -d "$TMP/caseXXXXXX"; }` 就没有共享状态。**这个坑在「被测实现还不存在」的全红轮里完全看不见**（全红掩盖一切），只有拿候选实现跑一遍才暴露：当时 P15 那条 DESIGN.md 用例的 JSON 里冒出了 P9 的 `<数据库选型>` finding，才顺藤摸到。
- **测试日志里的截断函数必须 UTF-8 安全，否则整份日志对 grep 变「二进制」**：`cut -c1-200` 在非 UTF-8 locale 下按字节切，会把中文截成半个字，GNU grep 随后判定文件是二进制并静默不输出——我因此把「跑完了 PASS=23」误读成「脚本中途早退」，白诊断一轮。截断后接 `iconv -f UTF-8 -t UTF-8 -c`，读日志时一律 `grep -a`。
- **把 `.claude/tests/*.sh` 拷去别处跑会自己废掉**：脚本靠 `SRC=$(dirname "$0")/..` 推被测实现和示例夹具的位置，拷到 /tmp 再裸跑就是 `LINT=/tmp/scripts/...` 全红（实测一次 PASS=0 FAIL=27）。做「改夹具验对照」这类控制组时，必须把实现路径当 $1 传进去；同时 P24 那条会因示例 Spec 也按 SRC 找而自动跳过，断言总数从 28 变 27，别误读成少跑了一条。
- **给 ui-audit 这类外部引擎脚本写测，用 CJS 桩包顶掉 playwright**：mktemp 目录里造 `node_modules/playwright-core/{package.json(main:index.js),index.js}`，以该目录为 cwd 跑脚本，`resolveModule` 的 `createRequire(cwd)` 分支就会捡到桩——本机没装引擎也能验「launch 抛错」「审计判定」两条路径，不必随真渲染一起 SKIPPED。三个坑：① 桩写 `exports.chromium = {...}` 而不是 `module.exports = {...}`，否则脚本裸 `import(pkg)` 命中时拿不到具名导出、`mod.chromium` 为 undefined，会伪装成「引擎缺席 rc 3」把红锁撑绿；② 本机若真装了引擎，裸 import 会抢在 cwd 之前，得改把脚本复制进桩目录再跑；③ 假 page 的 `evaluate` 按实参个数分流——带主题参数那次是设 data-theme，不带参数那次才是 PAGE_AUDIT，用调用序号计数会随组合矩阵变化而错位。
- **要看某条用例的夹具到底长什么样，`eval` 把测试里的 `write_*` 函数抽出来复用，别另写一份**：`eval "$(sed -n '/^write_arch() {/,/^}$/p' 测试.sh)"` 之后直接 `write_arch /tmp/f madr`，验的就是用例本尊那份夹具。另手抄一份夹具去查 findings，查的是另一个东西——夹具差一个字，结论就跑偏。函数体里的 `local` 在 eval 出来的函数里照常好用，收尾的 `^}$` 是唯一的顶格右花括号，sed 范围切得干净。
- **测路径穿越必须 raw socket，`fetch` 测不出来**：undici 会先把 `/../` 规范化再发，穿越请求压根出不了客户端。用 `net.connect` 手写 `GET /../x HTTP/1.1\r\nHost: …\r\nConnection: close\r\n\r\n`，三种写法一起发（`/../x`、`/%2e%2e/x`、`/..%2Fx`），判据是「响应非 200 且响应体不含 canary」；canary 文件放服务目录的**父目录**。
- **验安全类红锁要打「中间态候选」，不能只打全量修复**：ui-audit 的穿越锁现状红在「import 就把 main() 跑掉、serveDir 没导出」，这跟漏洞本身无关。先打一版**只加导出 + main 守卫、不修穿越**的候选，看探针是否喊出 `U8-LEAK` 三条——喊出来才证明探针真看得见漏洞；再打全量修复看转绿。只跑全量那版，说不清红是「测不到」还是「真有洞」。
- **路径前缀校验挡不住符号链接，安全断言要落在 realpath 层**：`resolve()` 只做字符串归一化，不解析软链——站内一个 `link.txt -> ../outside/secret.txt` 解析后仍在 root 前缀之内，`readFile` 到那一步才跟随链接把站外文件端出来。夹具要同时放**文件链和目录链**（`dirlink -> ../outside` 再请求 `/dirlink/secret.txt`），并用 `ln -s ... 2>/dev/null || skip` 守住建不了软链的文件系统。
- **拿「上一轮自己写进去的行」当锚点前先 grep 实际内容**：上一轮若是**前缀替换**，行尾还挂着旧文本（`…第五批红锁（…）` 后面仍跟着 `，在 predev-lint.mjs 修好之前必红；…`），照记忆里的新串去匹配必然 0 命中。批量打补丁的 `edit()` 写成「全部锚点命中才写盘」是对的——这次最后一个锚点没中，整批回滚，两个文件一个字没动，比写一半干净得多。
- **要验「两闸口径一致」类修复，把整个 `.claude` 拷成副本再打补丁**：`cp -r .claude /tmp/x/.claude`，改副本里的 predev-lint.mjs / spec.mjs / ui-audit.mjs，然后跑 `/tmp/x/.claude/tests/*.sh` 与 `/tmp/x/.claude/harness/harness.mjs selftest`——测试脚本靠 `dirname $0/..` 推路径，整树拷贝后一切自洽，跨闸对拍的两侧也都用副本。比单文件拷贝省心，也避开了 harness 引擎相对路径那个老坑。
- **双引号 shell 字符串里不许出现裸反引号**：`chk "...``` 围栏里的槽..."` 会触发命令替换，报 `unexpected EOF while looking for matching \``——脚本从那一行起停止执行、连 `==== PASS=x FAIL=y ====` 汇总行都不打印，`rc=2`。看到「没有汇总行 + rc 2」先 `bash -n` 查语法，别当成用例逻辑问题。测试描述里提围栏写「css 围栏」就行。
- **用 python 给候选副本打补丁时，`re.sub` 的替换串会解释 `\n` / `\g` 等转义**：往 JS 里注正则 `[^{}\n]` 时那个 `\n` 会变成真换行，插进正则字面量就是语法错。踩过一次——副本 `PASS=1 FAIL=59`，看着像断言全写错，其实只是补丁把被测文件弄坏了。两条对策：替换一律走 `re.subn(pat, lambda m: rep, s, count=1)` 绕开转义处理；候选副本跑测试前先 `node --check` 一遍，语法不过就别往下读测试结论。
- **要断言「默认输出目录里的旧文件被作废」，把被测脚本拷进临时目录再跑**：`DEFAULT_OUT` 是按 `import.meta.url` 相对脚本位置算的，直接跑仓里那份会写到 `.claude/evidence/` 去——测试改仓内文件是硬伤。造 `$TMP/x/scripts/ui-audit.mjs` + `$TMP/x/evidence/ui-audit/`，默认目录自然落在临时目录里，断言随便写。单文件拷贝对 ui-audit 安全（只 import node: 内置）。
- **验「参数解析的坏值落到哪」要把 cwd 也放进沙箱，并预置同名诱饵目录**：`--out -x` 若被当成目录名，落点是 `./-x`——cwd 在仓里就写到仓里去了。沙箱里 `( cd "$ISO_ROOT" && node ./scripts/x.mjs … )`，同时预置 `-x` / `--strict` 两个诱饵目录各放一份 `pass:true`，断言才能同时说清「默认目录该被作废」和「诱饵目录不许被动」。只判前者的话，作废错人这类缺陷测不出来。
- **写测前先用 `node -e` 把运行时的真实行为钉死，别按印象假设**：本机 node v24 的 `parseArgs` 对 `--out --strict` 和 `--out -x` 都抛 `argument is ambiguous`（走解析失败通道），而 `--out=--strict` 解析**成功**（走另一条通道）——同一类「坏值」分属两条代码路径。不先测这一下，很容易把两条通道的用例都挂在同一条路径上，另一条其实没被覆盖。

- **harness selftest 是 .mjs 平表，不是 bash 测试**：`.claude/harness/lib/selftest.mjs` 的 `selftestCases()` 返回 `[名字, fn]` 平表，`node .claude/harness/harness.mjs selftest` 跑全表、输出 `{"ok":..,"tests":N,"failed":[{name,error}]}`，**加一条用例 N 就 +1**——这是主 Agent 验「新分支有没有真进表」的硬指标，报「补了 K 条分支」就得对上 N 的增量。三个坑：① 该文件是**混排**——中文在字符串字面量/正则里一律 `\uXXXX` 转义（`，` 大写是既有写法），注释里才写中文原文；别看到一处转义就当全文纯 ASCII（实测 86 个原文中文全在 `//` 注释里），拿 `git show HEAD:<file>` + `JSON.stringify(line)` 看真字节才算数。② 用例名全英文，断言用 `assert.ok(/re/.test(x), x)` 不用 `assert.match`，reason 类断言把被测值当第二参传进去，红了直接看得见。③ 平表**没有 skip 机制**——判不准的用例不能挂红停在库里，只能不写 + 在回执里报反例。
- **改这类长 .mjs 用 heredoc 落脚本，别用 `node -e '...'`**：脚本里但凡含单引号（锚点字符串 `"    ['assessAdrRecords"`）就被 bash 单引号吞掉、报 `Expected ',' got <eof>`；`cat > /tmp/x.mjs <<'EOF'` + `node /tmp/x.mjs` 一次过。同理往文件里塞中文夹具：quoted heredoc 落原文、再跑一段只在改动区间做 `replace(/[^\x00-\x7F]/g, → \uXXXX)` 的转换，比手工敲转义少犯错，也不会误伤区间外的注释原文。
- **把「造夹具」和「断言」两行并成一行必须用 `; ` 接，并回头对一遍 PASS 计数**：为压行数用脚本批量合并时用空格拼了，`D=$(newdir); write_plan "$D" no_anchor expect "$D" 1 '缺字段…' "L4 …"` 整串 `expect …` 全成了 `write_plan` 的位置参数——10 条断言一声不响地没跑，汇总仍是 `FAIL=0` **rc 0**，唯一的信号是 PASS 从 13 掉成 3。合并前后各记一次 PASS 数，对不上就是有用例被吃掉了；存在但没跑 = 缺，不是绿。
- **验「测试有没有牙」用 /tmp 里的变异副本，别动仓内实现**：本仓 `tests/test-*.sh` 一律把被测脚本路径当 `$1`（`LINT=${1:-"$SRC/scripts/x"}`），所以 `cp 实现 /tmp/m/ && 打一处反向补丁 && bash tests/test-x.sh /tmp/m/x` 就能验红，仓内文件 sha 全程不变（跑前跑后各 `sha256sum` 一次贴进回执）。一个变异只该打死一条断言——打死多条说明用例互相搭便车，一条没打死说明那条断言在写「跑通了就算过」。实测三发：ui-slop 的 `isBodySel` 恒真只杀 U5、predev 的 `parseMs` 去掉秒→毫秒只杀 P55、plan-lint 的 `fenced` 置空只杀 L12。

- **给安装器断言做反向验证的配方**：`git ls-files -z --cached --others --exclude-standard | tar --null -T - -cf -` 拷一份工作树进 `mktemp -d`（带上未跟踪的新文件，`git archive` 会漏），再把沙箱里的 `setup.sh` 改名 `setup-real.sh` 并写个转发 shim，在 shim 末尾 `rm -f "$1/.claude/agents/<x>.md"` / `touch` 一份多余文件来注入「装少了 / 装多了」。`test-setup.sh` 里 setup.sh 六处调用的 `$1` 恒为 target，shim 稳。顺手把改前的旧断言也拷进沙箱跑一遍——旧版在「漏装新 agent」下判绿正是它该被换掉的铁证。
- **`test-hooks-node.sh` 头是 `# risk: low`，默认 `run-all` 整套跳过**：日常 `--level high` 只报「跳过 N 套 low」，只有 CI 的 `--level all` 或单独 `bash .claude/tests/test-hooks-node.sh` 才真跑。新断言落这里必须在回执里点明「默认档不执行」，否则主 Agent 抽查整仓回归时看不见它们。
- **`subagent-acceptance-reminder.mjs` 自带去重表 `.claude/.subagent-reminded`**：同 `agent_id`（或同 session+role+transcript 哈希）第二次事件直接静默 return、零输出。一个沙箱里连塞多个用例会从第二条起全假静默，每条用例各自 `newsb` + 各给不同 `agent_id` 才算数。
- **给「提醒正文里该不该多一句」写断言，GOT 要把追加段单独打出来**：`show()` 在 260 字符处截断，铁律正文本身就把额度吃满，追加段（红因本体）会被切掉。`追加段=[$(show "${AC#*<铁律正文末尾几字>}")]` 一行就把它摘出来，没追加时显示「空」——红因一眼可判，且这个字段只显示不判定，正文改措辞最多退化成打印整段。**但这个锚点是跟措辞绑死的**：正文一换词，`${AC#*<锚>}` 不匹配就原样回整串，「追加段」看起来满满当当、实则一个字都不是追加的——所以红因必须由具名探针（`证据分类=无 只报不判=无`）承担，切片只当辅助。
- **否定式断言（「不许提某句」）必须配三个正向合取**：rc=0 + hookEventName + 正文里点名角色与铁律词。实测把 hook 的 `emit` 前插 `return`（静默变异），四条 SA 断言全红才证明否定那两条不是被空输出顶绿的。
- **手搓 stdin 探 hook 一律 `printf '%s' '<json>'`，别 `printf '<json>'`**：后者把回执夹具里的 `\n` 当转义展开成真换行，JSON 字符串内含裸换行即非法，`readStdinJson()` 返回 null → 角色退化成「子 Agent」、`last_assistant_message` 读成空、领域追加段整段不出现——长得跟「功能还没实现」一模一样，白诊断一轮。`run_script` 用的就是 `printf '%s'`，手工复现时照抄它。
- **JS 的 `\b` 在中文哨兵后面不成立，「以 X 起头」类判据别用它**：`/^(none|n\/a|无)\b/i` 对 `None（括注）` 成立、对 `无（括注）` 和裸 `无` 一律**不**匹配——`\b` 是 ASCII 词边界，`无` 不是词字符，后面跟 `（` 或行尾时两侧都非词字符、边界不存在。写中文回执夹具的判据要么按语种分臂（`^无(?![一-鿿])`），要么纯前缀（但纯前缀会把 `无线接入…` 这种真发现吞成「没有」）。拿 node 一行把候选正则对七种写法摆成表，比读实现靠谱。
- **`test-gate-audit.sh` 头是 `# risk: low`**：拿 run-all 自己的 `risk_of` / `level_covers` 实测过，`--level high` **和 `--level medium` 都跳过**，只有 `--level all`（CI）才跑。往它加红锁必须在回执里点明「默认档和 medium 档都不执行」，否则主 Agent 抽查整仓回归时看不见那几条红。
